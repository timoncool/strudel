# Wavetables (Волновые таблицы)

**Коллекция Dough Waveforms** - это набор из **65 волновых таблиц**, включающих синтетические формы волн, сэмплы инструментов и экспериментальные звуки. Основано на знаменитой коллекции AKWF (Adventure Kid Waveforms).

## 📊 Статистика

- **Всего wavetables**: 65
- **Типы**: Синтетические, инструментальные, экспериментальные
- **Источник**: AKWF и кастомные волновые формы
- **Использование**: Мелодии, басы, пэды, синтезаторные линии

---

## 🎹 Как использовать

### Базовый синтаксис

```javascript
// Простая мелодия
n("c3 d3 e3 g3").s("wt_piano")

// Бас-линия
n("c2 c2 eb2 g2").s("wt_ebass")

// С эффектами
n("c4 e4 g4 b4")
  .s("wt_epiano")
  .room(0.4)
  .delay(0.3)
```

### Октавы и ноты

```javascript
// Разные октавы
n("c2 c3 c4 c5").s("wt_sine")

// Хроматическая гамма
n("c3 cs3 d3 ds3 e3 f3 fs3 g3 gs3 a3 as3 b3")
  .s("wt_piano")
```

---

## 📋 Полный список Wavetables

### 🎵 Базовые формы волн (Basic Waveforms)

#### **wt_bw_sin** - Синусоида
Чистая синусоидальная волна
```javascript
n("c3 e3 g3").s("wt_bw_sin")          // Чистый тон
n("c2").s("wt_bw_sin").lpf(800)       // Суб-бас
```

#### **wt_bw_saw** - Пила
Пилообразная волна
```javascript
n("c3 d3 f3 g3").s("wt_bw_saw")       // Классический синт
n("c2 c2 eb2 g2").s("wt_bw_saw").lpf(500)  // Синт-бас
```

#### **wt_bw_sawbright** - Яркая пила
Более яркая пилообразная волна
```javascript
n("c4 e4 g4").s("wt_bw_sawbright")    // Яркий лид
```

#### **wt_bw_sawgap** - Пила с разрывом
Пила с пропусками
```javascript
n("c3 e3 g3").s("wt_bw_sawgap")       // Необычная текстура
```

#### **wt_bw_sawrounded** - Округленная пила
Пила с мягкими краями
```javascript
n("c3 eb3 g3").s("wt_bw_sawrounded")  // Мягкий синт
```

#### **wt_bw_squ** - Квадрат
Прямоугольная волна
```javascript
n("c3 d3 e3 g3").s("wt_bw_squ")       // Чиптюн звук
n("c2").s("wt_bw_squ").lpf(300)       // Квадратный бас
```

#### **wt_bw_squrounded** - Округленный квадрат
Квадрат с мягкими краями
```javascript
n("c4 e4 g4").s("wt_bw_squrounded")   // Мягкий квадрат
```

#### **wt_bw_tri** - Треугольник
Треугольная волна
```javascript
n("c3 e3 g3").s("wt_bw_tri")          // Мягкий флейтовый тон
```

#### **wt_bw_perfectwaves** - Идеальные волны
Математически точные формы волн
```javascript
n("c4 e4 g4 b4").s("wt_bw_perfectwaves")  // Чистые гармоники
```

#### **wt_bw_blended** - Смешанные волны
Бленд различных форм волн
```javascript
n("c3 e3 g3 c4").s("wt_bw_blended")   // Гибридный звук
```

---

### 🎸 Инструменты (Instruments)

#### **wt_piano** - Фортепиано
Волновая таблица фортепиано
```javascript
n("c4 e4 g4 c5").s("wt_piano")        // Пианино мелодия
n("c2 e2 g2 c3").s("wt_piano").gain(0.8)  // Бас партия
```

#### **wt_epiano** - Электропиано
Электрическое пиано (Rhodes-стиль)
```javascript
n("c4 e4 g4 b4")
  .s("wt_epiano")
  .room(0.5)
  .delay(0.3)                         // Rhodes звук с эффектами
```

#### **wt_ebass** - Электробас
Электрический бас
```javascript
n("c2 c2 eb2 g2").s("wt_ebass")       // Бас-линия
n("a1 a1 c2 d2").s("wt_ebass").lpf(600)  // Приглушенный бас
```

#### **wt_dbass** - Контрабас
Акустический контрабас
```javascript
n("c2 d2 e2 g2").s("wt_dbass")        // Джазовый бас
```

#### **wt_aguitar** - Акустическая гитара
Акустическая гитара
```javascript
n("c3 e3 g3 c4").s("wt_aguitar")      // Гитарная мелодия
n("c2 f2 g2 c3").s("wt_aguitar")      // Гитарный бас
```

#### **wt_eguitar** - Электрогитара
Электрическая гитара
```javascript
n("c3 e3 g3").s("wt_eguitar")         // Электро-риф
n("c3 e3 g3").s("wt_eguitar").crush(4)  // Дисторшн
```

#### **wt_violin** - Скрипка
Скрипичный звук
```javascript
n("c4 e4 g4 c5").s("wt_violin")       // Струнная мелодия
n("c3 e3 g3").s("wt_violin").slow(2)  // Медленные струнные
```

#### **wt_cello** - Виолончель
Звук виолончели
```javascript
n("c2 e2 g2 c3").s("wt_cello")        // Виолончельная партия
```

#### **wt_altosax** - Альт-саксофон
Альт-саксофон
```javascript
n("c4 d4 f4 g4").s("wt_altosax")      // Саксофонная мелодия
```

#### **wt_oboe** - Гобой
Звук гобоя
```javascript
n("c4 e4 g4").s("wt_oboe")            // Классический гобой
```

#### **wt_clarinett** - Кларнет
Кларнет
```javascript
n("c3 e3 g3 c4").s("wt_clarinett")    // Джазовый кларнет
```

#### **wt_flute** - Флейта
Флейтовый звук
```javascript
n("c5 d5 e5 g5").s("wt_flute")        // Высокая флейта
```

#### **wt_clavinet** - Клавинет
Электромеханическое клавинет
```javascript
n("c3 e3 g3")
  .s("wt_clavinet")
  .fast(2)                            // Фанковый клавинет
```

#### **wt_eorgan** - Электроорган
Электрический орган
```javascript
n("c3 e3 g3 c4").s("wt_eorgan")       // Орган аккорды
```

---

### 🎛️ Синтезаторные звуки

#### **wt_theremin** - Терменвокс
Классический терменвокс
```javascript
n("c4 d4 f4 g4")
  .s("wt_theremin")
  .slow(2)                            // Эфирный звук
```

#### **wt_fmsynth** - FM синтез
FM синтезированные волны
```javascript
n("c3 e3 g3").s("wt_fmsynth")         // FM колокольчики
n("c2").s("wt_fmsynth").lpf(400)      // FM бас
```

#### **wt_pluckalgo** - Алгоритмическая щипковая
Алгоритмически сгенерированный щипковый звук
```javascript
n("c3 e3 g3 c4").s("wt_pluckalgo")    // Цифровой плак
```

#### **wt_stringbox** - Струнная машина
Винтажная струнная машина (String Machine)
```javascript
n("c3 e3 g3 c4").s("wt_stringbox")    // Ретро струнные
```

#### **wt_sinharm** - Синусоидальные гармоники
Синусы с добавленными гармониками
```javascript
n("c4 e4 g4 b4").s("wt_sinharm")      // Колокольный звук
```

---

### 🎮 Цифровые и игровые

#### **wt_vgame** - Видеоигры
Игровые 8-бит звуки
```javascript
n("c4 e4 g4 c5").s("wt_vgame")        // Ретро-игровая мелодия
```

#### **wt_vgamebasic** - Базовые игровые
Основные чиптюн звуки
```javascript
n("c3 e3 g3").s("wt_vgamebasic")      // Чиптюн бас
```

#### **wt_oscchip** - Чип-осциллятор
Звуки чиповых осцилляторов
```javascript
n("c4 d4 e4 g4")
  .s("wt_oscchip")
  .fast(2)                            // Быстрый чиптюн
```

#### **wt_c604** - C64 стиль
Звуки в стиле Commodore 64
```javascript
n("c3 e3 g3").s("wt_c604")            // C64 звук
```

#### **wt_bitreduced** - Битовая редукция
Редуцированные по битам звуки
```javascript
n("c3 e3 g3")
  .s("wt_bitreduced")
  .crush(4)                           // Lo-fi эффект
```

---

### 🌌 Экспериментальные

#### **wt_granular** - Гранулярные
Гранулярно синтезированные звуки
```javascript
n("c3 e3 g3")
  .s("wt_granular")
  .slow(2)                            // Гранулярные текстуры
```

#### **wt_snippets** - Фрагменты
Короткие звуковые фрагменты
```javascript
n("c4 e4 g4").s("wt_snippets")        // Нарезанные звуки
```

#### **wt_distorted** - Искаженные
Дисторшированные волны
```javascript
n("c3 e3 g3")
  .s("wt_distorted")
  .crush(6)                           // Жесткое искажение
```

#### **wt_raw** - Сырые
Необработанные, сырые звуки
```javascript
n("c2 e2 g2").s("wt_raw")             // Индустриальный бас
```

#### **wt_overtone** - Обертоны
Волны с богатыми обертонами
```javascript
n("c3 e3 g3 c4").s("wt_overtone")     // Богатые гармоники
```

#### **wt_symetric** - Симметричные
Симметричные волновые формы
```javascript
n("c4 e4 g4").s("wt_symetric")        // Сбалансированный звук
```

#### **wt_hdrawn** - Нарисованные вручную
Волны, нарисованные вручную
```javascript
n("c3 e3 g3").s("wt_hdrawn")          // Органические формы
```

#### **wt_hvoice** - Человеческий голос
Вокалоподобные волны
```javascript
n("c3 e3 g3 c4")
  .s("wt_hvoice")
  .slow(2)                            // Вокальный синтез
```

#### **wt_stereo** - Стерео волны
Стерео волновые формы
```javascript
n("c3 e3 g3 c4")
  .s("wt_stereo")
  .jux(rev)                           // Стерео эффект
```

---

### 🔢 Нумерованные (Numbered)

Коллекция из 20 экспериментальных волновых таблиц:

#### **wt_01** до **wt_20**
```javascript
// Сканирование через все нумерованные wavetables
n("c3").s("<wt_01 wt_02 wt_03 wt_04 wt_05>")

// Случайный выбор
n("c3 e3 g3")
  .s(choose([
    "wt_01", "wt_05", "wt_10",
    "wt_15", "wt_20"
  ]))

// Последовательность
n("c3").s("wt_" + (irand(20) + 1))
```

---

### 🌟 Специальные

#### **wt_linear** - Линейные
Линейные интерполяции
```javascript
n("c3 e3 g3").s("wt_linear")          // Плавные переходы
```

#### **wt_birds** - Птицы
Звуки птиц как волновая таблица
```javascript
n("c4 e4 g4").s("wt_birds")           // Природные звуки
```

---

## 🎵 Примеры использования

### 1. Базовая мелодия

```javascript
// Простая мелодия на пианино
n("c4 d4 e4 g4 e4 d4 c4")
  .s("wt_piano")
  .slow(2)
```

### 2. Бас-линия

```javascript
// Глубокий бас
n("c2 c2 eb2 g2")
  .s("wt_ebass")
  .lpf(500)
  .res(0.3)
```

### 3. Синтезаторный пэд

```javascript
// Атмосферный пэд
n("c3 e3 g3 c4")
  .s("wt_stringbox")
  .slow(4)
  .room(0.7)
  .delay(0.5)
```

### 4. Лид-синтезатор

```javascript
// Яркий лид
n("c4 d4 e4 g4 a4 g4 e4 d4")
  .s("wt_bw_sawbright")
  .lpf(sine.range(800, 3000))
  .res(0.2)
```

### 5. Арпеджио

```javascript
// Быстрое арпеджио
n("c3 e3 g3 c4")
  .s("wt_epiano")
  .fast(4)
  .delay(0.3)
  .room(0.4)
```

### 6. Чиптюн мелодия

```javascript
// 8-бит стиль
n("c4 e4 g4 c5 g4 e4 c4")
  .s("wt_vgame")
  .fast(2)
  .gain(0.8)
```

### 7. Экспериментальный бас

```javascript
// Дисторшированный бас
n("c2 c2 f2 g2")
  .s("wt_distorted")
  .lpf(400)
  .crush(6)
  .gain(0.9)
```

### 8. Многослойная текстура

```javascript
// Наслоение волновых таблиц
stack(
  n("c3 e3 g3").s("wt_piano").gain(0.6),
  n("c3 e3 g3").s("wt_stringbox").gain(0.4).delay(0.1),
  n("c2 e2 g2").s("wt_ebass").gain(0.7)
).slow(2)
```

### 9. Модулированный синтезатор

```javascript
// С модуляцией фильтра
n("c3 e3 g3 c4")
  .s("wt_bw_saw")
  .lpf(sine.range(200, 2000))
  .res(0.3)
  .slow(2)
```

### 10. Джазовая гармония

```javascript
// Джазовые аккорды
n("<c3 e3 g3 b3 | f3 a3 c4 e4 | g3 b3 d4 f4>")
  .s("wt_epiano")
  .room(0.4)
  .slow(4)
```

---

## 💡 Советы по использованию Wavetables

### 1. Выбор по стилю

- **Акустическая музыка**: wt_piano, wt_aguitar, wt_violin, wt_cello
- **Джаз**: wt_epiano, wt_dbass, wt_altosax, wt_clarinett
- **Электроника**: wt_bw_saw, wt_bw_squ, wt_fmsynth, wt_ebass
- **Ambient**: wt_stringbox, wt_hvoice, wt_granular, wt_overtone
- **Chiptune**: wt_vgame, wt_oscchip, wt_c604, wt_bw_squ
- **Experimental**: wt_distorted, wt_raw, wt_bitreduced

### 2. Обработка эффектами

```javascript
// Классический синт-звук
n("c3 e3 g3")
  .s("wt_bw_saw")
  .lpf(1000)        // Низкочастотный фильтр
  .res(0.3)         // Резонанс
  .delay(0.25)      // Дилей
  .room(0.3)        // Реверберация
```

### 3. Создание движения

```javascript
// Модуляция параметров
n("c3")
  .s("wt_epiano")
  .lpf(sine.range(300, 3000))    // Фильтр-свип
  .gain(sine.range(0.5, 1))       // Пульсация громкости
```

### 4. Комбинирование таблиц

```javascript
// Чередование звуков
n("c3 e3 g3 c4")
  .s("<wt_piano wt_epiano wt_clavinet>")
  .slow(2)
```

### 5. Использование октав

```javascript
// Разные регистры
stack(
  n("c2 e2 g2").s("wt_ebass"),         // Бас
  n("c3 e3 g3").s("wt_epiano"),        // Средний регистр
  n("c4 e4 g4").s("wt_violin").gain(0.6)  // Высокий регистр
)
```

---

## 🎓 Музыкальная теория

### Ноты и октавы

```javascript
// C major scale (До мажор)
n("c3 d3 e3 f3 g3 a3 b3 c4").s("wt_piano")

// C minor scale (До минор)
n("c3 d3 eb3 f3 g3 ab3 bb3 c4").s("wt_piano")

// Pentatonic scale (Пентатоника)
n("c3 d3 e3 g3 a3 c4").s("wt_piano")
```

### Аккорды

```javascript
// Major triad (Мажорное трезвучие)
n("c3 e3 g3").s("wt_epiano")

// Minor triad (Минорное трезвучие)
n("c3 eb3 g3").s("wt_epiano")

// Seventh chord (Септаккорд)
n("c3 e3 g3 b3").s("wt_piano")
```

### Басовые линии

```javascript
// Walking bass
n("c2 e2 g2 b2 a2 f2 d2 g2")
  .s("wt_dbass")
  .slow(2)

// Octave bass
n("c2 c3 c2 c3")
  .s("wt_ebass")
  .lpf(600)
```

---

## 🔧 Технические детали

### Диапазон нот

Wavetables поддерживают широкий диапазон нот:
- **Самые низкие**: C0-B1 (суб-басы)
- **Басы**: C1-B2
- **Средний регистр**: C3-B4
- **Высокий регистр**: C5-B6
- **Самые высокие**: C7-C8

### Комбинирование с эффектами

```javascript
// Полная цепь эффектов
n("c3 e3 g3")
  .s("wt_bw_saw")
  .lpf(1200)         // Фильтр
  .res(0.3)          // Резонанс
  .hpf(80)           // Высокочастотный фильтр
  .crush(0)          // Битовая редукция
  .delay(0.25)       // Дилей
  .delayfb(0.6)      // Фидбэк дилея
  .delaytime(0.125)  // Время дилея
  .room(0.3)         // Реверберация
  .size(0.8)         // Размер реверберации
```

---

**Версия**: 1.0
**Обновлено**: Декабрь 2025
**Всего wavetables**: 65
**Источник**: Dough Waveforms (AKWF)
