# Miscellaneous (Разное)

**Коллекция разных звуков** включает уникальные семплы, которые не вписываются в основные категории: голосовые семплы, экспериментальные звуки и специальные эффекты.

## 📊 Статистика

- **Паки**: sounds-repetition и другие
- **Типы**: Вокал, эффекты, экспериментальные
- **Применение**: Звуковой дизайн, акценты, текстуры

---

## 🎤 Sounds-Repetition

### Обзор
Коллекция вокальных семплов на тему "repetition" (повторение) - как английская, так и испанская версии.

---

### **repetition** - English Version
Английские вокальные семплы со словом "repetition"

```javascript
// Базовое использование
s("repetition:0")                      // Первый вариант
s("repetition:3")                      // Четвертый вариант
s("repetition:6")                      // Последний вариант

// Случайный выбор
s("repetition").n(irand(7))

// Последовательность
s("repetition*7").n("0..6").slow(2)

// Ритмичный паттерн
s("repetition:[0,2,4,6]").fast(2)
```

**Варианты**: 7 (a, b, c, d, e, f, g)
**Язык**: Английский
**Применение**: Вокальные акценты, лупы, экспериментальные композиции

#### Примеры использования

```javascript
// Вокальный лупп
s("repetition:0")
  .chop(8)
  .delay(0.25)

// С музыкой
stack(
  s("bd sd hh sd"),
  s("repetition:[0,3]").slow(2)
    .room(0.4)
    .delay(0.5)
)

// Глитч-эффект
s("repetition").n(irand(7))
  .fast(rand.range(1,4))
  .crush(perlin.range(0,8))

// Ambient текстура
s("repetition:[0,3,6]")
  .slow(4)
  .room(0.9)
  .delay(0.7)
  .gain(0.6)
```

---

### **repeticion** - Spanish Version
Испанские вокальные семплы на тему "repetición"

```javascript
// Базовое использование
s("repeticion:0")                      // Первый вариант
s("repeticion:1")                      // Второй вариант

// Чередование версий
s("<repetition repeticion>")
  .n(irand(2))
  .slow(2)

// Двуязычный паттерн
s("repetition:0 repeticion:0 repetition:3 repeticion:1")
  .slow(2)
```

**Варианты**: 2 (001-algorave-sp, 002-algorave-sp)
**Язык**: Испанский
**Применение**: Мультикультурные композиции, разнообразие

#### Примеры использования

```javascript
// Испанский лупп
s("repeticion:0")
  .chop(4)
  .delay(0.3)

// Билингвальная композиция
stack(
  s("repetition:0").pan(0.3),
  s("repeticion:0").pan(0.7).delay(0.1)
).slow(2)

// Алгоритмический паттерн
s("<repetition:0 repeticion:0 repetition:3 repeticion:1>")
  .chop(choose([4,8,16]))
  .sometimes(x => x.rev())
```

---

## 🎨 Творческие техники

### 1. Вокальные лупы

```javascript
// Базовый вокальный лупп
s("repetition:0")
  .chop(8)
  .fast(2)

// С реверсом
s("repetition:3")
  .chop(16)
  .every(4, x => x.rev())

// Глитч-лупп
s("repetition").n(irand(7))
  .chop(32)
  .sometimes(x => x.fast(2))
  .rarely(x => x.speed(0.5))
```

### 2. Слоение вокала

```javascript
// Двойной вокал
stack(
  s("repetition:0"),
  s("repetition:0").speed(0.95).delay(0.05).gain(0.7)
)

// Многослойный хор
stack(
  s("repetition:0"),
  s("repetition:2").pan(0.3),
  s("repetition:4").pan(0.7),
  s("repeticion:0").gain(0.5)
).slow(2)

// Октавное слоение
stack(
  s("repetition:0"),
  s("repetition:0").speed(0.5).gain(0.6),
  s("repetition:0").speed(2).gain(0.4)
)
```

### 3. Ритмические паттерны

```javascript
// Вокальная перкуссия
s("repetition:[0,2,4,0,3,5,1,6]")
  .fast(2)
  .gain("[1 0.8 0.9 0.7]*2")

// С барабанами
stack(
  s("bd(3,8)"),
  s("sd(4,8,2)"),
  s("repetition:[0,3]").n("~ 0 ~ 3")
)

// Синкопированный паттерн
s("repetition*8")
  .n("[0 ~ 3 ~ 5 ~ 1 6]")
  .gain("[1 ~ 0.8 ~ 0.9 ~ 0.7 1]")
```

### 4. Эффекты и обработка

```javascript
// Космический эффект
s("repetition:0")
  .room(0.9)
  .delay(0.7)
  .delayfb(0.8)
  .speed(0.8)

// Дисторшн
s("repetition:3")
  .crush(6)
  .gain(1.2)
  .lpf(2000)

// Модуляция
s("repetition:5")
  .speed(sine.range(0.8, 1.2))
  .lpf(sine.range(500, 5000))
  .pan(sine)
```

### 5. Чоппинг и нарезка

```javascript
// Мелкая нарезка
s("repetition:0")
  .chop(32)
  .fast(2)

// Случайная нарезка
s("repetition:3")
  .chop(16)
  .shuffle(4)

// Реаранжировка
s("repetition:0")
  .chop(16)
  .n("0 4 8 12 2 6 10 14 1 5 9 13 3 7 11 15")
```

### 6. Темповые манипуляции

```javascript
// Замедление
s("repetition:0")
  .slow(4)
  .speed(0.5)
  .room(0.7)

// Ускорение
s("repetition:3")
  .fast(4)
  .speed(1.5)

// Переменный темп
s("repetition:5")
  .speed("<0.5 1 1.5 2>")
  .slow(4)
```

---

## 🎵 Жанровые примеры

### 1. Ambient

```javascript
// Эмбиент-текстура
stack(
  s("repetition:0")
    .slow(8)
    .room(0.9)
    .delay(0.8)
    .gain(0.5),
  s("repetition:3")
    .slow(6)
    .speed(0.7)
    .room(0.8)
    .gain(0.4)
)
```

### 2. Techno

```javascript
// Техно с вокалом
stack(
  s("bd(3,8)"),
  s("sd(4,8,2)"),
  s("hh*16").gain("[0.6 1]*8"),
  s("repetition:[0,3]")
    .chop(16)
    .fast(2)
    .crush(2)
    .gain(0.7)
)
```

### 3. Experimental/IDM

```javascript
// IDM/глитч
s("repetition").n(irand(7))
  .chop(irand(32))
  .speed(perlin.range(0.5, 2))
  .crush(perlin.range(0, 8))
  .pan(perlin)
  .sometimes(x => x.rev())
```

### 4. Hip-Hop

```javascript
// Хип-хоп с вокальными семплами
stack(
  s("bd:0 ~ bd:0 ~"),
  s("~ sd:0 ~ sd:0"),
  s("hh*8").gain(0.6),
  s("repetition:0")
    .chop(8)
    .n("~ 0 ~ 2")
    .gain(0.8)
)
```

### 5. Breakbeat

```javascript
// Брейкбит с вокалом
stack(
  s("amen").chop(16),
  s("repetition:[0,3,5]")
    .slow(2)
    .delay(0.3)
    .gain(0.7)
)
```

### 6. House

```javascript
// Хаус-грув
stack(
  s("bd*4"),
  s("hh*16").gain("[0.6 1]*8"),
  s("~ sd ~ sd"),
  s("repetition:0")
    .chop(4)
    .every(4, x => x.rev())
    .gain(0.6)
).fast(1.3)
```

---

## 🔧 Продвинутые техники

### 1. Грануляция

```javascript
// Гранулярный эффект
s("repetition:0")
  .chop(64)
  .fast(8)
  .gain(rand.range(0.3, 1))
  .pan(rand)
```

### 2. Pitch Shifting

```javascript
// Изменение высоты тона
s("repetition:0")
  .speed("<0.5 0.75 1 1.5 2>")
  .slow(4)

// Хроматическая гамма
s("repetition:0*12")
  .speed("0.5 0.53 0.56 0.59 0.63 0.67 0.71 0.75 0.79 0.84 0.89 0.94")
  .slow(2)
```

### 3. Стереоэффекты

```javascript
// Стерео-расширение
stack(
  s("repetition:0").pan(0.2),
  s("repetition:0").pan(0.8).delay(0.02)
)

// Ping-pong
s("repetition:3")
  .delay(0.5)
  .delaytime(0.25)
  .pan(sine.range(0, 1))
```

### 4. Фильтрация

```javascript
// Sweep фильтр
s("repetition:0")
  .lpf(sine.range(200, 5000))
  .res(0.3)

// Bandpass
s("repetition:3")
  .bpf(1000)
  .bpq(10)

// Notch
s("repetition:5")
  .hpf(200)
  .lpf(8000)
```

### 5. Комбинирование с другими звуками

```javascript
// С Dirt Samples
stack(
  s("repetition:0").slow(2),
  s("bd sd hh sd"),
  s("bass1").n("c2 c2 eb2 g2").lpf(600)
)

// С wavetables
stack(
  s("repetition:3").slow(4).gain(0.6),
  n("c3 e3 g3").s("wt_piano").room(0.4)
)

// С брейками
stack(
  s("amen").chop(16),
  s("repetition:[0,3,6]").slow(2).delay(0.3)
)
```

---

## 💡 Творческие идеи

### 1. Вокальная перкуссия

```javascript
// Использование вокала как ритм-секции
s("repetition:[0,2,4,6,1,3,5,0]")
  .fast(2)
  .gain("[1 0.7 0.9 0.6]*2")
  .lpf(2000)
```

### 2. Мелодические паттерны

```javascript
// Создание мелодии через pitch shifting
s("repetition:0*8")
  .speed("1 1.2 1.5 1.8 1.5 1.2 1 0.8")
  .slow(2)
```

### 3. Текстурные пады

```javascript
// Вокальный пэд
stack(
  s("repetition:0").slow(16).room(0.9).gain(0.4),
  s("repetition:3").slow(12).speed(0.8).room(0.8).gain(0.3),
  s("repetition:5").slow(14).speed(1.2).room(0.7).gain(0.3)
)
```

### 4. Ритмичные акценты

```javascript
// Вокальные акценты
stack(
  s("bd sd hh sd"),
  s("~ ~ repetition:0 ~").gain(0.8)
)
```

### 5. Экспериментальные композиции

```javascript
// Алгоритмическая композиция
s("repetition").n(irand(7))
  .chop(choose([4,8,16,32]))
  .speed(choose([0.5,0.75,1,1.5,2]))
  .sometimes(x => x.rev())
  .rarely(x => x.crush(6))
  .pan(perlin)
```

---

## 🎓 Советы по использованию

### 1. Контроль громкости
```javascript
// Не перегружайте микс
s("repetition:0").gain(0.6)

// Динамика
s("repetition*4").gain("[1 0.7 0.9 0.8]")
```

### 2. Частотная балансировка
```javascript
// Освободите место для других звуков
s("repetition:0")
  .lpf(8000)
  .hpf(200)
```

### 3. Пространственность
```javascript
// Стерео-пространство
s("repetition:0")
  .room(0.4)
  .pan(0.3)
```

### 4. Интеграция в микс
```javascript
// Хорошая интеграция
stack(
  s("bd(3,8)"),
  s("sd(4,8,2)"),
  s("hh*16").gain(0.5),
  s("repetition:0")
    .slow(2)
    .gain(0.6)
    .lpf(5000)
    .room(0.3)
)
```

---

## 📚 Концептуальное применение

### Тема повторения

Семплы "repetition" идеально подходят для:

1. **Live Coding перформансов** - слово "repetition" отражает суть live coding
2. **Образовательных целей** - демонстрация концепции повторения в музыке
3. **Минималистичной музыки** - Steve Reich, Philip Glass стиль
4. **Экспериментальной электроники** - исследование темы репетиции
5. **Алгоритмической композиции** - рекурсия и паттерны

### Философский контекст

```javascript
// Минималистичная композиция
s("repetition:0")
  .slow(4)
  .every(8, x => x.speed(0.95))
  .every(16, x => x.delay(0.1))
  // Медленная эволюция через повторение
```

---

## 🌐 Мультиязычность

### Использование обеих версий

```javascript
// Билингвальная композиция
s("<repetition repeticion>:0")
  .slow(2)

// Диалог между языками
stack(
  s("repetition:0").pan(0.3),
  s("repeticion:0").pan(0.7).delay(0.1)
).slow(2)

// Полиритмия
stack(
  s("repetition*3").n("0 2 4"),
  s("repeticion*2").n("0 1")
).slow(2)
```

---

## 🔮 Экспериментальные идеи

### 1. Вокальная драм-машина

```javascript
// Имитация драм-машины вокалом
s("repetition:[0,2,4,6]")
  .chop(4)
  .gain("[1 0.8 0.9 0.7]")
  .lpf("[2000 3000 1500 2500]")
```

### 2. Вокальный бас

```javascript
// Низкочастотный вокал
s("repetition:0")
  .speed(0.25)
  .lpf(300)
  .gain(1.2)
```

### 3. Вокальный lead

```javascript
// Высокий лид
s("repetition:3")
  .speed(2)
  .delay(0.25)
  .lpf(5000)
```

---

## 📖 Рекомендации

### Для новичков
- Начните с простых паттернов
- Экспериментируйте с `.chop()`
- Добавляйте эффекты постепенно

### Для продвинутых
- Комбинируйте с другими семплами
- Создавайте сложные полиритмы
- Исследуйте грануляцию и pitch shifting

### Для live coding
- Используйте как тематический элемент
- Создавайте метаязыковые паттерны
- Играйте с концепцией повторения

---

**Версия**: 1.0
**Обновлено**: Декабрь 2025
**Всего семплов**: 9 (7 английских + 2 испанских)
**Источник**: Sounds-Repetition коллекция
**Концепция**: Исследование повторения в live coding и электронной музыке
