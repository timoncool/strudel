# Остальные функции

Всего функций: 45

---

## aliasBank

**Параметры:**

- **bank** (string): Банк для создания alias
- **alias** (string): Alias, который будет использоваться для банка

**Описание:** Регистрирует alias для банка звуков.
Опционально принимает один аргумент - объект с alias банков.
Опционально принимает один аргумент - строку с путем к JSON файлу, содержащему alias банков.

---

## applyGradualLowpass

**Параметры:**

- **input** (AudioBuffer): 
- **lpFreqStart** (number): 
- **lpFreqEnd** (number): 
- **lpFreqEndAt** (number): 
- **callback** (function): Может быть вызвана
немедленно в текущем контексте выполнения или позже.

**Описание:** Применяет постоянно изменяющийся lowpass filter к данному звуку.

---

## applyHannWindow

**Параметры:**

Нет параметров

**Описание:** Apply Hann window in-place

---

## chyx

**Параметры:**

Нет параметров

**Описание:** BYTE BEATS

---

## computeMagnitudes

**Параметры:**

Нет параметров

**Описание:** Compute squared magnitudes for peak finding

---

## crossfade

**Параметры:**

- **a** (number): Сигнал A (может быть одиночным значением или значением массива при обработке буфера).
- **b** (number): Сигнал B (может быть одиночным значением или значением массива при обработке буфера).
- **m** (number): Параметр кроссфейда (0.0 = только A, 1.0 = только B, 0.5 = равная смесь).

**Описание:** Функция кроссфейда с равной мощностью.
Плавно переключает между сигналами A и B, поддерживая постоянную воспринимаемую громкость.

---

## defaultmidimap

**Параметры:**

Нет параметров

**Описание:** Настраивает midimap по умолчанию, который используется когда параметр &quot;midimap&quot; не задан

**Примеры:**

```javascript
defaultmidimap({ lpf: 74 })
$: note("c a f e").midi();
$: lpf(sine.slow(4).segment(16)).midi();
```

---

## DoughVoice

**Параметры:**

- **value** (DoughVoice): 

**Описание:** Описание отсутствует

---

## findPeaks

**Параметры:**

Нет параметров

**Описание:** Find peaks in spectrum magnitudes

---

## fscope

**Параметры:**

- **color** (string): Цвет линии в виде hex или названия цвета. По умолчанию белый.
- **scale** (number): Масштабирует ось y. По умолчанию 0.25
- **pos** (number): Позиция по оси y относительно высоты экрана. 0 = верх, 1 = низ экрана
- **lean** (number): Выравнивание по оси y, где 0 = верх и 1 = низ
- **min** (number): Минимальное значение
- **max** (number): Максимальное значение

**Описание:** Рендерит осциллограф для частотной области аудиосигнала.

**Примеры:**

```javascript
s("sawtooth").fscope()
```

---

## generateGraph

**Параметры:**

- **data** (Float32Array): Массив чисел или Float32Array.
- **width** (number): Ширина canvas в пикселях.
- **height** (number): Высота canvas в пикселях.
- **min** (number): Минимальное значение данных для графика (нижний край).
- **max** (number): Максимальное значение данных на графике (верхний край).

**Описание:** Создает элемент canvas с графиком предоставленных данных.

---

## generateReverb

**Параметры:**

- **params** (Object): TODO: Документировать свойства.
- **callback** (function): Функция для вызова, когда
импульсный отклик был сгенерирован. Импульсный отклик
передается этой функции в качестве параметра. Может быть вызвана
немедленно в текущем контексте выполнения или позже.

**Описание:** Генерирует импульсный отклик reverb.

---

## getAllChannelData

**Параметры:**

- **buffer** (AudioBuffer): 

**Описание:** Описание отсутствует

---

## getFreq

**Параметры:**

Нет параметров

**Описание:** Описание отсутствует

---

## handleOutputBuffersToRetrieve

**Параметры:**

Нет параметров

**Описание:** Добавляет содержимое только что обработанных выходных буферов к выходным буферам

---

## id

**Параметры:**

Нет параметров

**Описание:** Описание отсутствует

---

## markcss

**Параметры:**

Нет параметров

**Описание:** Переопределяет css для выделенных событий. Обязательно используйте одинарные кавычки!

**Примеры:**

```javascript
note("c a f e")
.markcss('text-decoration:underline')
```

---

## OLAProcessor

**Параметры:**

Нет параметров

**Описание:** Описание отсутствует

---

## osc

**Параметры:**

Нет параметров

**Описание:** Отправляет каждый hap как OSC сообщение, которое может быть принято SuperCollider или любым другим программным обеспечением с поддержкой OSC.
Для дополнительной информации читайте <a href="https://strudel.cc/learn/input-output/">MIDI &amp; OSC в документации</a>

---

## out

**Параметры:**

Нет параметров

**Описание:** Описание отсутствует

---

## pickAndRename

**Параметры:**

Нет параметров

**Описание:** Выбирает записи из <code>source</code> и переименовывает их через <code>map</code>

---

## pickmodF

**Параметры:**

- **pat** (Pattern): 
- **lookup** (Pattern): pattern индексов
- **funcs** (Array.<function()>): массив функций, из которых нужно выбирать

**Описание:** То же самое, что <code>pickF</code>, но если вы выбираете число больше размера списка функций,
оно зацикливается, вместо того чтобы остановиться на максимальном значении.

---

## pickmodOut

**Параметры:**

- **pat** (Pattern): 
- **xs** (*): 

**Описание:** То же самое, что <code>pickOut</code>, но если вы выбираете число больше размера списка,
оно зацикливается, вместо того чтобы остановиться на максимальном значении.

---

## pickmodReset

**Параметры:**

- **pat** (Pattern): 
- **xs** (*): 

**Описание:** То же самое, что <code>pickReset</code>, но если вы выбираете число больше размера списка,
оно зацикливается, вместо того чтобы остановиться на максимальном значении.

---

## pickmodRestart

**Параметры:**

- **pat** (Pattern): 
- **xs** (*): 

**Описание:** То же самое, что <code>pickRestart</code>, но если вы выбираете число больше размера списка,
оно зацикливается, вместо того чтобы остановиться на максимальном значении.

**Примеры:**

```javascript
"<a@2 b@2 c@2 d@2>".pickRestart({
        a: n("0 1 2 0"),
        b: n("2 3 4 ~"),
        c: n("[4 5] [4 3] 2 0"),
        d: n("0 -3 0 ~")
      }).scale("C:major").s("piano")
```

---

## pickOut

**Параметры:**

- **pat** (Pattern): 
- **xs** (*): 

**Описание:** Похож на <code>pick</code>, но применяет outerJoin вместо innerJoin.

---

## pitchwheel

**Параметры:**

- **hapcircles** (number): 
- **circle** (number): 
- **edo** (number): 
- **root** (string): 
- **thickness** (number): 
- **hapRadius** (number): 
- **mode** (string): 
- **margin** (number): 

**Описание:** Отрисовывает круг высот для визуализации частот в пределах одной октавы

**Примеры:**

```javascript
n("0 .. 12").scale("C:chromatic")
.s("sawtooth")
.lpf(500)
._pitchwheel()
```

---

## prepareInputBuffersToSend

**Параметры:**

Нет параметров

**Описание:** Копирует содержимое входных буферов в буфер, фактически отправляемый на обработку

---

## readInputs

**Параметры:**

Нет параметров

**Описание:** Читает следующий блок web audio в входные буферы

---

## reallocateChannelsIfNeeded

**Параметры:**

Нет параметров

**Описание:** Обрабатывает динамическую реаллокацию буферов входных/выходных каналов
(количество каналов может изменяться в течение жизненного цикла)

---

## samples

**Параметры:**

Нет параметров

**Описание:** Загружает коллекцию sample для использования с <code>s</code>

**Примеры:**

```javascript
samples('github:tidalcycles/dirt-samples');
s("[bd ~]*2, [~ hh]*2, ~ sd")
```

```javascript
samples({
 bd: '808bd/BD0000.WAV',
 sd: '808sd/SD0010.WAV'
 }, 'https://raw.githubusercontent.com/tidalcycles/Dirt-Samples/master/');
s("[bd ~]*2, [~ hh]*2, ~ sd")
```

```javascript
samples('shabda:noise,chimp:2')
s("noise <chimp:0*2 chimp:1>")
```

```javascript
samples('shabda/speech/fr-FR/f:chocolat')
s("chocolat*4")
```

---

## scope

**Синонимы:** tscope

**Параметры:**

- **config** (object): Опциональная конфигурация с параметрами:
- **align** (boolean): Если 1, осциллограф будет выровнен по первому пересечению нуля. По умолчанию 1
- **color** (string): Цвет линии в виде hex или названия цвета. По умолчанию белый.
- **thickness** (number): Толщина линии. По умолчанию 3
- **scale** (number): Масштабирует ось y. По умолчанию 0.25
- **pos** (number): Позиция по оси y относительно высоты экрана. 0 = верх, 1 = низ экрана
- **trigger** (number): Значение амплитуды, используемое для выравнивания осциллографа. По умолчанию 0.

**Описание:** Рендерит осциллограф для временной области аудиосигнала.

**Примеры:**

```javascript
s("sawtooth")._scope()
```

---

## setcpm

**Параметры:**

- **cpm** (number): cycles в минуту

**Описание:** Изменяет глобальный темп на указанное количество cycles в минуту

**Примеры:**

```javascript
setcpm(140/4) // =140 bpm в 4/4
$: s("bd*4,[- sd]*2").bank('tr707')
```

---

## shiftInputBuffers

**Параметры:**

Нет параметров

**Описание:** Сдвигает содержимое входных буферов влево для получения нового блока web audio

---

## shiftOutputBuffers

**Параметры:**

Нет параметров

**Описание:** Сдвигает содержимое выходных буферов влево для получения нового блока web audio

---

## shiftPeaks

**Параметры:**

Нет параметров

**Описание:** Shift peaks and regions of influence by pitchFactor into new specturm

---

## slider

**Параметры:**

- **value** (number): Начальное значение
- **min** (number): Минимальное значение - необязательно, по умолчанию 0
- **max** (number): Максимальное значение - необязательно, по умолчанию 1
- **step** (number): Размер шага - необязательно

**Описание:** Отображает widget-слайдер, позволяющий пользователю манипулировать значением

---

## spectrum

**Параметры:**

- **config** (object): Опциональная конфигурация с параметрами:
- **thickness** (integer): Толщина линии в px (по умолчанию 3)
- **speed** (integer): Скорость прокрутки (по умолчанию 1)
- **min** (integer): Минимальное значение в db (по умолчанию -80)
- **max** (integer): Максимальное значение в db (по умолчанию 0)

**Описание:** Рендерит анализатор спектра для входящего аудиосигнала.

**Примеры:**

```javascript
n("<0 4 <2 3> 1>*3")
.off(1/8, add(n(5)))
.off(1/5, add(n(7)))
.scale("d3:minor:pentatonic")
.s('sine')
.dec(.3).room(.5)
._spectrum()
```

---

## spiral

**Параметры:**

- **options** (Object): Объект, содержащий все следующие необязательные параметры в виде пар ключ-значение:
- **stretch** (number): управляет соотношением вращений за цикл, где 1 = 1 цикл / 360 градусов
- **size** (number): диаметр спирали
- **thickness** (number): толщина линии
- **cap** (string): стиль концов линий: butt (по умолчанию), round, square
- **inset** (string): количество вращений до начала спирали (по умолчанию 3)
- **playheadColor** (string): цвет playhead, по умолчанию white
- **playheadLength** (number): длина playhead во вращениях, по умолчанию 0.02
- **playheadThickness** (number): толщина playhead, по умолчанию равна thickness
- **padding** (number): пространство вокруг спирали
- **steady** (number): устойчивость спирали относительно playhead. 1 = спираль не двигается, двигается playhead.
- **activeColor** (number): цвет активного сегмента. по умолчанию foreground темы
- **inactiveColor** (number): цвет неактивных сегментов. по умолчанию gutterForeground темы
- **colorizeInactive** (boolean): окрашивать ли неактивные сегменты, по умолчанию 0
- **fade** (boolean): затухают ли прошлое и будущее. по умолчанию 1
- **logSpiral** (boolean): должна ли спираль быть логарифмической. по умолчанию 0

**Описание:** Отображает спиральную визуализацию.

**Примеры:**

```javascript
note("c2 a2 eb2")
.euclid(5,8)
.s('sawtooth')
.lpenv(4).lpf(300)
._spiral({ steady: .96 })
```

---

## tables

**Параметры:**

Нет параметров

**Описание:** Загружает коллекцию wavetable для использования с <code>s</code>

---

## vib

**Параметры:**

Нет параметров

**Описание:** Описание отсутствует

---

## vibmod

**Параметры:**

Нет параметров

**Описание:** Описание отсутствует

---

## voicings

**Параметры:**

- **dictionary** (string): какой словарь voicing использовать.

**Описание:** УСТАРЕЛО: всё ещё работает, но рекомендуется использовать .voicing (без s).
Преобразует символы chord в voicing, используя максимально плавное голосоведение.
Использует <a href="https://github.com/felixroos/chord-voicings#chord-voicings">пакет chord-voicings</a>.

**Примеры:**

```javascript
stack("<C^7 A7 Dm7 G7>".voicings('lefthand'), "<C3 A2 D3 G2>").note()
```

---

## wordfall

**Параметры:**

Нет параметров

**Описание:** Отображает вертикальный pianoroll с метками событий.
Поддерживает все те же опции, что и pianoroll.

---

## writeOutputs

**Параметры:**

Нет параметров

**Описание:** Записывает следующий блок web audio из выходных буферов

---

