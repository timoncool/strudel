# Changelog Bulka

Все изменения проекта Bulka документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).

---

## Декабрь 2025

### ✨ Новые возможности

- pre-commit hook для автообновления changelog (`9dbc62e5`)
- добавить версию (git commit) в header документации (`eb04b0e1`)
- автоматический changelog и страница /whatsnew (`23b898fb`)
- добавить Hydra визуализацию к треку coastline в примерах (`ceca8b8d`)
- добавить ссылку 'сообщество' в навигацию (`df6fb334`)
- 3 колонки в ленте на широких экранах (`641fcda7`)
- легкая плашка вместо модального окна для шаринга + объединение Пекарни и Ленты (`a8130ae3`)
- сохранение черновика чата в localStorage (`78dd0678`)
- добавить страницу Лента с галереей треков и системой лайков (`f78755d9`)
- добавить секцию поддержки проекта с графиком звёзд (`7139d930`)
- добавить 35 новых примеров треков в галерею (`7e3cca73`)
- добавить 35+ треков в галерею и примеры (`2033ed57`)
- добавить динамические Open Graph мета-теги для шеринга треков (`2586e026`)
- добавить полную документацию по Hydra на русском языке (`b6ff1ed2`)
- update Supabase credentials for URL shortener (`4322c070`)
- обновление домена, сообщества и добавление Яндекс.Метрики (`69402558`)
- Integrate Sparkway Drum Kit sample pack (168 samples) (`7d965d0d`)
- Add Sparkway Drum Kit and fix sample count display (`847acea8`)
- Add expandable sample banks in sounds browser (`63f2687c`)
- Add sample pack tools for AI agent (`d857ca04`)
- Add E-Pianos sample pack (CP80, Wurlitzer, Pianet) (`6cdfa576`)
- Add E-Pianos sample pack (CP80, Wurlitzer, Pianet) (`bd605abb`)
- Add agent behavior patterns and console debugging (`007349ec`)
- add getExamples usage instructions for agent (`a1eb3a24`)
- add thinking protocol and workflow examples to AI agent (`4225b110`)
- add advanced API functions to knowledge base (`67467c50`)
- add 10 anti-patterns to agent prompt (`dc04cebe`)
- add music theory and composition knowledge to AI agent (`40dd36a4`)
- add track structure guide to agent prompt (`fab15526`)
- add arpeggios usage guide and metadata tags (`40cd1313`)
- Add version and commit hash to header and console (`031abdff`)
- Major UI/UX improvements and AI agent enhancements (`7465340f`)
- Add audio recorder to header (`8354d7df`)
- Add AI Music Agent chat interface for vibe-coding music (`fd5879eb`)
- Add AI Music Agent with full RAG on MD documentation (`57b7b268`)
- Update AI models to latest versions (GPT-4.1, o3, Claude 4.5) (`5aaa09e9`)
- Implement full AI agent with tool calling (`c7a6890d`)
- Add GPT-5/5.1 and Claude Opus 4.5 models (`45f4fbac`)
- Artifact-style code editing + fix model defaults (`230493e7`)
- Add markdown rendering in chat messages (`59a7543a`)
- Improve AI music agent UX and system prompt (`14880d75`)
- Enhanced system prompt with detailed tools, Strudel reference and examples (~7k chars) (`74e6d587`)
- Enhanced system prompt with creativity, visualization, sliders and complex examples (`f7304b2d`)
- Enhanced system prompt with creativity, visualization, sliders and complex examples (`23b6e6da`)
- Add volume control, undo/redo, selection support and highlight tool (`94b60cde`)
- Persist chat dialog in localStorage (`85e79f3e`)
- UI improvements and rate limit handling (`3427c6cd`)
- Add Claude extended thinking streaming support (`f78c24b7`)
- Add GPT-5.1 Responses API with reasoning summaries streaming (`8a706ab4`)
- UI tweaks and short URLs (`7f1d1e8f`)
- Multi-provider API keys + Gemini support + reasoning models fix (`fdf8f964`)
- Dynamic model fetching from provider APIs (`95393fe9`)

### 🐛 Исправления

- заменить ForBiggerBlazes на friday.mp4 от MDN (`83ed7a4`)
- заменить нерабочие URL видео на рабочие с CORS (`bc15ab1`)
- вернуть changelog в prebuild для автообновления на Vercel (`0fbf867`)
- переименовать кнопку "Редактировать" в "Открыть" в ленте (`5fbb2b9`)
- очистка Hydra при включении нового трека (`90e1d9c1`)
- очистка Hydra при включении нового трека (`62d02b12`)
- откатить ленту к состоянию до 3 колонок (`f6bd6639`)
- вернуть центрирование контейнера ленты (`bf227ad0`)
- вернуть 2 колонки в ленте вместо 3 (`521d4cd0`)
- убрать устаревшие названия моделей из описания AI-ассистента (`a65c61b7`)
- убрать hydra demo из примеров и дополнить возможности Bulka (`20c7fef0`)
- переписать пост про Hydra - конкретно про initImage/initVideo (`f08c58e9`)
- исправить шеринг из ленты и расширить контейнер (`1247b2a1`)
- дедупликация в changelog (`75f7f619`)
- реально копировать ссылку в буфер при шеринге (`be03bb8c`)
- инкрементальное обновление changelog (только новые коммиты) (`1688a1bc`)
- удалить GitHub Action, использовать prebuild на Vercel (`8f9c8c36`)
- добавить permissions для GitHub Action (`d3aa073c`)
- использовать version.ts для отображения версии в доках + GitHub Action для автообновления changelog (`1b0e9c9b`)
- переместить 'Что нового' в конец раздела Презентация (`3f797173`)
- использовать editorRef.current.code для шеринга (`225ff8ca`)
- брать код из редактора напрямую при шеринге (`05011fad`)
- разрешить перезапись уже расшаренных треков (`04c90e7e`)
- исключить URL функции Hydra из парсинга mini-нотации (`57ca8ea8`)
- заменить первый пример на coastline с Hydra (`d6f8d0ce`)
- предотвратить дубликаты и пустые записи при шаринге (`95a0d434`)
- исправить позиционирование плашки шаринга (`aca75e65`)
- заменить иконки штруделя на булку для og:image превью (`68b3cc22`)
- заменить OG-картинку на версию с тёмным фоном (`9636d807`)
- заменить иконку REC на SVG с поддержкой цветов темы (`5526bcb0`)
- Showcase newest first by default, add themes page (`fa4e7505`)
- Remove oversized samples (>5MB) to fix build (`1234fb4f`)
- Convert all samples to OGG format (168 files, 20MB total) (`049b1790`)
- Show bank names in getAvailablePacks for agent (`3356bcf1`)
- address rate limit errors for OpenAI and Anthropic APIs (`10bc1bee`)
- Resolve rate limit errors based on analysis of top AI agents (`e25dc5ff`)
- add critical warnings to enforce tool usage for samples (`31ca3f88`)
- correct .room().size() to .room().roomsize() syntax (`d398dbc8`)
- syntax error and auto-versioning (`d99dedb3`)
- escape backticks in code examples (`9c66e69f`)
- Proper streaming and thinking for all providers (`b2646a65`)
- Real-time streaming for all AI providers (`abb4b6c2`)
- Capture thinking signature for Anthropic conversation continuity (`5adf751f`)
- GPT-5 series model support with reasoning_effort (`7ae7055c`)
- Hide o-series models that don't support tools (`d7bd176f`)
- GPT-5 models don't support temperature parameter (`b3e84d70`)
- Remove unsupported reasoning_effort parameter for GPT-5 models (`36e3450e`)
- Version color, update AI docs, remove Mastodon link (`44487919`)
- Change default volume from 80% to 100% (`31b72c3c`)
- Default volume 100%, fix falsy check for 0 volume (`e39e84b3`)
- Show only commit hash in header instead of unused version number (`5f5ce29d`)
- Improve audio recorder - WAV lossless, @title filename, UI position (`2ef6bc08`)
- Improve header spacing - more distance between elements (`19965843`)
- Switch to Vercel adapter for server-side API routes (`c37523dc`)
- Copy docs_md to website for Vercel serverless access (`2fb3188d`)
- Update Vercel outputDirectory to .vercel/output for Astro adapter (`ebe8f541`)
- Remove outputDirectory - let Astro Vercel adapter handle output automatically (`f7f43b31`)
- Copy .vercel/output to root after build for Vercel to find it (`910f28a7`)
- Correct path for copying .vercel/output to root (`194ed89e`)
- Update ChatTab colors to match other panel tabs (`9a7ddd88`)
- Add Claude Opus 4.5 as top model (`e3e964fb`)
- Correct docs_md path for RAG search (`e6e28e01`)
- Update default Anthropic model to Claude Sonnet 4.5 (`834dd842`)
- Change superdough import to webaudio for Vercel client build (`3b95b0f3`)
- Prevent AI agent from inventing sample pack names (`8222dc00`)
- Volume slider as floating overlay + mute state sync (`c1a7cdf7`)
- Volume slider UX improvements and undo/redo fixes (`00bf123e`)
- Update to December 2025 model names (`211f68dd`)
- Use real working model names (`a5040342`)
- Proper model filtering and thinking display (`297e7686`)
- use simpleTune (Pixel Sunrise) as default (`1f5e08a9`)

### 📚 Документация

- добавить ссылку на внешние источники в навигацию (`406f9ed8`)
- добавить примеры использования initImage и initVideo с URL (`2912f794`)
- Add color themes documentation and improve gallery (`85e636c8`)
- Add Sparkway Drum Kit to AI agent knowledge base (`0b8be5db`)
- Mark Sparkway Drum Kit for modern electronic genres (`83b1b57e`)
- Expand sample pack descriptions with genre guidance for AI agent (`646a68d7`)
- Add E-Pianos to agent knowledge base (`c830479b`)
- Add e-pianos to agent genre recommendations (`a1b87564`)
- добавлен критический отчёт о несоответствиях в системе знаний агента (`0d95d5d6`)
- comprehensive AI prompts analysis and recommendations (`eb0371be`)
- Add autonomous agents analysis from research (`b7a5df15`)
- Add Bulka AI Agent documentation page (`b1d722b0`)

### ♻️ Рефакторинг

- улучшение README и создание отдельной страницы скриншотов (`c5d4d3da`)
- replace rigid structure with abstract music principles (`c88cdbe2`)
- Simplify system prompt to ~1600 chars (`744f641e`)

### 🎨 Стили

- глобальная стилизация скроллбаров для всего приложения (`e638417`)
- добавить стилизацию скроллбаров под тему (`ad667c7`)

### 🧪 Тесты

- проверка pre-commit hook для changelog (`68423e41`)

### 🔧 Обслуживание

- удалить тестовый файл (`7d6768d9`)
- обновить changelog [skip ci] (`3fd8aec3`)
- удаление мусорных MD файлов и обновление .gitignore (`3967642b`)
- remove version numbering, use git commit only (`2d2f0664`)
- Add .vercel to gitignore, remove build artifacts (`bf2097f7`)

### 🔍 Отладка

- добавить логирование в shareCode (`08b2e285`)

### 📝 Блог

- пост про расширение Hydra и документацию (`ddecebb1`)
- Replace Strudel changelog with Bulka changelog (`425b5794`)

### 📦 Прочее

- сделать описания возможностей более привлекательными (`54e93d7e`)
- разрешить конфликт в блоге про Hydra (`8eedb425`)
- Описание изменений (`e2be3660`)
- Описание изменений (`c995e5bc`)
- Добавлен пак семплов Sparkway Drum Kit (`0898290b`)
- Заменены жанровые примеры на каверы от eefano + обновлена Витрина (`40d054c8`)
- Fix GitHub sample packs loading - pass empty baseUrl (`b815729b`)
- Добавлены видео 2025 в Витрину + треки Nerual Dreming в Галерею (`d355a252`)
- Добавлены сложные примеры от сообщества в Галерею (`fe4bdee5`)
- Удалены дубликаты треков Nerual Dreming из конца Галереи (`25ccfa8e`)
- Добавлена полная документация в формате Markdown для обучения AI агента (`9f840f3e`)
- Исправлена EN документация: добавлены все примеры кода (`6b375d2d`)
- Add doc.json for Vercel builds (`f7b1d695`)
- Add vite-plugin-bundle-audioworklet to codemirror-repl example (`27bcb123`)
- Update pnpm-lock.yaml for codemirror-repl audioworklet plugin (`773724c1`)
- Fix CORS issue: use soundfonts-only default tune (`614dead4`)
- Add local tidal-drum-machines.json for drum samples (`4fe92e51`)
- Add complete local sample library (~486MB) (`0284403d`)
- Add Dirt-Samples and tidal-drum-machines (447MB) (`74620206`)
- Configure all sample packs for 100% local playback (`ae65cc22`)
- Fix syntax error: replace escaped backticks with regular backticks (`9a39f255`)
- Set "Pixel Sunrise" by Nerual Dreaming as default track (`be155554`)
- Set Pixel Sunrise as default tune (`222e6d2b`)
- Fix default tune - exact Pixel Sunrise code (`54cba34e`)
- Add samples() to load RolandTR808 drum machines (`90cb836a`)
- Load all sample packs globally in prebake instead of per-tune (`818c2489`)
- Update composer credit to Nerual Dreming (`886c6096`)
- Add Vercel config to deploy full website instead of minimal example (`4dbc703b`)
- Fix Vercel output directory - remove framework override (`2192d80f`)
- Replace default tune with Pixel Sunrise by Nerual Dreming (`b90f290c`)
- Rebrand project to Bulka - Russian fork of Strudel (`9890c75b`)
- Fix Vercel build config - explicitly build website directory (`aa19cb9f`)
- Clean up branding - remove excessive attribution, add noto:bread favicon (`881d5362`)
- Локализация интерфейса на русский язык (`a219d784`)
- Обновлён README - фокус на проект, а не на форк (`babe5f6c`)
- Исправлен switch в Panel.jsx - вкладки снова работают (`4cd64670`)
- Перевод встроенной справки API на русский язык (`b4194669`)
- Перевод документации и туториалов на русский язык (`757a77ec`)
- Перевод навигации и Technical Manual на русский язык (`1a8b387b`)
- Добавлены кнопки копирования и вставки звуков (`75584a56`)
- Исправлена кнопка вставки звука в редактор (`d1e41614`)
- Обновлены тексты и добавлена информация об авторе (`ef3117d9`)
- Добавлен фильтр по пакам для браузера семплов (`4e271525`)
- Добавлена документация по сообществу и продвинутым интеграциям (`582b9ee6`)
- Add 5 new sample packs (4733 samples) (`5c6b8694`)
- Add tree view component and UX spec for samples browser (`c1c18c08`)
- Integrate TreeView for samples browser (`8e52982c`)
- Добавлена галерея треков от сообщества (`b467be34`)
- Fix samples browser and add local sample packs (`06a3bfc8`)
- Исправлены примеры в галерее треков (`2a8e4120`)
- Восстановлены оригинальные треки от сообщества (`e4bf28cd`)
- Галерея с оригинальным кодом от сообщества (`eea28db1`)
- Галерея с 15 треками от сообщества (`57395fb0`)
- Load ALL Dirt-Samples via JSON + add local sample packs (`3a883b54`)
- Fix sample loading paths and add local sample packs (`f1e65e40`)
- Fix _base paths in local sample JSON files (`ad8e3725`)
- Switch back to explicit exports and use new release mechanism (`1d4f2b81`)
- Formatting (`196f70ab`)
- onceEnded mechanism to clean audioNode.onended callbacks (`0ec9826d`)
- [perf] release unused AudioBufferSourceNode + releaseAudioNode (`68b1cb87`)
- Accept non-instanciated nodes for better developer experience (`af785540`)

## Ноябрь 2025

### 🐛 Исправления

- return silence when no pattern is returned (`ec263925`)
- **tool/dbpatch**: add missing package name (`d2171193`)
- Add missing vite.config.js to supradough package (`bc1c83ed`)
- Update github.com links (`404d9999`)

### 📦 Прочее

- added export to getSuperdoughAudioController() so that its possible to route superdough audio on external application integrations. (`a6008e27`)
- Typos and cleanup (`18dc4944`)
- fix copy paste errors (`cb591815`)
- snapshot (`956598da`)
- add revv() for reversing whole patterns (`f4b1baac`)
- Merge remote-tracking branch 'origin/main' into glossing/sounds-tab-preview (`b824c45b`)
- Merge remote-tracking branch 'origin/main' into glossing/sounds-tab-preview (`7fcb5262`)
- cleanup (`3c4ba667`)
- add CHANGELOG.md + basic script to generate (`405338e4`)
- [perf] in `noise`, let noiseMix do the disconnect when it exists (`89da9bbd`)
- [hydra] return the hydra object when await initHydra(..) is called (`ab64451d`)
- Add oct synonym (`8b427bc1`)
- Disconnect lfos for phaser and filters (`d65b908b`)
- Typo and formatting (`69235930`)
- Add back options code (will resolve in another PR) (`a5c7fcc1`)
- Fix lint (`a126bc9b`)
- fix style (`d5e5ab83`)
- format (`7a4babf6`)
- [perf] level 5 `connect-leak` on `vowel` (`89a6efb6`)
- frequency mod (`e86850b6`)
- [perf] fix `connect-leak` from audioworklet=>param in lfo/tremolo (`a3947d1c`)
- add tests (`7ee6b933`)
- [perf] fix `connect-leak` from audioworklet=>param in lfo/tremolo (`f0d49e0e`)
- Use errorLogger for query and tonal errors (`2bc208d8`)
- test (`bde17594`)
- Hook up the octave control (`cf12e3e1`)
- Fix example (`6c0d550b`)
- working (`6221099a`)
- cleanup (`d5dfbd7a`)
- change variable name (`3a7b2377`)
- synonym: (`3b2df0b7`)
- only eval if script (`4caacb0d`)
- update hash (`20016fd9`)
- solo search n = 1 (`1c4b05d5`)
- add comment (`e72e26eb`)
- rm unessecary file (`19fd0fc6`)
- add synonym (`2efd56e3`)
- prevent filter modulation pops (`1d5f3a4f`)
- Swap with temp variable (`adc3a9ac`)
- wip (`30e672a0`)
- fixed (`7bbb6539`)
- [perf] fix `connect-leak` in `delay` effect (`8ed72b45`)
- Fix codeformat (`7267990e`)
- [perf] fix `connect-leak` added by #1742 when noise() is not used (`04357110`)
- [perf] fix `connect-leak` in fm modulation (`907f03f3`)
- Use frac due to negative frequencies from FM (`4758effd`)
- Also for wavetable (`21543956`)
- [perf] fix `connect-leak` in fm modulation (`f637b8b0`)
- Remove base url from sample server and properly handle url schemes (`d70b7580`)
- reduce sounds-tab click to play latency (`dd2c808c`)
- Attempt system for sounds tab previews (`8144ec46`)
- Stray log message (`74488f84`)
- Simplify declarations via globalThis; fix cleanup/stop (`654c1a86`)
- wchooseCycles has now notes in an example (`30cc46ea`)
- update snapshot with note example (`99beb445`)
- [perf] fix `connect leak` when .noise() is in the mix (`bfa8fa3c`)
- Some final tweaks (`3962558c`)
- Testing out approach to allow list patterns in partials (`cc10122b`)
- Working version (`7a3bea6f`)
- Top level functions (`f5ed6dbe`)
- Properly patternify everything (`641bd2f3`)
- [perf] fix `connect leak` when .noise() is in the mix (`d9f63b8d`)
- [perf] disconnect Offline AudioNode connections in generateReverb (`92d87093`)
- update superdough documentation (`91799f20`)
- Use weak references for sound sources (`22807b38`)
- [perf] disconnect `send` effect AudioNode when `room` is used (`3e05525d`)
- First pass of worklets optimizations (`3c6de3be`)
- Further optimizations, including detuner (`aabf1221`)
- Optimize phase wrap; make voices k-rate (`be3a88b0`)
- Simplify spread (`f8c94255`)
- Final cleanup (`157b0553`)
- Square instead of self-multiply; more voices; more cleanup (`2875814a`)
- Fix typo: 'studel' -> 'strudel' (`3e7ae880`)
- test fix (`a3f82f90`)
- soundAlias example fix (`1b37f1ed`)
- fix branch (`79875ebc`)
- fix for node 24 support #1718 (`247bf9b9`)
- added docs for spaces in scale names (`4df2cc30`)
- codeformat (`809eb713`)
- working (`484c5e6c`)
- format (`f38792c5`)
- fixed build (`a41b07c6`)
- rm console (`10bfe32a`)
- fix broken array (`7b928749`)
- temporarily disable sample autocomplete (`2b575d50`)
- cf (`4f046ae8`)
- working (`91e1cc13`)

## Октябрь 2025

### ✨ Новые возможности

- **codemirror**: add Vim :w evaluate, :q stop, and gc/gcc toggle comment via custom events; docs: add Vim shortcuts page and link from REPL manual (`be9368e8`)

### 🐛 Исправления

- use replicate for ! in mondo (`20e5fded`)
- tonal import in autocomplete + add tonal to codemirror package dependencies (`105db2a4`)
- tests (`319a2d72`)

### 📚 Документация

- add example of custom chained function (`0065db85`)

### ♻️ Рефакторинг

- add op_precedence to express & with desugar_ops (`3a7d5092`)

### 📦 Прочее

- remove extraneous eslint ignore directive (`9fdba637`)
- Merge remote-tracking branch 'upstream/main' (`a036c4f9`)
- use json files in dough-samples via bunny cdn (`9ff0449c`)
- Merge remote-tracking branch 'upstream/main' (`c0e7971b`)
- Publish (`cbe7aaac`)
- Set pattern auto-start to true by default (`fddf2ff2`)
- Re-evaluate pattern if activated (`fcd83cce`)
- Add stick button mappings to gamepad implementation and improve docs (`ee43d85f`)
- degithub (`5cc93996`)
- piano via bunnycdn (`8398385d`)
- Fix trampling of port env variable (`af90baea`)
- Fix example (`1a362bd8`)
- Test snapshot (`6d4787ee`)
- Move deploy location to deploy/strudel.cc (`8f7b439d`)
- Update .forgejo/workflows/deploy.yml (`b09230fe`)
- Add action to deploy 'beta' version to warm.strudel.cc (`ccb5ec16`)
- add _processParts() (`39e2221c`)
- revert 39e2221c29fab34bb4fda6532084d6e624ca38fa (`7071dec7`)
- Refactor sound stopping and triggering logic in SoundsTab component (`a8c613b7`)
- Add setting to toggle pattern auto-start on pattern change (`20bbda30`)
- Fix ZZFX example (`28056deb`)
- Make osc port and host configurable. Changes dependencies. (`06ea5a36`)
- Fix sampler.mjs githubPath (`f0bd5bff`)
- Add semicolon (`ea91d30c`)
- Voicings JSDoc (`fd0a910b`)
- add new flavour under replicate (`c2a5562b`)
- snapshot (`188f0e90`)
- Fix a bug introduced by #4e17cfbdd6 (`46af6ed9`)
- Adds back shape to superdough (`72423c3a`)
- More examples (`4c49cd92`)
- fix interoperability of `all` with hydra (`47c85f85`)
- Don't tag when scale fails, allow mixed sharps and flats, simplify scale function (`c02def03`)
- Fix onPaint for widgets (`ba93bd1b`)
- Some examples (`0fe7edc1`)
- Mention range in docstring (`35016b58`)
- Update conditional to allow  to control user waveforms (`fd03d1fd`)
- Typos on envelope values (`ac7e9c69`)
- Format (`3c1a8c8b`)
- fix: repair REPL sample sources and URL concat (#1640) (`72eaf96b`)
- default to samples if repo not found (`909e0154`)
- update docs (`83c7e634`)
- Fixing some syntax and a typo (`af53bab2`)
- Removing semicolon to be more idiomatic;;; (`9c5c71c3`)
- First pass (`a3b803c3`)
- Remove todo (`57fac576`)
- fix purity of 'withState' so it returns a new pattern (`694162a7`)
- add pattern id to query state controls (`fdde05e7`)
- fix setControls to do union with existing controls (`086596c4`)
- add cyclist version to the query metadata (`ffbbc43c`)
- placate format gods (`64f16c4f`)
- add & sugar (`3dc9b352`)
- interpret & (`b7fa440b`)
- - refactor bjork -> bjorklund - refactor e -> bjork - flip & desugared arguments (`f33db5f0`)
- fix empty catches (`f48f8e46`)
- change the trigger handler to match new hap (`0d082259`)
- Handle detune in the presence of pitch envelope (`120f89d5`)
- Handle scale-for-notes when n also supplied (`3ef6c7c9`)
- Cleanup (`11dd8a31`)
- Working version of partials and phases (`cc4452ce`)
- remove unnecessary comments (`a79fc3db`)
- Working version (`c9381a11`)
- Big matrix refactor (`7cf9db48`)
- Cleanup (`bd73d968`)
- codeformat (`73a98349`)
- remove gcc (`46ba165c`)
- wrong unicode char (`cfe17fcb`)
- weird permission change (`30a2e149`)
- Wrap properly when phase === 1 (`ac582b4d`)
- Consistency (`99e14dae`)
- Hook up FM to wavetables (`f369de13`)
- Import (`d496919f`)
- Add aliases for distort types (`a14bf4d9`)
- Add comment (`c885d847`)
- Docstring cleanup (`72298c5f`)
- Update website/src/pages/technical-manual/project-start.mdx (`8cd497ab`)
- format (`0ecacae7`)
- Fix case sensitivity for synonym search (`b7d98dd3`)

## Сентябрь 2025

### ✨ Новые возможности

- osc add --debug flag to log messages + log errors (`91ad3d72`)
- implement context-aware autocomplete (`7ff7e885`)
- add E# and B# to pitchNames for complete enharmonic coverage (`9200a67a`)
- sound autocomplete matches anywhere in name using includes() (`41d6f1b5`)
- implement context-aware autocomplete (`d87ce960`)
- add E# and B# to pitchNames for complete enharmonic coverage (`90614612`)
- sound autocomplete matches anywhere in name using includes() (`cd659ac2`)

### 🐛 Исправления

- all function in mondo (`8833d623`)
- mondo setcps / setcpm (`86248328`)
- unstable filter (`82c90471`)
- add clamp function (`a0c2edad`)
- rephrase error message + bump to 1.2.10 (`1c3e07af`)
- add node shebang + bump osc to 1.2.6 (`5d7c6c3e`)
- bin field (`aba594b7`)
- use template element for string to html (`2d2b238d`)
- autocomplete container style (`97827957`)
- bring back tests on external PRs (push doesn't cut it for forks) (`d6254294`)
- exclude mondough dependencies (`27339715`)

### 📚 Документация

- add --debug flag to readme (`a25f7637`)

### ♻️ Рефакторинг

- modularize autocomplete context handlers using strategy array (`085e839b`)
- modularize autocomplete context handlers using strategy array (`80fe5b81`)
- modularize autocomplete context handlers using strategy array (`0b67473b`)
- modularize autocomplete context handlers using strategy array (`6b6d4b86`)

### 🔧 Обслуживание

- bump osc to 1.2.8 (`c54fa7d2`)

### 📦 Прочее

- Remove unused var (`bad498a1`)
- working (`cdc5e5f3`)
- format (`9f8143e0`)
- Add back mipmaps; add caching (`a2b8407f`)
- Move invsr to constructor (`3a7ec18d`)
- WIP (`c5f1085b`)
- Remove mipmaps, fix clamping of parameters (`4de0c11f`)
- rename controls (`23d4bfa9`)
- simplify modulators (`c689874b`)
- fix tests (`54ebc97d`)
- fix unison gain (`bf3fe605`)
- fix vib (`d94f1456`)
- working (`8d45016c`)
- add_vgame (`48718dae`)
- gain adj (`4bd103e6`)
- suppoert existing wt_ api (`a9957b45`)
- Actually update normalizer (`a8918d55`)
- Clean up to align with other PR (`c5c05eba`)
- Missed deletion (`8c3ee6db`)
- supports old documented way of loading wavetables: (`626a99ba`)
- format (`774372a3`)
- Add LFOs and envelopes (`917d01c4`)
- sample source for WT (`2b3b6389`)
- Added examples, fixed samplerate issue on import, added to settings tab, fixed spread, added default wavetables, change default phaserand to 0 (`97beaec2`)
- Add control over phase randomization and fix a bug with supersaw (`e3741ae8`)
- Typo (`bd8d207a`)
- A bit more cleanup (`96981c3c`)
- Typo - add back slight delay in cleanup (`f8f42565`)
- Cleanup (`8b2c35b7`)
- res adjust (`dfc4a081`)
- add better example (`266cb614`)
- working (`0633954e`)
- format (`28c94efa`)
- Move algorithm into constructor (`f0669ce3`)
- adjust control range (`da81ba00`)
- rm comment (`c474b8c9`)
- Remove unnecessary defaulting (`d3d9f23c`)
- Some cleanup (`b3537a9a`)
- format (`beafd9c7`)
- fix import (`82893ffc`)
- rm dead code (`da926805`)
- rm deadcode (`fdcdc3aa`)
- fix duck (`1eb9dc73`)
- move unpure up (`45282763`)
- Fix link syntax in `project-start` (`c64aa3bf`)
- Fix web README sample code (`4d39f368`)
- working (`c5bd2a74`)
- Merge handlers and cache regexes in autocomplete (`7080490c`)
- configurable port for osc bridge + add bin field (`5445f081`)
- bump osc to 1.2.5 (`820744dc`)
- export osc function to be able to do all(osc) (`28d7e0e4`)
- overhaul osc readme (`cdb623cb`)
- bump osc to 1.2.8 (`663229ec`)
- working in dev (`3eb40ee7`)
- rm comment (`9a8f8a05`)
- Reordered handlers to be a bit more organised. (`edb6c0db`)
- Working (`9cdba1a5`)
- fix test (`89b7eac7`)
- format (`f2b2f9f9`)
- rm unessecary change (`a68c7847`)
- Merged autocomplete.mjs conflict. (`97861683`)
- Improve autocompletion behavior (`c8875b29`)
- Add autocomplete for mode function (`cb011c8b`)
- add tic80 font (`a7e48599`)
- Added completion for chord. (`6ebe84e0`)
- working (`2cba1dcb`)
- rmconsole (`7dfd7dbc`)
- coerce in correct place (`74e27ca9`)
- fix test (`4cc453c6`)
- format (`a9f7d825`)
- format (`9da027d3`)
- updated names + snapshot (`addb6db1`)
- add some typechecking + fix delay (`df1934f8`)
- Correctly handle silences for non-notes (`92b2013c`)
- Update website/src/pages/workshop/first-sounds.mdx (`352a3c39`)
- word (`e18296e7`)
- Codeformat (`ed006868`)
- Codeformat (`69b03434`)
- Updated the gitignore rules for vscode. (`1670aeba`)
- Add synonyms to autocomplete (`709b654e`)
- added plyWith/plyWithClassic functions (`f5c4373f`)
- use ?audioworklet for supradough worklet import (`a46617f3`)
- Commiting lockfile. (`7676641c`)
- Commiting lockfile. (`39460eb1`)
- Publish (`5e2e6411`)
- Publish (`702e558d`)
- export mondo getLocations (`c42d00f0`)
- Publish (`637d714a`)
- Updated the codemirror packages to be able to access additional imports. (`2a00fcdf`)
- Updated the codemirror packages to be able to access additional imports. (`ed182d4b`)
- Publish (`d0ce82e3`)
- Optimize folding for audio rate (`51b150b8`)
- Formatting and tests (`9848740e`)
- Allow penv values to be falsy (`38ce593c`)
- working (`2511dcc0`)
- working (`e7e80bfd`)
- only show button on user samples (`5414bbe8`)
- change sound background to distinguish action buttons (`a9685454`)
- fix formatting (`66aad000`)
- cleanup div (`70e776e7`)
- format (`99bb227c`)
- fixed (`89e7d523`)
- rm errorlogger console (`5e5e7730`)
- comment out tauri stuff to fix osc (`0a96fa58`)
- formatting (`2fbdc9fd`)
- Fix formatting of REPL footnote (`977420e7`)
- Use currentTime for exact scheduling (`798eb22d`)
- Corrected piping (`9c108862`)
- Move things around to allow selection by name and avoid circular imports; fixed chebyshev (`42ab65ab`)
- Some tuning experimentally and adding back chebyshev (`9b1c23d4`)
- Some missing cleanup (`46a45b45`)
- Add soundAlias function (`56b1ea3f`)
- Add documentation for ?n in mini-notation (`591c3fe0`)
- First pass at modes (`bb982e6b`)
- Include synonyms in autocomplete (`439a7dc5`)
- Add synonyms to reference (`052d09e8`)
- Add some more synonyms to controls docs (`8d2a368d`)
- Update autocomplete name to be the label and update ref to use main name as a synonym (`c199b516`)
- Add examples for ? and (`James Walker`)
- Set delay and reverb nodes properly (`4e2e7908`)

## Август 2025

### 🐛 Исправления

- prettier (`215ab878`)

### 📦 Прочее

- Add vibrato to list of continuous modulators (`6a09f54b`)
- Add verify link to mastodon a/c (`9eb3c941`)
- Update README.md (`dc6b766a`)
- Add flat flag, automatic json saving; filter out non-audio at base dir (`189e650a`)
- More descriptive variables and comments (`e4070b86`)
- Parity with old adsr (`869141b0`)
- Codeformat (`9eeb6da3`)
- Remove internal LFO (`d1869c18`)
- Update docstring to include new warp modes; save sample server for a separate PR (`fe46e1da`)
- First pass at envelopes with curves (`763f900c`)
- Working version (`ea017e76`)
- Rename params, more examples (`3d25aa1b`)
- Typo (`022ac95b`)
- Add a space after paragraphs in descriptions and add padding to hover tooltips (`18b1739b`)
- Switch to more specific class and fix p->div (`877bc95a`)
- Use white-space instead; fix scrolling; don't close autocomplete tooltips on click (`a5886bb9`)
- Add wavetable oscillator with scanning, warps, and detune (`02cd79a6`)
- fix benchmarks (`b8c98736`)
- Working version with note names (`b08890bd`)
- Allow negatives and multi-accidentals (`79453ac2`)
- Added some description and examples of multi-accidentals and negative octaves (`b8c46d6b`)
- Update restore defaults to not delete patterns (`9b917632`)
- remove hs2js postinstall (`8465517c`)
- Add release parameter, avoid clicks, some cleanup (`4f15d681`)
- Codeformat and examples (`bf5d9917`)
- add --json flag to strudel sampler (`66aa3ac1`)
- bump @strudel/sampler to 0.2.1 (`b1270731`)
- generate strudel.json (`da283eb5`)
- add --yes flag to readme, making sure first run works (`42a903ec`)
- Example tests (`9a9fe83f`)
- Final tweaks (`a0fc52b1`)
- Added Refrence to arp() arpWidth() and hush to Docs (`d009b959`)
- Added Refrence to arp() arpWidth() to Docs (`189daa39`)
- Fixing hush refrence to code documentation (`36441e7b`)
- fixed new scrub refrence (`d6ee10e0`)
- Update effects documentation to include information on signal flow (`32fe73ab`)
- Clean up wording on orbit routing (`ce7cff2c`)
- Code format (`59c8d707`)
- Typos grammar etc (`34e8a574`)
- Mention order of effects (`e4907742`)
- Working version of duck on iOS safari (`64f7bc44`)
- Ensure zeroGain is cleaned up after use; move start/stop to after callback assigned for safety (`659071a4`)
- Lint requires no empty blocks: pass comment (`d64a0ef0`)
- Publish (`f17a4d04`)
- rm var (`cf55d4c8`)
- Added scrub() to Learn Docs (`7d24e456`)
- Adding the Duck/Sidechain effect to Learn Docs (learn/effects.mdx) (`f7f1bd63`)
- working (`e9aef9e4`)
- optimise euclidish, creating new `morph` function in the process (`b7176155`)
- add tests (`e7e63688`)
- minor changes (`a20468b8`)
- fix lookahead (`5ff59a87`)
- fix test (`018649c4`)
- Add example tests (`2ab2cc5f`)
- Add example test and update fastChunk test (which was previously broken) (`c8c6a2ce`)
- Fix for a zero appearing (first) in FM's ADSR (`3dd667e8`)
- Use the per-sample frequency and properly wrap polyBlep (`218952b8`)
- Seemingly working version of supersaw FM and light cleanup (`2e8a8b50`)
- First pass at extending scale function to include notes (`a4ab8e1b`)
- Cleanup and simplification; improved docstrings (`7046c1f5`)
- Add new tests, organize old tests, fix issue with  note (`a4c040e1`)
- Fix signature of functions in log and logValues and add tests (`3b6777e2`)
- Formatting (`23e5358a`)
- Add doscstrings and move stringifyValues into its own function for reuse (`de15d79e`)
- Final docstring cleanup & adding hap tag to logValues for consistency (`f22f8934`)
- fixed export (`450f9685`)
- codeformat (`34b10b6c`)
- pass an array of names instead (`39c45bc2`)
- document ability to specify an array of names (`ad113b38`)
- add synonyms for scaleTranspose (`efc23333`)
- add strans too (`d55ef8e8`)
- add trans alias for transpose (`9017085f`)
- add synonym to docs (`ef3cd56b`)
- flyby improvement: add dec synonym to docs (`051efdc1`)
- webtimeout (`c2b8cc4b`)

## Июль 2025

### ✨ Новые возможности

- **xen/tunejs**: support sending a custom tuning (`0722cf7d`)

### 🐛 Исправления

- repl autocomplete not rendering correctly (`3292f888`)
- tooltip should respect themes (`e7839a09`)
- **midi**: ensure midin initializes device state correctly (`c8cf1dc7`)
- supradough onTrigger after breaking change (`5632d83a`)
- can now use def in mondough (`16e9aca2`)
- @synonym -> @synonyms (`bfdfab0a`)
- remove github contributor avatars (`72b582c6`)
- remove first gm_synth_bass_1, as it doesn't work in safari (`a6ad58a3`)
- lint (`82b14b52`)

### 📚 Документация

- **website/learn/xen**: discussed strumming (`37fde8f3`)
- **website/learn/xen**: add tune() examples (`31c1450e`)

### 🔧 Обслуживание

- add link to contributors (`671a22fc`)
- **docker**: basic support added (`cbb4207b`)

### 📦 Прочее

- working (`d6045c94`)
- prefix "S" for solo (`7f103e77`)
- works (`30c80119`)
- add euclidish (`ee3e1217`)
- fixes (`784ee576`)
- delint (`03cbe4b6`)
- Fix incorrect stack Mini Notation (`6ee9dea2`)
- working (`3b6e3624`)
- prevent clicks (`7691403f`)
- variable srate (`4d13b981`)
- change 909bd to sbd (`973ae4c7`)
- clean up comments (`612efad3`)
- duckarray (`51f0bab1`)
- description (`dae1560c`)
- working (`fb7d76a2`)
- add another example (`82f0dbb7`)
- test (`fe18aa77`)
- fix lint (`2415210b`)
- rm unessecary json (`cf5c502a`)
- frmt (`4a894b0b`)
- working (`66828e17`)
- fix (`75b6f592`)
- fixing... (`9416f317`)
- fixing.. (`ccf2a6de`)
- fix def skew (`b2f90ecb`)
- change params (`34874862`)
- fix test (`dd04d49c`)
- Ensure no default extensions conflict with extensions in defaultSettings (`06fafbd8`)
- removed duplication and unnecessary keymappings in codemirror config (`5448fc60`)
- def (`1fdd87a4`)
- Merge remote-tracking branch 'origin/main' into supradough (`a32473d5`)
- add basicSetup for keybindings (`10156f00`)
- added codemirror as an option + removed fallback on defaults (`bf3de368`)
- added multicursor support on ctrl/cmd + click (`e7e321fe`)
- pnpm codeformat (`eb4f1f69`)
- working (`735f57e2`)
- add new am examples to doc (`cf7fc239`)
- fixed keybinding presedence issue (`d645cc6f`)
- fully implemented (`ce5f4251`)
- rm deadcode (`1dd1ce25`)
- remove unessecary cyclist changes (`6327e17f`)
- c (`9b3c6b9f`)
- Merge remote-tracking branch 'origin/main' into daslyfe/gainmod (`16b2fc06`)
- added tab-indent setting (`8defdc42`)
- default cycle (`63fcce79`)
- format (`edb347e8`)
- add logo (`81a98118`)
- revert add logo (`681eed3d`)
- sync without cyclist changes (`d9e44dcd`)
- add version tagging guide to contributing.md (`a7a283a9`)
- wip (`4cc2de9b`)
- Merge remote-tracking branch 'origin/ontrigger-refactoring' into daslyfe/gainmod (`077556e7`)
- Fix randrun and deps including shuffle. Fixes #1441 (`05bcd5e3`)
- snapshot (`a901e2e3`)
- resetDefaults when editor is reset (`664d76cf`)
- some clarification comments (`34771f03`)
- disable fm for supersaw (`168269a8`)
- Modify `extend` to better match behavior of `!` operator (`f5ae50c3`)

## Июнь 2025

### 🐛 Исправления

- format (`5a72ea94`)
- js example (`82e3f9b8`)
- reset all transforms before eval (`d58b3e49`)
- tests (`64ab3c9b`)
- cpm -> setcpm (`66673d21`)
- extend in some situations, like [[bd]@3 bd] (`15f3321b`)
- fall back to silence when empty removes annoying error (`1cc15c7e`)
- reference tab critical fail (`e52017c0`)
- hpf (`d1b6b6be`)
- node export (`c9f494d8`)
- calling doughsamples multiple times (`3963b807`)
- scheduling + sample playback (`356d4360`)
- note2midi for notes without octave (`2f2bf5bf`)
- hot reloading for dough.mjs (see comment) (`5988e3d3`)
- polyblepped sawtooth + fm = death (`ab052ce1`)

### ♻️ Рефакторинг

- remove first param of all onTrigger calls (`e3680b96`)
- replace .cpm with setcpm calls + doc setcpm (`6c4aa12c`)
- stereo sample support + channelwise fx (`1081fc69`)
- make webaudio glue as minimal as possible (`2cbd35f6`)

### 📦 Прочее

- add setDefault + resetDefaults to superdough (`2e335697`)
- merge (`688e275e`)
- correct rd to cr (`c063fb6b`)
- document setcps in cycles reference (`504fe9ed`)
- add 1.1.0 release notes (`f2330a23`)
- add 1.2.0 release notes (`eac6f27c`)
- cf (`77bab433`)
- working (`fef50e65`)
- fix json (`2dd52813`)
- delaysync (`09d05abd`)
- add test (`7a53f6c0`)
- test + bugfix for chunkinto (`ff5b11f5`)
- tests and tweaks, and add chunkBackInto (`f44caf90`)
- format (`27073d6c`)
- snapshot (`df06248c`)
- fix upstream test (`b1aba294`)
- Case insensitive search in the reference tab (`e19fd244`)
- don't interpret note as n (`e2a29914`)
- add back comment (`71e60ed8`)
- allow calling `all` multiple times (`0003ce17`)
- add unjoin, into and chunkinto (`fe9a91d1`)
- Update CONTRIBUTING.md (`a750764f`)
- website intro: fix whitespace and code hosting name (`e68df9e7`)
- a test for #1396 (`95193b0b`)
- reify arguments of stepcat (`440f1cb0`)
- format (`a1d181d6`)
- avoid double action for PRs, try turning on pnpm cache (`a8757fec`)
- apt install zstd for caching (`f3c4afaa`)
- don't trash lists passed to timecat (`c01b4c65`)
- Update README.md (`4d3bbd50`)
- Update website/src/pages/learn/code.mdx (`5efcd14f`)
- arch (`184cb746`)
- arch2 (`011139ef`)
- allow _ as silence (`c751bb7e`)
- add e as euclid that accepts a list (`9412bf60`)
- use + and - as late / early + add some extra error handling (`10ac7c35`)
- comment (`7e6876c2`)
- codeformat (`bebbe30b`)
- rename .github to .forgejo (`351314cc`)
- replace github links in release notes (`2bc3d69f`)
- use yt for gh video assets + replace release asset links (`912b3270`)
- replace remaining links to old github repo (`0163c8ba`)
- Merge remote-tracking branch 'origin/main' into degithub-links (`4137f394`)
- more degithubbing (`a7f044e1`)
- link back to tech manual in wiki (`d856e567`)
- even more degithubbing (`9738c90b`)
- fix homepage link (`ee7d7830`)
- move iclc paper under docs/ (`2cb22e94`)
- add the technical manual again (`cc4cad05`)
- Update .github/workflows/test.yml (`fec38bd5`)
- Update .github/workflows/test.yml (`1141b180`)
- Update .github/workflows/test.yml (`8c8e9141`)
- fix json (`062201d1`)
- missing double quotes (`dcac2547`)
- ignore .pnpm-store (`3dbae790`)
- avoid floating point inaccuracy by using Fraction (`7f50bceb`)
- Update .github/workflows/deploy.yml (`38e7fe60`)
- Update .github/workflows/deploy.yml (`8fba92f4`)
- Update .github/workflows/deploy.yml (`3428e18e`)
- Update .github/workflows/deploy.yml (`0c193238`)
- Update .github/workflows/deploy.yml (`47de9e45`)
- Update .github/workflows/test.yml (`aa205269`)
- lazy sample loading (`bbd7ed0f`)
- improve filter performance making it accept hz directly (`5cbd38a3`)
- up latency (`3caf08e1`)
- improve export: add stereo + measure performance + add bigger pattern for testing (`14193abe`)
- close bakery (`2c9f9d78`)
- add default code (`79f7938b`)
- don't load patterns from db (`40ba5d44`)
- leave out part about shuffle in welcome tab (`ef3b9a68`)
- bring back loading code + fill in default pattern later (`694a22af`)
- Merge remote-tracking branch 'origin/main' into supradough (`30f7127a`)
- format (`8363ba5a`)
- snapshot (`239987af`)
- working (`372542dc`)
- Update README.md (`94746b5f`)
- Update README.md (`84efa664`)
- > codeberg (`b6e67f34`)
- move technical manual from https://github.com/tidalcycles/strudel/wiki/Technical-Manual (`580447d0`)
- github > strudel (`5fdea7fd`)
- less github (`307e5ea2`)
- add codeberg repo update command (`c0ae12f3`)
- degithub (`990f1c6e`)
- less github (`a3d9d68c`)
- fix issue #1368 euclidLegatoRot (`d7b83e20`)
- pitched delay (`7e8206dc`)
- Fix style issue (`1b7fbecf`)
- add chorus (`43504d16`)
- rudimentary sample map loading (doughsamples) (`3ac0fdf6`)
- Add browser cache explanation in samples.mdx (`ae15bb72`)
- gaincurve (`aeaed504`)
- supersaw (`3d300077`)
- apply speed to synths and samples + add normalize flag (for fit) (`e8254735`)
- Update samples.mdx (`117cf7e1`)
- vibrato (`e99adffc`)
- pitch envelope (`ac7ee376`)
- half working samples (`0f2aa956`)
- buffers can now be repitched (still with aliasing) (`55aef188`)
- exponential lerp (`ec9109dd`)
- adjustable envelope curves (`181e51ef`)
- move things around in init + shorten things (`73ef43dd`)
- feedback delay (still hard coded feedback / time values) (`1dac6963`)
- set delay time and feedback from voice (`d7435705`)
- white brown pink noise + crackle alias for dust (`e54449f5`)
- add square alias for pulse (`77463a19`)
- fm (`b000f229`)
- coarse (`87598e10`)
- crush (might be wrong) (`0cc18a64`)
- panning (`623bf933`)
- pulse osc + velocity (`9ed690ce`)
- distort (`794bf869`)
- hpf + bpf (`6cf0b6f6`)
- rough filter envelope implementation (`e4cd5dc5`)
- better filter envelopes (`998a0102`)
- ignore dough-export to fix checks (`ccc90d54`)
- move supradough to separate package (`43c05cb5`)
- remove log (`49f38aed`)
- dough as a singleton worklet with voice allocation (`930dabe4`)
- handle invalid spawns + remove logs (`f0e6c548`)
- render pattern audio with node script (`ccec7e72`)
- fix click (`5129fa66`)
- fix volume (`ff4b7ca1`)
- working (`df923174`)
- berlin (`e0a21a93`)
- fix test (`cde943d7`)
- begin supradough (`483843bd`)
- proper lpf mapping + remove redundant worklet params, (`5336569c`)

## Май 2025

### 🐛 Исправления

- typo on docs causing problems with autocompletion. (#1350) (`5c3501ca`)
- rests at the end (`2951a60f`)
- ! and @ operators (`4208c79c`)
- superdough worklets bundling (`7bec92ca`)
- tests -> use vite plugin in tests as well (`f993056f`)
- import in vitest config (`6f2e026e`)
- duplicate key (`685b4f17`)
- repl package build (`e9e2e881`)

### 📦 Прочее

- bbstart (`0287be54`)
- test (`a4b55776`)
- preserve stepcount in chunks (#1358) (`0b0f9fe1`)
- fixed (`3d37c731`)
- codeformat (`009f5445`)
- fixtest (`14a0dea8`)
- Publish (`b312ff63`)
- bb waveforms (`1e3f5469`)
- WIP oscillations (`608dfd51`)
- bbs (`36663702`)
- working (`63f4f202`)
- working (`382dce65`)
- working (`dde2b9c6`)
- Publish (`9bd7b662`)
- structure (`058cb366`)
- bb (`b8d2d024`)
- format (`65f1760f`)
- add test (`45bccf13`)
- gain (`5e4eb7fc`)
- remove n (`760d1abe`)
- Fix typo (#1346) (`99779171`)
- asconst (`56d3436f`)
- format (`59bdfd2c`)
- format (`099d321d`)
- lint (`64ebfb57`)
- working (`38908bac`)
- lint (`38e47188`)
- lint (`98346261`)
- format (`440ce0cd`)
- change pipe symbol from '.' to '#' (`9b10dc85`)
- format (`1eb5f6a9`)
- delint (`98efd72a`)
- format again.. (`1b3b0784`)
- revert web + repl versions for next try (`9cf1cd26`)
- Publish (`8d02d427`)
- support line comments (`e3a6444f`)
- tweak (`29e5833c`)
- tweak again (`60dd8bd7`)
- strings now get type "string" to be discernable from plain variables (`ffe831d2`)
- fallback to triangle when non-string is given (`75653542`)
- rename mondo package to mondolang (`607a6121`)
- delaytime (`006ce9d1`)
- lint (`94257e81`)
- more sicp tests (`243adda3`)
- Merge remote-tracking branch 'origin/main' into uzu (`2da03ab4`)
- Publish (`177f5c14`)
- reference package vite config (`9ceebd46`)
- make broken hs2js + tidal packages private to opt out of release - some issue with the build command (`6398b5fb`)
- downgrade reference to be able to get 1.2.0 (`f2a9e2d4`)
- Publish (`57029146`)
- add vite-plugin-bundle-audioworklet and use it both from website and superdough (`fdbbcd2d`)
- update metadata (`22f114be`)
- remove broken tauri workflow (`306a6db8`)
- Publish (`8f58d578`)
- also use worklet plugin in web package (`53e50d81`)

## Апрель 2025

### 🐛 Исправления

- lint (`b2588e8c`)

### 📦 Прочее

- button size and icon (`c48aba50`)
- reset gaincurve (`f32abac0`)
- def (`c088c0c9`)
- fix vel (`480b5ff5`)
- update docs to reflect import sounds tab change (`d02b1fc0`)
- working (`53d43dc4`)
- lint (`3ee7d0cd`)
- formatted code with prettier (`f5399305`)
- change import sounds label to import sounds folder (`3dae2a80`)
- add new import sounds tab with explanation and hyperlinks (`759cdb62`)
- fixed strudel bool (`bd08b74e`)
- fix lint (`c852b5fa`)
- working (`ad25ce7b`)
- udelsconfig (`17e1ebeb`)
- disabled (`29680173`)
- format (`6e3aa693`)
- send delta in seconds, to match tidal/superdirt (#1323) (`ab76b442`)
- rm dependency (`a32bb58f`)
- lint (`2b69004e`)
- working (`344d9ca1`)
- rm deadcode (`b1090a1d`)
- var name (`9e233fe5`)
- working (`05ea9c86`)
- add tests (`a4682018`)
- var (`695ac6a5`)
- sicp 100 (`d5ef686f`)
- rational number test (`7fc21d55`)
- working (`c2d4b529`)
- lint (`6b8ee8ad`)
- let expressions (`989bd099`)
- add half interval method example (`d4733a30`)
- more sicp tests + support special form for function def (might remove later) + support multiple expressions in fn body + support lexical scoping (`39d27844`)
- more tests (`60d09adb`)
- remember pair locations + allow passing a scope to MondoRunner.run + make def a side effect + proper lambda with multiple args + closure (`94a3a60e`)
- add some examples from sicp book (`077aac18`)
- move +- to ops + add "raw" to parsed pairs + implement match + if + add more sicp examples (`f6ffdd6a`)
- more sicp (`aa70246a`)

## Март 2025

### ✨ Новые возможности

- add mondough package to run strudel via mondo (`fae59a6b`)

### 🐛 Исправления

- mondo dont mutate (breaks lambdas) (`b4027fd9`)
- lint (`1738e4d5`)
- test (`6b2b8b51`)
- patterns without structure would error on draw (`cf1f4ec3`)
- markcss can now override styles (like color) (`65a7b303`)
- lint (`d2e93a99`)
- formatting (`95526ac9`)
- lint error (`ea616279`)
- support negative numbers (`90201275`)
- top-level functions arp/arpWith (`9b8761bc`)
- lint errors (`dbe39153`)
- relative imports (`edc29031`)

### ♻️ Рефакторинг

- Consolidate configuration variables into midiConfig object (`2ccb95ae`)

### 📦 Прочее

- gain curve func (`5ed28d61`)
- lint (`999c0b23`)
- mostly working (`e55b7c6c`)
- max poly (`8962e5a4`)
- def node (`a0fb8fb3`)
- refactor evaluate function into bits (`f2372e7a`)
- ramp cut audio node (`6f9bcc53`)
- settings panel (`d8e4055c`)
- format (`2f4cba4e`)
- rm comment (`bceb37e8`)
- remove signal synonyms for now (`e9de4599`)
- remove minor change (`c9f58220`)
- trim readme (`2e017e46`)
- improve mondo package api + update readme (`c9cafa37`)
- add highly impractical .(. notation for local infix application (`2938bfd8`)
- refactor - rename MondoRunner.call => .evaluate - can now evaluate leafs - remove .( because it's confusing and half baked - support (.) as id function (`7716fdb9`)
- this is the way (`abc7a1e4`)
- patternable function names (`64a6dacc`)
- Fix typo pattnr (#1316) (`6d355a80`)
- update docs (`cbd448e9`)
- update test (`739fb856`)
- fix doc (`bfbd920b`)
- fix doc (`3eea9086`)
- style update (`282180ef`)
- lint (`dc8e5d53`)
- update tests (`dee75bc7`)
- allow # character in plain values (for sharps) (`6d214564`)
- mondo mode for StrudelMirror / repl / mini repl (`5eabcf06`)
- mondolang function for mondo repl + fix rests (`51e3aa25`)
- bring back $ for stacking (`dd5743ab`)
- doc (`3a19e234`)
- improve doc (`92b5b653`)
- more docs (`222e479e`)
- fixed (`38876e13`)
- test (`dccfefac`)
- support $ tidal style (`492271d7`)
- rename resolve_ -> desugar_ (`bca16cdf`)
- add chooseIn + chooseOut (`12bb82d2`)
- add (`Felix Roos`)
- mondo cleanup (`5d4ef46a`)
- flip pipe order: now pining to the end of the function... (`f71db4ae`)
- simplify mondo: ":" is just another operator (`208706fb`)
- change default markcss (`3657e2fd`)
- transpiler cleanup (`55a5f1d6`)
- add .. operator (`3505732a`)
- support ! and @, also express seq and cat with stepcat (`432e8dcc`)
- mondo add % for pace (`9626f3cc`)
- use stepcat for curly braces (`e1629562`)
- generic bracket names + simplify call handler + refactor mondough (`c691916c`)
- add sin sqr cos aliases (`05ae3183`)
- add mechanism to register custom mini language (`989fdfa2`)
- mondo as minilang (inactive) (`ac6472a4`)
- waveform aliases: tri, sqr, saw, sin (`c33cfc78`)
- add notes (`129fd7d6`)
- rename @strudel/mondough to @strudel/mondo (`88ca5446`)
- in mondo (`642ddcdb`)
- proper lambda functions with local scope (`e04f250a`)
- Add num samples from 0 up to 20 (#1310) (`f5bb88a6`)
- mondo improvements: - add second string type - add $ as alias for stack - simplify leaf handling (`7db52b3d`)
- add num samples (edited numbers) (#1309) (`b27c9dbf`)
- stacks now work within () + write more tests (`b71b1354`)
- rename uzu to mondo (`e3df5044`)
- export strudelScope for mondo to use (`e782dc09`)
- refactor controls - multiple args wont be interpreted as sequence anymore - you can now pass value, pat to set value to pat (`11196fb1`)
- mondo highlighting (`b0da3531`)
- slightly improve error handling (`77ade075`)
- native support for rest symbols - ~ in s (`3c3832db`)
- support variables (`2dd445c1`)
- support - ~ in plain type + simplify errors (`118b619b`)
- reify variables (`40f64891`)
- begin uzu package (`62705da3`)
- Update Documentation (`17df4189`)
- codeformat (`5fcb96f7`)
- remove debugging artifacts (`3c2692bd`)
- change behaviour of polymeter, and remove polymeterSteps (#1302) (`b15843f3`)
- pwm (`c7ee9092`)
- vibrato working (`8a502e88`)
- docs (`b4af8402`)
- gain adj (`64b99f5a`)
- format (`4048ced7`)
- bounds (`003e3b79`)
- bugfix zoom stepcount (#1301) (`fece4e8b`)

## Февраль 2025

### ✨ Новые возможности

- basic midimap handling (`0b1bc74c`)

### 🐛 Исправления

- dont bail when ip cannot be determined (happens when no wifi) (`c639e44c`)
- pnpm check (`88fb0b3b`)
- make sure ac is defined (`63ee218d`)
- sf2 soundfont timing (`1bf74024`)

### ♻️ Рефакторинг

- **midi**: extract MIDI message handlers into dedicated functions (`f802f18d`)

### 🔧 Обслуживание

- delete old files (`dc8becc6`)

### 📦 Прочее

- Fix test error #1297 (`7c1e968d`)
- atomify log (`afdcd420`)
- Fix misplaced ending sentence (#1296) (`6e972f43`)
- fallback (`8867a843`)
- fixed (`b531962b`)
- foreground text color (`904c213d`)
- button hover color (`f66478b9`)
- lint (`5a5bbfc8`)
- theme attr (`3df9744d`)
- lint (`65282bf5`)
- Allow wchooseCycles probabilities to be patterned (#1292) (`0925ea56`)
- Signpost licenses for source code and samples a bit more, ref #1277 (#1289) (`470a3452`)
- fixed style (`1ded3984`)
- font settings (`4959bff0`)
- working (`42c22625`)
- showcase tweaks (#1291) (`64c94b36`)
- accessibility (`bbb88a23`)
- lint (`5c5d4115`)
- girl (`b664bd3d`)
- support serving any file (like strudel.json) (`b0167206`)
- working (`50f07608`)
- fix paginator (`c210ea3f`)
- remove unesseccary code (`47f65da6`)
- prettier (`f47f5c13`)
- fix offset (`3e6a45cf`)
- prettier (`dfb7d7bb`)
- reorganize panels (`5cd98880`)
- Fix for `squeezejoin` and functions using it, including `bite` (#1286) (`84581b22`)
- Fixes inverted triangle wave by renaming it to `itri`, making non-inverted `tri` (#1283) (`aabc82be`)
- remove midimap JSDoc (`b98ebc69`)
- codeformat (`43523b73`)
- rename repeat back to extend (#1285) (`3575fc3c`)
- 'miditouch' change sendKeyAfterTouch to sendChannelAfterTouch (`5ff1d352`)
- fix sendChannelAfterTouch -> sendChannelAftertouch (`451cdcc3`)
- Make mqtt topic patternable (#1280) (`07b0a2e0`)
- update mqtt connections dictionary (#1281) (`cda79f3e`)
- mqtt bugfix - connection check (#1282) (`cf8203a4`)
- Add DOI (`981ad1e2`)
- remove midisounds for now (`efd40716`)
- allow duration and cps metadata to be added to mqtt messages (#1279) (`8bde61fa`)
- Add midicmd JSdoc (`52d1443c`)
- add sysex handler to midicmd (`20dcae68`)
- update 'midicmd' in README.md (`9b279ff6`)
- edit README.md (`54c54543`)
- [breaking change] Sample signals from query onset, rather than midpoint (#1278) (`49e9b849`)
- codeformat (`ed7dc4ef`)
- remove experimental code (`4636d824`)
- - add defaultmidimap function - add midisounds function - add midiport control (`c810a02a`)
- defaultmidimap + midimaps (`e99f229b`)
- midisounds (`848edb1f`)
- Polish, rename, and document stepwise functions (#1262) (`ce9d2304`)
- simplify (`95b1548e`)
- simplify more (`b87b2aff`)
- allow passing ccn numbers directly to midimapped control name (`b74d5bec`)
- allow loading midimap from url (`d86df33b`)
- Stepwise documentation tweaks, with mridangam samples (#1275) (`018d1ccd`)

## Январь 2025

### 🐛 Исправления

- doc example (`bcb1da82`)
- bring back examples test + ignore device motion examples (`75f5f456`)
- error (`06415d7f`)
- react 19 error (`892a6864`)
- build error -> workbox max file size limit (`d54022a6`)
- tailwind setup for real now (`f4b87531`)
- lint error (`46400eff`)

### 📚 Документация

- move dev info down (`44828e20`)
- cpm -> setcpm (`1f5f1a63`)
- use mask instead of gain, so you see what's active (`9d5d86bb`)
- add info on how to test repl package (`d03fc43f`)

### ♻️ Рефакторинг

- simplify test runtime (`5cef1a8c`)

### 🔧 Обслуживание

- bump vite + update lockfile (`bcef67e5`)
- update vite (`ceb466ab`)
- revert to main package lock (`b611c9c2`)
- pnpm i (`1fb68af5`)
- remove obsolete snapshots (`e2b7c3a6`)
- update website packages (`1d7f755b`)
- remove vite-plugin-pwa dependency - its only used for typings - it creates problems with peerDependencies - just let it die (`0e59a233`)
- update test action versions (`de7c6527`)

### 📦 Прочее

- update action versions (`f558b4b8`)
- improve vscodeDark (`ce5add4a`)
- comment out xcode theme (`d353eff1`)
- Merge remote-tracking branch 'origin/main' into housekeeping (`fbf9d06d`)
- add as to controls page (`1058c272`)
- migrate remaining themes (`b55661b2`)
- theme glowup (`ac83ff88`)
- glow up android studio theme (`6062a2e7`)
- improve duotoneDark (`6c537cb1`)
- improve github dark theme (`e31a9965`)
- improve gruvbox dark (`1e59b4ef`)
- improve material dark theme (`b51dacb1`)
- rename okaidia to monokai + improve it (`33b31fb4`)
- improve sublime theme (`212a4781`)
- abcdef + androidstudio (`e754070a`)
- migrate 5 more themes (`9112a0c4`)
- 5 more themes (`63f9f6cb`)
- 4 more themes (`c09b590c`)
- 4 more themes (`1053c7eb`)
- 5 more themes (`1460defa`)
- format (`ff4d4357`)
- Refactor gamepad polling to use Strudel's signal system (`bb56ed47`)
- update documentation (`443760c0`)
- prettier! (`23ed3426`)
- Merge remote-tracking branch 'origin/main' into lu/add-drum-suffixes (`4f6e38f7`)
- add aliasbank function (`e6604491`)
- migrate eslint (`c7cd03a2`)
- fix repeated calls (`a2c896f7`)
- update pnpm action-setup (`b3e0f180`)
- update pnpm version in workflows (`4f35027d`)
- update codemirror package (`badbcdcf`)
- update all the things again (`3e721d91`)
- ignore ts error (`aa1efcc7`)
- improve function (`710da1d2`)
- remove unused check task (`5d98fd2a`)
- cleanup (`5a188f74`)
- set by value so it actually updates (`90fe4d38`)
- revert manual bank aliases (`234beff0`)
- fix and overload (`7222c29c`)
- add docs and allow arrays of aliases (`fd68ad9b`)
- add comments and case insensitivity (`f362f1c9`)
- allow aliasing via a path (`5d3778fb`)
- setup default aliasing (`7e1c6c1c`)
- add more aliases (`c9bdfe57`)
- Update website/public/tidal-drum-machines-alias.json (`d192201e`)
- remove uiw codemirror themes to fix site + todo: refactor themes to not use uiw package (`fc3d7ade`)
- migrate custom themes awa from @uiw/codemirror-themes (`afa202e2`)
- fix test error (`393d17a4`)
- add `midibend`, `miditouch` (`b8b999ea`)
- update documents (`e19d059c`)
- fix midibend and miditouch (`3189b365`)
- Prettier! (`3ffe3957`)
- add testsnapshot (`c5d7f954`)
- Add cc to `midicmd`, add API Reference for midi related controls (`d06a75a2`)
- MQTT - if password isn't provided, prompt for one (`9432f780`)
- add reference package (`5d562e8c`)
- support `all(pianoroll)` and `all(pianoroll({labels: true}))` (#1234) (`5731ae0c`)
- add reference readme (`0ca540b6`)
- update main package + core package + vite / vitest (`1b8ccc05`)
- update mini package (`024f424b`)
- update tailwind (`35911f30`)
- Add 'sysex' control (`57c48f0c`)
- update ProgramChange from `pc` to `progNum` (`a4792e29`)
- fixed (`188d3181`)
- snapshot (`6cde970e`)
- add midicmd documentation (`268c66cd`)
- add midicmd documentation 2 (`1349ecb4`)
- adjust midicmd heading level (`f95eadab`)
- Update README.md (`cdf50cba`)
- Get sysex working (`13a45126`)
- Add device specific setting folder 1 (`3325a8db`)
- prettier! (`a8803784`)
- add shortened bank names (`45fffa51`)
- add "as" function + getControlName (`773420fd`)
- snapshot (`fc41bd1a`)
- Fix sometimes (#1243) (`00b03418`)
- add documentation for pc and sysex (`e085819f`)
- update function (`7b6074cc`)
- remove possibly not needed alt (`c33c8b54`)
- merge main (`2348023b`)
- format (`d2a9b9b7`)
- _keyDown (`570c7a0d`)
- format (`36374fb3`)
- Add program change(pc) and sysex to midi (`859f153e`)
- midi mapping to handle program change and sysex (`c242f5f6`)
- Add stepBind, and some toplevel aliases for binds and withValue (#1241) (`55c226cd`)
- register pc and sysex as control keywords (`155ef9e9`)
- Update value-modifiers.mdx (#1238) (`c3be21e6`)
- channge function name (`f5127147`)
- snapshot (`7410ac5c`)
- add keyDown (`3d162536`)
- add doc link (`052addd6`)
- working (`7b914e7f`)
- update snapshot (`1d144fae`)

## Декабрь 2024

### 🐛 Исправления

- spectrum example test (`896a3828`)

### 📦 Прочее

- key alias and breakout (`2443aad0`)
- add func (`42519581`)
- merge main (`1da80db0`)
- fix conflict (`974376d4`)
- fix test (`08f6c1ad`)
- Make `all()` post-stack again, and add `each()` for pre-stack (#1229) (`9bf0f624`)
- Documentation for all/each, and bugfix for each (#1233) (`1e3bbc57`)
- Update tests snapshot (`426c5edf`)
- Fix accidental multi-line jsdoc name (`8644b3d1`)
- Add binary and binaryN to learn pages (`9d029ac9`)
- Fix lint (`c6418789`)
- some voicing doc edits (`49d4fb49`)
- minor amendments (`d30623da`)
- write more (`e12dadb3`)
- delete timbre draft (`e4f89f07`)
- Fix regression for d1, p1, p(n) (#1227) (`04eac3e7`)
- Add DOC for devicemotion (`a5b05d21`)
- Add DOCS for gamepad module (`033b123a`)
- getting rid of AI-smell (`36fe942e`)
- Prettier💄 (`0be9f439`)
- Make the docs friendlier (`7cf7575e`)
- spectrum widget (`8a33c171`)
- spectrum doc (`eb029978`)
- Fix lint (`111cea23`)

## Ноябрь 2024

### ✨ Новые возможности

- midi() command support external instrument parameter mapping (`ec470aa2`)

### 📦 Прочее

- Apply `all` function to individual patterns rather than final stack (#1209) (`ef53367b`)
- MQTT support (#1224) (`c66f1910`)
- Make cps patternable (#1001) (`05313696`)
- Add GamePad Module (`d741b4d1`)
- fix errors in README.md (`f01d51f7`)
- Add Device Motion package (`f33ec0e2`)
- solo and sync configuration; codeformat (`a7d74ed6`)
- Add high-resolution CC option to midi (`bd69ffb4`)
- Getting rid of second argument (`23a4bf66`)
- solo and sync configuration (`b913e1c5`)
- add basic spectrum function (`648fbf99`)

## Октябрь 2024

### ✨ Новые возможности

- markcss (`1e6e0a61`)
- mobile responsiveness (`b0d2115f`)
- menu pinning (`96802a27`)

### 🐛 Исправления

- lint (`7d8984b7`)
- format + ignore samples (`a46bbeaa`)
- lint (`8703d684`)
- lint (`61f07074`)
- vertical breakpoints on mobile (`bb4b7dc5`)
- tiny vertical menu (`0d0cb3fd`)

### 📦 Прочее

- Preserve tactus for 'degrade' and friends, and tidy up 'pick' and friends (#1205) (`9ecad810`)
- Add s_zip for 'cat'-ing patterns together step-by-step (#1208) (`ae335ac4`)
- edit run locally instructions in README.md (`08c918f1`)
- - move console logic to console tab - colorize logs when color is set (`1a2847d9`)
- use fontFamily + fontSize from settings in console (`801879bd`)
- - remove header icons (except play/stop button) - use unicode spiral instead of emoji spiral - fix header height on mobile (`2a676986`)
- cleanup (`75f7f4de`)
- more cleanup (`614f393a`)
- add doc (`aeaf4471`)
- snapshot + add markcss in test runtime (`53b8bbb7`)
- use color inherit in monochrome themes (to be able to set color in markcss) (`f61de0c4`)
- gitignore (`2ec20c4e`)
- remove astro (`94e8b18f`)
- Delete website/.astro/astro/content.d.ts (`6057e9ad`)
- Delete website/.astro/types.d.ts (`8ac98262`)
- prettier (`630ba62c`)
- add 2 new settings - disable css animations - hide buttons - add docs link to left (to use when buttons are hidden) (`b6b54a85`)
- hide docs link when embedded (`be4c8cbd`)
- ignore .astro folder + delete files from git (`1e0cc2ab`)
- alt+dot breakage (`d9bd6f9e`)
- panel options (`df2d28f0`)
- fix horizontal button (`c4fe2c74`)
- failing to upgrade eslint (`67964c7e`)
- update lockfile version from 6 to 9 (`c6cc487f`)
- update minor versions (`0c1234a3`)
- update vitest from 1 to 2 (`9e5f9d37`)
- panel position height dependent (`dd171f83`)
- remove spillover (`2e6a5611`)
- remove unessecary hover logic (`8440fa42`)
- better width calc (`c191471e`)
- welcome width (`c22c786c`)
- Get a basic version of binary working (`33fef379`)
- Put msb on the right (`0d660277`)
- Make example more concise (`0a408e46`)
- Add tests (`947b263b`)
- Rework binary to binaryN (`34f4afad`)
- Rename variable (`4cc019f0`)
- Support those who prefer no left padding (`f87f2791`)

## Сентябрь 2024

### 📦 Прочее

- Fixed access to merged description in factories.mdx (`e9e7e025`)
- Reverting code (`bd3f2845`)
- format code for tests (`3a9349d9`)
- Screenreader improvements (#1158) (`2d320c45`)
- Combine examples for seq and remove extra entry (`717e4a2c`)
- Combine examples for stack, match wording for cat, stack and seq (`2c16a37a`)
- Simplify search bar code (`ff9f4a8a`)
- Combine examples for cat (`c225b471`)
- Fix serial timing (#1188) (`b75561e3`)
- remove redundant example for cat, update snapshot (`25f762c3`)
- Add bite function (#1187) (`abaab890`)
- Adding search bar in soundtab.jsx (`6cb61ade`)
- revert lockfile (`532f1435`)
- Correct spelling mistakes (#1183) (`9714d88d`)
- Add a search bar to the REPL Reference tab (#1165) (`bfb22228`)
- add seqPLoop, ref #31 (#1182) (`bdddb915`)
- create meta params (`07bcaa65`)
- updated (`ebc0b293`)
- fixed formatting (`da45ce68`)
- fix (`aba633e1`)
- update test snapshot (`202685e8`)
- Fixes fit so it works after a chop or slice (#1171) (`263c13c3`)
- spelling (`c4243f41`)

## Август 2024

### 📦 Прочее

- add docs for OSC target (`11a07df1`)
- spelling (`ff8ec628`)
- prettier (`ddceb366`)
- polyJoin (#1168) (`39006e4d`)
- Add scramble and shuffle (#1167) (`8b6c8eb3`)
- snapshot (`812ed6b6`)
- cleaning up (`4eae366b`)
- prettier (`2c9ea03f`)
- prettier (`d3733d42`)
- cleanup (`dcda9a57`)
- testing (`9c899d53`)
- working (`73ee84a1`)
- cleaning up (`117f7fa4`)
- reset collator after n seconds (`8da1d9fb`)
- use performance clock for osc mode (`796f741a`)
- prettier (`b60beaf5`)
- check iframe (`926c893c`)
- prettier (`bfead582`)
- testing: (`2899ee5a`)
- reorganized repl (`5e6131af`)
- prettier (`25d61a2e`)
- fix confirm dialog (`d80c06cd`)
- fixed dialog (`e9a0a06b`)
- update select dialog (`cba9169b`)
- remove desktop restriction (`ac0dc7a8`)
- fixed osc clock (`c2be2ed7`)
- fixed prettier (`3cd31e48`)
- offset calc (`94ccfa63`)
- clock fail strategy (`7d2110d7`)
- rolling clock average (`28334ec9`)
- cleaning up (`814babbf`)
- fix test (`1b01f41f`)
- fix (`c7c56a13`)
- pickRestart example (`d40e4dac`)

## Июль 2024

### 📦 Прочее

- Fix loop at tactus (#1145) (`6e496b7a`)
- Merge remote-tracking branch 'origin/main' into bettern-zen-spacing (`dffeac41`)
- remove log (`03bbc0bc`)
- handle midin device not found error (`5ac59e95`)
- better spacing in zen mode (`943d78f2`)
- Fix tactus marking in mininotation (#1144) (`4c345743`)
- prettier (`94c07b6b`)
- push code to window on aftereval (`42000a76`)

## Июнь 2024

### 🐛 Исправления

- editPattern bug (`3dc5fd0e`)
- mini repl punchcard + claviature flags (`bf908676`)
- step based naming (`e6b55bae`)

### 📦 Прочее

- export comment commands (`d53929cf`)
- Chop chop (#1078) (`a4bd0ae1`)
- Fix bug in Fraction.lcm (#1133) (`283a071c`)
- cleanup (`b5fbcd10`)
- added udels editor and header (`6c9e0aaa`)
- need to fix window message (`2a2ddf20`)
- fixed build window issue: (`1d95da7a`)
- prettier (`f3b83b41`)
- fixed window: (`5e9da78e`)
- fixed offset time (`9f958abb`)
- unessecary import (`11102adb`)
- fix pnpm lock (`f3fa1483`)
- prettier (`248f67d1`)
- Update base path build setting for core and repl packages (`b72e71b0`)
- revert (`e62d5759`)
- copied files (`e6f3b1f1`)
- Configure base url for @strudel/web package (`758b9477`)
- working (`065b24a2`)
- use labels instead of stacks (`771eb31d`)
- don't use labeled statements in example for now (`666c695c`)
- remove stack from workshop (`269d6dde`)
- no custom line height (`300022d8`)
- don't clear hydra in minirepl for now (`c05bbf34`)
- add beforeStart callback (`94e411aa`)
- implement onPaint with pattern state (`faba4a4e`)
- painters are now handled by Drawer (`bf391ad6`)
- support onload inline viz with autodraw (`aadb2fee`)
- add autodraw flag + scope (`a7650f1a`)
- small example tweaks (`4a87df64`)
- clear hydra in mini repl (`d0510f86`)
- use inline scope in examples (`b6e277f1`)
- snapshot (`6adf1ff9`)
- snapshot (`25981092`)
- support aac (`8c73ad08`)
- visual functions (`1acb675f`)
- snapshot (`357fbf8c`)
- make sure draw logic works with multiple repls (`375c6877`)
- start visual feedback article (`0e6a17fc`)
- faster upload (`1fca6f25`)
- convert to blob (`1fd9ba5d`)
- fixed indexdb (`bf7fe2bf`)
- add catch (`321a8889`)
- remove unessecary change (`6b2d080a`)
- remove console statement (`ffa21ddf`)

## Май 2024

### 🐛 Исправления

- use full repl in web package (`3cd31b22`)
- lint (`bf5e66d6`)
- add node to eslint (`8b1df7ce`)
- links (`97568300`)
- add back hs2js as workspace dep (`373b63c7`)
- lint (`aa2466b2`)
- import (`dcd828a1`)
- dep (`a8dce199`)
- lint + format (`5b89500a`)
- lint (`226cf356`)
- lint (`29598d17`)
- missing events due to premature worklet cleanup (`956ceb8a`)
- csound + dough timing (`8db443ba`)
- url parsing with extra params (`5b4322c9`)

### 🔧 Обслуживание

- simplify publish docs (`827993a8`)

### 📦 Прочее

- fix example (`08f08255`)
- bring back promise cache just in case loadWorklets is called more than once (`07f58c02`)
- simplify initAudio (`5c9a16bc`)
- immediately remove click listener (`c20e8477`)
- tweaks + logs (`7a490286`)
- Merge remote-tracking branch 'origin/main' into migrate-tutorial-fanchor (`33322be9`)
- also add versioning in mini repl (`05289b17`)
- versioning (`1eef2c25`)
- wording (`1a72b9e5`)
- update some examples (`94f58864`)
- snapshots (`a17351e1`)
- debugging (`e29876e7`)
- rename tidal-repl example (`2febbaf4`)
- Publish (`2aa1acfd`)
- add strudelMirror to scope (`49287875`)
- add a few helpers (`d424b118`)
- format (`e408feda`)
- fixed (`8be5e9d2`)
- remove unessecary export (`c0c54c21`)
- updated to return promise on first click (`af3d32a4`)
- remove unessecary functions (`ff85f230`)
- updated lock file (`61d795af`)
- Added alias and jsdoc (`29013694`)
- Should fix the tests (`be6634f9`)
- Add mousex and mousey the Docs (`0efac42d`)
- lockfile (`553045e6`)
- Fix drawPianoroll import in codemirror example (`3cacafff`)
- use dec instead of ds (`35e9385f`)
- add simple default value versioning (`2bae6e06`)
- added condition to pass the tests (`8f1496ce`)
- rollback phaser (`1ad118d8`)
- remove imports (`12cb5b09`)
- Add the mousex and mousey signal (`aec7417c`)
- Runned Prettier (`7efbee06`)
- remove unessecary comment (`98cb128f`)
- spare some cycles (`b19c737c`)
- bump sampler (`7952dfa5`)
- works great (`9bb1039b`)
- update resonance (`c797fea1`)
- revert default res (`94529549`)
- fix default res (`e60e9b2f`)
- fix sampler on windows (`2d3a9b80`)
- probably fixes sampler on windows (`892a0abe`)
- bump (`349f902d`)
- bump (`1cebfd18`)
- Merge remote-tracking branch 'origin/main' into ladder_filter (`888e0d47`)
- bump (`8f86bbc6`)
- remove old comment (`c4b3d484`)
- simplify fn (`8ac409f0`)
- add aif to list of supported audio file endings (`5e4eefa5`)
- bump (`574c5285`)
- remove aif support as it doesn't work (`e483f4c3`)
- remove hs2js dep from website package (`fe1e1029`)
- try static dep (`1bc1346f`)
- reset?! (`ef331f25`)
- total confusion (`1761fa03`)
- filter type can take in number also (`84a8ca9d`)
- organize code (`255c283a`)
- update comment (`be65a109`)
- add example (`1952e4d4`)
- change fanchor to 0 (`b8085e9d`)
- fix test (`17e2ee4a`)
- add defaults and fanchor docs (`eed4a83b`)
- fill in remaining defaults git push (`a214b1b2`)
- tidal highlighting (single line only for now) (`a94ddc79`)
- fix all the problems (`b0edf74a`)
- documentation (`a8eafbc3`)
- documentation (`9b52807c`)
- fix import (`e87055d5`)
- fix little dub tune example (`d0ecf15f`)
- fix tests (`bce24980`)
- working (`d49c9dce`)
- hs2js test (`ed506cab`)
- rename "samples" to "bank" (`69a5d620`)
- move hs2js package here + improve readme (`c232fb7d`)
- fix lint errors + cleanup (`0123f4e5`)
- replace postinstall with prepare for dev only (`bbeb243c`)
- remove prepare script for now (`8c480807`)
- bump version (`f54f80e6`)
- hs2js api docs (`70e938b7`)
- attribution (`846c1280`)
- add npmignore (`a9fa6f14`)
- remove graphviz (`5f8da04c`)
- rename (`d108de40`)
- tidy tidal package - move tidal repl to examples - make hs2js synchronous (`fd950d44`)
- add tidal package to website (`a35e9aef`)
- update readme after making it sync (`b4f1cedc`)
- bump (`d132344d`)
- update examples after making hs2js sync (`71f8c074`)
- allow using tidal function as template literal + add d1...d9 (`2499c405`)
- transpiler (for future reference) (`3b4ae44f`)
- cleanup / allow ops to target node types (`5fb3aada`)
- integrated with envelopes (`4160639d`)
- stereo (`e6f272dd`)
- begin "understanding voicings" (`95ae5191`)
- typo (`83e46037`)
- refactor sampler: - delegate getSampleBufferSource logic to getSampleInfo + getSampleBuffer - move buffer logic up from onTriggerSample to those functions (`73d49f15`)
- rebuild parser to remove logs (`4ade86f7`)
- fix lockfile: reset to main one (`179931be`)
- pnpm i (`733d6ec6`)
- lint fix (`2aac085b`)
- move haskell-tree-sitter-playground (`09e455e1`)
- rename haskell -> tidal (`eba7dec1`)
- working (`803b5e56`)
- sort samples in tabs (`16ad2740`)
- OSC/SuperDirt fixes (#1093) (`e268802f`)
- kinda fixed it, need to find cause of incorrect cps calculation (`19df1937`)
- almost figured it out (`e024ae21`)
- fixed non synced clock (`c6c5a303`)
- fix test (`4ba9d47b`)
- fix tests (`d5ab6b72`)
- updated non synced clock to work more like synced clock (`4a181d5b`)
- remove unesseccary time call (`8ad48e16`)
- simplify targettime (`bee8c812`)
- set document.title from @title (#1090) (`a69563cb`)
- fixed duplicate variable (`cba96049`)
- fixed tri shape (`6c05a4eb`)
- formatting (`6a33032d`)
- codeformat (`4e6d3966`)
- fix tests (`33a73c37`)
- fix cyclist clock cycle (`16472b59`)
- bug fix (`a4f10d53`)
- codeformat (`8d8b8ab9`)
- use sessionStorage instead of localStorage (`7f22040a`)
- fix events, add comment (`b69b9c37`)
- implement sessionAtom function (same api as persistenAtom, just used sessionStorage instead of localStorage) (`6249631a`)
- simplify implementation (`6e6a2439`)
- cleanup (`5dc2ecd7`)
- bug fix (`02ea4d11`)
- simplify more (`33ebf08e`)
- - fix scale() to allow both n() and note() at the same time - fix scale() to allow scale names without tonic and then default it to C (`a5b60225`)
- sine test (`0d900c01`)
- working (`3159f665`)
- memory leaking (`7df663e8`)
- Benchmarks (#1079) (`e47d67a9`)
- testing (`496d60dc`)
- implementing (`66a27a60`)
- Improve performance of ! (replicate) (#1084) (`34e9d739`)

## Апрель 2024

### 🐛 Исправления

- don't reset on each eval (`0190604a`)
- do not reset cc input values on each eval (`81b33afc`)
- cyclist desync (`df23ea6c`)
- use foreground color for pianoroll playhead (`6ff7697c`)
- import (`a4897adb`)
- call setTime in initStrudel + return promise with scheduler (`d06b70c2`)
- reify hydra pattern (`610c848f`)
- example encoding for testing builds locally without server (`915a4d21`)
- proper error log with call stack (`010329dc`)
- ignore haps that error at getFrequency (`c7f811e1`)

### 📚 Документация

- custom samples - strudel.json - github shortcut - "import sounds" - @strudel/sampler - improve structure - add drumset (`971d497e`)

### 📦 Прочее

- Add calculateTactus() for turning off calculation of tactus in (#1081) (`91bb0a1d`)
- fix docs on alignment.mdx (`d253e35b`)
- fix failing format test (`ce79a448`)
- ran prettier again? (`15f16006`)
- fix translation issue in first-effects.mdx (`0be5f772`)
- add signals to recap in first-effects.mdx (`5c4ad573`)
- clarify `off` in pattern-effects.mdx (`6b919132`)
- add nesting to `off` example variation in pattern-effects.mdx (`975eb684`)
- Use "Alternate" for `<...>`, consistent with recap page in first-sounds.mdx (`69f8498d`)
- More tactus tidying (#1071) (`bbf8577f`)
- fix cr typo on first-sounds.mdx (`5c01760b`)
- fix first sounds typo (`54829bd2`)
- add `<...>` to recap (`d8eb186e`)
- Tactus tweaks - fixes for maintaining tactus and highlight locations (#1065) (`267f58b7`)
- clarify license (#1064) (`6a1fa7e6`)
- Fix stepjoin (#1067) (`a189626e`)
- Fix wchooseCycles not picking the whole pattern (#1061) (`a1941801`)
- Stepwise functions from Tidal (#1060) (`0a3694fb`)
- fixed (`5d8eea72`)
- fixed osc server (`57ad2781`)
- anonymous patterns + muting with _ (`93d151b9`)
- restored (`5ca9afa9`)
- bump @strudel/sampler (`8494353a`)
- tutorial tweaks: - add drumset image - introduce angle brackets earlier (`a3bd0473`)
- bring back division (`83eedcb2`)
- update german tutorial (`559f3179`)
- slightly reword (`ca36a31f`)
- More experimental step functions: stepwax, stepwane, steptaper, steptour and taperlist (#1042) (`ebeefc0a`)
- support alls combinations of numbers and strings for notes and intervals (`3d065283`)
- mfix (`8e4549d6`)
- fix desktop midi (`183d3ea4`)
- remove unessecary changes (`cbfa09a2`)
- adjust latency (`0a6373f6`)
- add superdough warning when scheduling in the past (`bea25a28`)
- add fallback theme (`18abcacc`)
- cooler example (`ca8707f3`)
- bg (`0ba4be05`)
- add swing / swingBy to time modifiers page (`97d292ad`)
- dont ignore hydra.mjs in eslint + fix lint (`fde0da3b`)
- add more info on using strings for patterns in web package (`3a4ffb13`)
- add swing + swingBy (`8f06bc4b`)
- snap (`16851cf8`)
- add filter + filterWhen + within (`cdba9efb`)
- make soundfont base url configurable (`e3b6884b`)
- basic pitchwheel vis (`8a3e9171`)
- more settings (`53916063`)
- (squash me) save setting in variable, not store (`b233c33f`)
- tweaks (`6479c603`)
- ireal voicings add aug alias for + (`e242e82b`)

## Март 2024

### 🐛 Исправления

- lint (`abe775c3`)
- lint (`422a9ad5`)
- lint (`b6171600`)
- lint (`f9876334`)
- char encoding (`7d8cb59a`)
- cyclist.now() should return 0 when stopped (`14b1a120`)
- allow multiple visuals for the same pattern + using .tag function instead of single .id (`b5720355`)
- id widgets by type index to prevent erasing memory in some cases (`6a2486d9`)
- lint (`a8c2586d`)
- id at wrong place (`a629c5c9`)
- await injectPatternMethods (`16210e12`)
- clock jitter in cyclist (`ba8f9961`)
- center supersaw if only 1 voice (`5982e3eb`)
- enable locations again (`a71a1671`)
- bugs catched by linter (`ef284e9d`)
- animation frame cleanup (`dd78dc96`)
- claviature height + better defaults + break out solid web component creation (`dfd22b5e`)
- catch errors in pianoroll getValue (`1043baf0`)
- share now shares what's visible instead of active (`7eb2f765`)
- clear hydra on reset (`012b19c7`)
- hydra canvas adjust to screen size + simplify hydra init (use getDrawContext) + make getDrawContext more versatile (`d5ed0650`)
- migrate more imports (`4f94de86`)

### 📦 Прочее

- add simple local sample serving (`39204ad9`)
- clean lockfile (`bd9a8082`)
- proper sample server (`95e0496d`)
- add sampler script + samples folder with README (`8f355dcf`)
- run sampler in parallel by default + move samples folder to root (`22774596`)
- make sample server available over local network (`29fe4fd9`)
- improve log (`3a60f63f`)
- improve logging + skip folders that throw (`8a8f6a66`)
- bump (`e39cd2c9`)
- add LOG env option (`3e66e1ee`)
- options (`1a209c8b`)
- bump (`934a3cde`)
- better warning (`1aca5db4`)
- bump version (`30a15cb4`)
- remove starting sampler by default (`ff923a6b`)
- simplify readme (`fd08b253`)
- Tactus tidy (#1027) (`9c66d9f1`)
- remove dangerous arithmetic feature (`12d8cdff`)
- add closeBrackets setting (`4e7f5e9c`)
- add font file types to offline cache (`1af55c0d`)
- add some hap methods for time introspection (`1a0dff40`)
- simplify draw plumbing (`04a0952a`)
- another hap time introspection method (`060d2cfe`)
- add memory to onFrame (`eb8ac291`)
- remove old .draw, rename .onFrame to .draw + breaking: from, to are now called lookbehind, lookahead + migrate .pianoroll (`ab015ff4`)
- visuals now adhere to theme by default (`c400deac`)
- add 3 new fonts (`6c07feb3`)
- clean (`cc49e0f7`)
- add setting for sync flag (`12655663`)
- disable sync by default? (`50ed4668`)
- tactus marking (#1021) (`2fd2bdba`)
- working (`bbfdd2cc`)
- Document signals (`69211b87`)
- Document conditional modifiers (`064bff6e`)
- Fix codestyle (`fc4c70c9`)
- Add examples test snapshots (`af35a1af`)
- Add random modifiers docs to its own page (`e6eb6c19`)
- Fix undegrade doc naming (`731d545a`)
- Fix snapshot undegrade function name (`8b96e3a6`)
- Fewer undocumented functions (`aba4e72d`)
- move color to hap value (`d0366359`)
- snapshots (`13d56b6b`)
- rename trig -> reset, trigzero -> restart (`c2ef90d6`)
- add Pattern.id + use it for _punchcard and _spiral (`2857e816`)
- improve spiral colors (`0eccbae0`)
- fix _pianoroll (`21f5d639`)
- make spiral size param 3 in one (`49e381f2`)
- Merge remote-tracking branch 'origin/main' into inline-punchcard (`ac36423f`)
- update undocumented script (`fc329818`)
- still kinda working (`aa36d25b`)
- sync mostly working (`619ca538`)
- sync false (`bc917dcf`)
- working (`f00a08ef`)
- build working properly (`bd16b403`)
- removed unessecary variable (`86c94511`)
- cleanup (`8e2407cf`)
- working poc cyclist with cps change (`8d8c843c`)
- delete now obsolete poc (`78890174`)
- make regular cyclist work in the background + use worker-timers in Repl + repl / cyclist now accept custom interval functions (`14a5e7dc`)
- + make cyclist trigger backwards compatible for now + add absolute time as new 5th onTrigger param (`e8f6124a`)
- document umbrella packages (`8d96a071`)
- begin project-start doc + improve embed + repl package doc (`6365e162`)
- add link to StrudelMirror (`52a311aa`)
- finish project start page + link it in main readme (`df03e9a3`)
- a few more words (`4f0f388c`)
- remove demo + scripts on all doc pages (`52d3ee0f`)
- remove embed and repl deps from website (`d299e76c`)
- delete redundant emu samples (`456250a1`)
- use script tag instead of skypack (`be2ec969`)
- delete astro favicon (`d2fc7840`)
- outsource piano samples (`f5c087fc`)
- remove canvas dependency (`45117322`)
- scheduling in a worker, draft (`0565558e`)
- halve detune range (`1d48d5d5`)
- disable sync (`5d0abe41`)
- allow absolute time for superdough with prefix = (`10bd6680`)
- more options for zyklus api: + allow setting custom interval functions + allow disabling phase rounding (`dc977b29`)
- export createClock from core (`4362cb93`)
- add working poc for precise cyclist (`ac441b43`)
- add readme (`837dfcab`)
- add run instruction (`41a40c74`)
- add missing dep (`96a97799`)
- move rollup plugin to dev dependencies (`d3a17891`)
- remove unecessary multiplier (`cbe7e69e`)
- make onPaint work with mutiple repls on screen + add warning if stock onPaint is not overloaded (`6b98e414`)
- add inline _punchcard (`1e1dbd60`)
- accidents in scale grades (`e32c965a`)
- Fix pure mini highlight (#994) (`29cb6195`)
- improve scope memory footprint (`16506c5a`)
- make labeled statements look good on custom themes (`d1c713fa`)
- transpile label statements to p calls (`6359bbe1`)
- add comment (`2de18846`)
- move canvas functions to codemirror package + fix id collisions (`076b6f1c`)
- rename inline functions to match global ones, prefixed with _ (`be77882d`)
- comment out _spiral for now (`4319c43c`)
- move stuff for less changes (`b5312c27`)
- Merge remote-tracking branch 'origin/main' into claviature (`2900401d`)
- disable sync, it somehow breaks playback in the built version (pnpm build && pnpm preview) (`dd16a1cb`)
- Merge remote-tracking branch 'origin/main' into claviature (`8b9a2e1d`)
- disconnect worklet  at scheduled time to destroy faster (`dad18a24`)
- fixed end time (`6d874af4`)
- breakout widgets package for now (`95446a76`)
- remove unessecary math (`48e40513`)
- inline multichannel scopes (`f4032dad`)
- draw straight line when no analyser is defined yet + add todo for memory leak (`cdc200e1`)
- Beat-oriented functionality (#976) (`39853387`)
- allow any web component to become a widget (`82c4926f`)
- rename claviature package to widgets (`70867507`)
- use hap color for claviature (`094b30cd`)
- add pianoroll widget (`48e0691e`)
- encapsulate canvas logic (`83ea3bb1`)
- remove this mess (`a8712fd8`)
- support multiple animationFrames + break out spiral draw logic (`6dce9d5d`)
- simplify widget creation + fix bugs (`8742d50b`)
- move widget registry to codemirror package + add transpiler as dependency to codemirror (`4a95bf67`)
- full size piano by default (`d9d05e21`)
- remove hyperpop test (`5d55b9c9`)
- remove console statement (`83145784`)
- Merge remote-tracking branch 'origin/main' into claviature (`062e45ee`)
- claviature lockfile (`0a1da6f6`)
- add working claviature method (`f2e16f94`)
- add readme (`cfdd4ac3`)
- remove other watch tasks for now (`1e75f045`)
- can now load claviature as a codemirror widget (`29dab578`)
- use lerp (`fd946107`)
- fix tests (`1f39e1c0`)
- merging main (`7f9cc7cd`)
- fix test again (`8f291b0f`)
- Merge remote-tracking branch 'origin/main' into clear-hydra (`4a25eab0`)
- Merge remote-tracking branch 'origin/main' into velocity-in-value (`ae780764`)
- fix update migration snippet (`708fea63`)
- hydra-canvas might already be removed (`d0c68e91`)
- Update synths.mdx (`bc641d8e`)
- stop clock if all stopped (`112ca187`)
- add claviature package (`85fee0cc`)
- global watch task (`9b3509c0`)
- no need for publishConfig (`43c40f7b`)
- remove changes to prevent conflicts with draw branch (`1af2da00`)
- rename zyklus (`619ffdd5`)
- add sync flag for neocyclist (`2fcbffea`)
- add note to future (`8dba865c`)
- allow offsetting onFrame (`75098adb`)
- canvas -> draw (`0f07bfc9`)
- fix android support (`30a51760`)
- fixed (`f1700d5e`)
- alias - for ~ (#981) (`8e41332d`)
- gain adjustment (`3410c332`)
- unity gain (`6a5bf521`)
- faster is odd (`4d1110b3`)
- comments (`9d1231ce`)
- implemented modulators (`c0b00339`)
- disable lint for global  worklet variables (`232b9c13`)
- added back (possibly unessecary? stop fm (`05887967`)
- fix test (`8db8307c`)
- little fix for withVal (`9f56a335`)
- cleaning (`c2df8aa6`)
- 100 (`1e3a23e0`)
- adding modulators (`bfcb2070`)
- working (`7383dca9`)
- yay (`7f60a9e5`)
- stereo (`78a6ec2f`)
- working on it (`ee01b4a3`)
- cleaning (`823c208c`)
- fixed output (`e839c271`)
- params (`1666b508`)
- fixed performance issue (`4af7262f`)
- working (`8a7d72bf`)
- nested controls poc (`4f551442`)
- simplify _composeOp (`bf343eb4`)
- onFrame function (`62d8955d`)
- make velocity a regular value prop (`1d92ee76`)
- delete file from wrong branch (`5017ac6f`)
- fix tunes + tests (`e9e5fac6`)
- trying things (`f911fceb`)
- default values needed in superdough for worklet input (`daa62202`)

## Февраль 2024

### ✨ Новые возможности

- can now invert euclid pulses with negative numbers (`d4724b0e`)

### 🐛 Исправления

- end / begin. sampler now needs clip to choose duration... (`96eef75f`)
- add name tags (`c524ebc7`)
- reset global fx on pattern change (`b9247db0`)
- test (`0baa8383`)

### ♻️ Рефакторинг

- duration is now a regular control (`eb93a6c1`)
- controls are now exported directly (`8efadd25`)
- remove now redundant controls imports (`664a19a5`)
- remove await before samples calls in docs etc.. + remove /master from dirt-samples calls (there is now a main branch) + remove /main endings from some places (`bf63b716`)

### 📦 Прочее

- fix for transpose(): preserve hap value object structure (`b870ab20`)
- snapshot (`7556da78`)
- add debounce to logger (`fcb247b6`)
- set ireal as default dict + add global way to set and reset default dict (`d4eebf1a`)
- repl reset default dict + add mechanism to get a module by name from dynamic imports (`30571527`)
- snapshot (`8f39a5ee`)
- playing (`c00ed14b`)
- rename pick*Outer > pickOut + add pick*Squeeze alias to inhabit* (`0deb905c`)
- testing (`f5c7b58b`)
- canvas package boilerplate (`94dd547a`)
- move draw logic to separate package (`6ce8a3fe`)
- playing (`f62b268f`)
- updated params (`416a03ae`)
- fix test (`c9b18f6f`)
- envelope (`1690030b`)
- working (`a6f7c57d`)
- fix examples (`abe25be0`)
- cleaning up (`972bfa33`)
- adjusting params (`a227c694`)
- remove legacy legato + make legato a synonym of clip (`5c71bb95`)
- remove old snapshots (`f779e819`)
- adjust param range (`c77b0098`)
- added distortion effect (`1221c614`)
- cleanup (`a3898c75`)
- merged new controls file (`e3463b3c`)
- updated controls (`da674d3e`)
- update test (`fba2422a`)
- working (`1416a0de`)
- cleaning_up (`360b9209`)
- mux operator, muxers.mjs with pick variants (`4aaf9eae`)
- Merge remote-tracking branch 'upstream/main' into pickr-pickrmod (`ed1f3e1e`)
- set bracket matching true by default (`22e3067b`)
- revert unintended change (`b0b41e67`)
- format (`75579521`)
- added pickOuter, pickmodOuter (`0986b145`)
- typo (`dbfdbcf2`)
- Merge remote-tracking branch 'upstream/main' into pickr-pickrmod (`74ef157f`)
- add iife build to web package (`e8b674ba`)
- bump web package version (`5419ca43`)
- add web package example using script tag (`a89e87ca`)
- try different main / module setting + bump (`2854f0cf`)
- simplify web example (`c23d7850`)
- adapt repl package similar to web package (`6d9327ae`)
- update example (`058e554b`)
- lower weighted average (`e54b2fb2`)
- seperated service worker zyklus and cyclist zyklus because of import constraints on service workers (`d544bf46`)
- 'Enable Bracket Matching' option in Codemirror (`f54aa3d8`)
- added <> for mininotation (`8af40a9c`)
- automatically add await to samples call (`ebdca32c`)
- add test (`38d389b8`)
- some samples were broken (`4e3d4a70`)
- restore old cyclist (`e5570a60`)
- fixed (`c06a3710`)
- fix test (`751a8256`)
- Update signal.mjs (`12c6f4e9`)
- Update signal.mjs (`23c55c96`)
- Update signal.mjs (`03a73fb9`)
- Update signal.mjs (`88ece92a`)
- Update signal.mjs (`33478173`)
- Update signal.mjs (`00c7da45`)
- Update signal.mjs (`d5e67fe1`)
- importScripts (`b37c560e`)
- cleanup (`15df20d2`)
- remove cjs builds + add type module everywhere (`60129413`)
- make sure to overwrite main field (`5dc04d95`)
- Publish (`051bdacc`)
- added worker, cleaned up, added setcps function (`90624abf`)
- cleaning up (`f5ac57f8`)
- cleaning up (`fc87a677`)
- fixed import (`cc323d0d`)
- fixing test (`a8a055d3`)
- try to fix test again: (`c761cd54`)
- add a broadcast channel for recieving messages from clock to ensure that they hit at the same time (`0d3eaf7f`)
- remove console statement (`cea33783`)
- Update README.md (`d2bf9370`)
- fix repl merge (`5a3272fe`)

## Январь 2024

### 🐛 Исправления

- pianoroll sorting (`434eb3b2`)
- "can't redefine non-configurable property" (`18ae8217`)
- localhost links (`229b1470`)
- heading (`95cf201c`)
- do not format autogenerated file (`7ceb8708`)
- synth default envelope (`eefa90a2`)
- fanchor + default fanchor to 0 (breaking change) (`f99557cc`)
- use end (`c68dba8c`)
- autocomplete / tooltip code example bug (`7157634d`)
- invisible selection on vim + emacs mode (`bcee6632`)
- format (`d582bbbb`)
- shuffle by default (`88fbd784`)

### 📦 Прочее

- Update first-sounds.mdx (`fb7c2b61`)
- Update README.md (`c66b9219`)
- Update README.md (`8fef7538`)
- fix midi issue on firefox and quote error (`6da98a5f`)
- make 0.5hz cps the default (#931) (`6422047c`)
- offset blog post anchor links (`23c0ab96`)
- finish year 2 post (`6fa66fb1`)
- youtube embed styles were lost on prod (`b184348f`)
- examples were still 1 cps based (`02efc090`)
- Publish (`1829d8ca`)
- v1 release notes (`a36cc240`)
- shorter titles for release notes (`3da65f90`)
- implement fit with pat state (`8052e10f`)
- implement loopAt with pat state (`0a824711`)
- inject scheduler into Pattern methods right before eval (`aa1b9d11`)
- mini repl max height option (`6224cd40`)
- show repls in community bakery (`0cfec3a7`)
- clean + comment (`0695b4be`)
- fix oven imports (`d31d1548`)
- title order (`b1d49552`)
- autoplay and reset cps on default (`961e931c`)
- reset/autoplay when window PARENT does not exist or does not contain oodles (`b46e8c07`)
- default pattern filtername (`15c451a6`)
- fixed if statement (`192ba696`)
- remove console statement (`c0885902`)
- fix invalid date (`b45fd5ce`)
- add pattern filter behaves like sounds tab (`9dca7eb8`)
- cleaning up (`3cd59cde`)
- cleanup (`4fe75143`)
- fix scroll (`6c28398d`)
- Make splice cps-aware (#932) (`738cea00`)
- fix annoying div (`1146f6ba`)
- cleaning up (`9a13c585`)
- cleaning up (`3431eaf3`)
- fixed imports (`7aea1b5f`)
- prettier (`af52ac30`)
- add pickF and pickmodF (`9ffbc06c`)
- begin 2 year post (`d49aa824`)
- Mini-notation additions towards tidal compatibility (#926) (`491c99c3`)
- write more (`c598fa02`)
- code format (`68113937`)
- update the .snap file (`2f8111be`)
- more writing (`710c3acb`)
- add stats (`d176e9cc`)
- avoid json errors in firefox (`0a349e17`)
- - more flexible pitch envelopes - fix: exponential envelopes could break due to 0 values (`d3ea4959`)
- pitch envelope now also works when setting one of the controls (penv not needed) (`1c99944a`)
- document pitch envelope (`67f0758c`)
- snapshot (`4107711d`)
- Publish (`b10612da`)
- the big rename: @strudel.cycles/* -> @strudel/* (`96bafa7f`)
- Publish (`b04a2624`)
- fix link (`e6b47d2c`)
- update versions in example html files (`a45f1fba`)
- add rss.xml (`ae5e6e3a`)
- blog style fixes (`2ed5f5aa`)
- add rss link to top + fix typo (`5ada0daf`)
- minirepl multi tune support + add other getting-started examples (more to come) (`f85fb35c`)
- format (`0778b480`)
- `pick` now accepts lookup tables, with alternate cycle squeezing behaviour as new `inhabit` function (#918) (`873a4121`)
- pick, pickmod, inhabit, inhabitmod (#921) (`98b78596`)
- basic community oven (`4229d673`)
- more text (`c6f3a8b7`)
- rename (`4f7415b0`)
- more rename (`6667738d`)
- rss links (`d8677c62`)
- cleaning up (`6cb156d8`)
- persistent pattern data (`77a48e83`)
- fixing edge cases... (`7adfe6f2`)
- add pcurve (`0655af4d`)
- Use astro Response class for swatch/[name].png.js endpoint (`96c21448`)
- vibrato for soundfonts + add getVibratoOscillator helper (`3506de8d`)
- making things consistent (`45df7bfe`)
- make sure the sustained pitch is the selected note (`fd735717`)
- remove todo (`3fc0fdbe`)
- basic pitch envelope (`bdc2af97`)
- pitch envelope for sampler and soundfonts + added getPitchEnvelope helper (`32456d69`)
- delete browse page for now (`5314c835`)
- fix build error (`f3875893`)
- fix built version loading at runtime.. (`6d32d402`)
- avoid crash for anonymous patterns (`53771d7f`)
- fix codeformat (`70712bda`)
- Link to creative commons chooser (`0d6bdfb5`)
- Update metadata.mdx (`945e7082`)
- soundfont mixing (`e6d028fd`)
- fanchor default to 0.5 for now (`09019485`)
- basic blog with release notes (`0566b7bc`)
- format autogenerated types (`ceb4376e`)
- add video component (`c0297678`)
- add anchor links (`98db3d37`)
- add date + author to posts + dedupe headings (`79f1a863`)
- add more videos to showcase (`4d22d957`)
- negative fenv values now stay in the same range as positives (`6f3b2fa6`)
- i have no clue why this works (`4fb5c182`)
- add 10 new vowel qualities to formant table (`e051d383`)
- document extra vowels in quick reference (`62325602`)
- added some aliases for the vowels (cosmetics) (`efa07376`)
- basic browse page (`3c184404`)
- ask for public sharing (very cheap) + order browse list (`1ede99f7`)
- integrate public patterns into patterns tab (`204c9640`)
- fixed animation drops (`4d929222`)
- cleaning... (`23867822`)
- move non pages out of pages dir (`9a0459fd`)
- cleaning (`ccb42c83`)
- add deprecation notes (`86bc28df`)
- add silent flag to nanFallback (`62cd2263`)
- add fenv to filter cutoff controls (`f785823e`)
- converting (`d329ccc4`)
- use 0.001 as linear mintime as well (to prevent cracks) (`f26fd18c`)
- trying stuff (`f1eaa83a`)
- ( (`0c7b731f`)
- working but animation is weird (`721f707c`)
- playing with workers (`0006f648`)
- workerizing (`19f24346`)
- all working (`51dd4087`)
- prettier (`e01c6dc1`)
- reset for example patterns (`78f6fdf1`)
- playing around (`6eec4277`)
- handle deletion cases (`f7fe4406`)
- refactoring (`1ff7548b`)
- init (`8d325b96`)
- Remove hideHeader for better mobile UI and consistency (`46510664`)
- init (`5e3a37c9`)
- Formatting for passing CI (`569137eb`)
- prettier (`86442adb`)
- highlight viewing pattern (`02b9bd82`)
- mutate viewing pattern (`3604d394`)
- example code logic (`4e0a60db`)
- fixed create and duplicate behavior (`0355c358`)
- fixed all the things (`2ee392be`)
- change default FM env to exp because it modulates frequency and sounds way better :) (`b2f63897`)
- scales can now be anchored (`b27f1771`)
- restore buffer hold behavior (`150a1b8e`)
- add root mode for voicings + allow numbers as anchor (`27049eb7`)
- Zach B-B (`4d25e3b9`)
- working (`1e2c2612`)
- state (`1bc86c35`)
- support , in < > (`ca3eda8f`)
- add ad function (`2ecc6b30`)
- add ar function (`041a809b`)
- add dec synonym for decay (`a2974099`)
- add mastodon link (`eb189d07`)
- add showcase (`d99607ea`)
- comment out chalmers vid (`9a235a7c`)
- prettier again (`83c820a1`)

## Декабрь 2023

### 🐛 Исправления

- build + seems fraction.js now works with ssr (`d47d9dad`)
- duplicate pretest (`b36d334f`)
- for some reason, ci fails without it... (`45eed1b7`)
- editor focus on click below code (`9059b16c`)
- scale errors safety net (`c44f202c`)
- graceful handling of invalid chord symbols (`4121ac91`)
- make sure n is never undefined before nanFallback (`64d80f98`)
- wrong parens.. (`52745086`)
- dumb react ssr workaround (`b0bdd090`)
- lint error (`967dea34`)
- loader (`ea8aabce`)
- tooltip padding (`89bf3b28`)
- drawer performance issue (`a9dc0912`)
- initial highlighting (`f96827d0`)
- lint (`2ed3a5c5`)
- hot reloading (`d7c2cf56`)
- lint errors (`4c337a51`)
- stop other repls on start (`e8e8f888`)
- add .piano function (`0ed615a3`)
- boolean settings (`b462f19e`)
- first frame (`ed8f5bf2`)
- redundant style injections for multiple repls (`48e06bd2`)
- drawTime + canvasHeight (`53d6ef04`)
- first frame active state (`40f3212e`)
- ctrl+enter on linux (`1b04243a`)
- improve last fix by using Prec.highest (`a8d0d390`)
- lint errors + wrong color (`f6b1e131`)
- rollup warning (`2cec7bc1`)
- process is not defined (`505ad174`)
- mini repl play toggle button (`9c13f6bb`)
- repl package import on server (`89c06046`)
- linter error (`f6f5ee03`)
- fontFamily setting (`d23cac50`)
- make canvas visible - automate editor background handling (`29082808`)
- finally repair envelopes (`b0bbd58f`)
- do not sync activePattern between tabs + only deselect activePattern if code in url differs (`faaed613`)
- style for small screen (`79e8e430`)
- deselect user pattern when opening links (`1f16ebc5`)
- update button bug (`69698001`)
- rename cancel (`a6f96961`)
- update / fork logic (`a45d8766`)
- duplicate example (`ac2e450e`)
- astro warnings (`6b31160b`)
- search links with basehref (`2ddd2d1f`)
- missing hash for links starting with / (`1b372a9a`)
- swatch png src (`cc4d38b4`)

### 📦 Прочее

- update eslint (`74b92794`)
- updates.. + codemirror-themes + acorn + escodegen + supabase (`70b9fbfc`)
- update webmidi + codemirror (`59ce6245`)
- update remaining packages (`b52f8c29`)
- bump node version for test (`67f1dc32`)
- use node 20 for deployment too + bump node types (`bd648185`)
- zen mode was broken (`2ae3ce53`)
- use ireal as default voicing dictionary + this has the big benefit that all chord symbols work by default without the need to specify a dict (`b14e5577`)
- rename default dict to legacy (`5a76bc93`)
- show line where mini notation parse errors happen (`ecf83763`)
- revert changing default voicing dict for now... (`9b32900b`)
- tolerate non-breaking space (alt+spacebar on macos) (`1d32da07`)
- suspend and close exisiting audio context (`8f56ef65`)
- prettier (`9756d63c`)
- update button behavior (`9a07b159`)
- another slight logic error (`307c69ed`)
- parser viz (`a39b4c16`)
- some styling (`89ae241b`)
- ignore their wasm file (`eefd8b37`)
- update vite + astro (`6f68bdcf`)
- update dependency-tree + prettier + rerun undocumented script (`1981e1c3`)
- update lerna (`7db684e3`)
- codeformat (`94fcea10`)
- update nanostores (`a747c976`)
- more updates.. (`d0a1374d`)
- updates... (`4f5ca076`)
- update fraction js (`c3ba8ee0`)
- prevent selection while playing to avoid artifacts (`b3522fea`)
- remove console statement (`34f0d631`)
- remove leftover setting (`63066937`)
- better repl init + a bit of ssr for main repl (`99743113`)
- cleanup (`57bc0b70`)
- add missing flash checkbox (`9fd2d3d6`)
- remove nano-repl workspace (`b1d06c4e`)
- remove gist function (`a5af6d5f`)
- delete outdated svg graphics (`f9c1e46c`)
- default panel position right (`92024f68`)
- move all examples to separate examples folder (`0323f75b`)
- add some readme (`5d6ccf56`)
- panel style (`6aebf99e`)
- better names + more readme (`c13feccd`)
- better naming (`49903abe`)
- add readme to examples folder (`b0f316ee`)
- lockfile (`76aaedba`)
- fix superdough example readme (`4871abbd`)
- little update (`64dbdd97`)
- merged with vanilla updates (`69554c07`)
- fix import (`b5039ecc`)
- microrepl claviature support (`837f8f31`)
- whole docs now run new repl - move MicroRepl code to MiniRepl - fix a ssr bug (`975a198e`)
- replace main repl (`201fe726`)
- remove dependencies to @strudel.cycles/react (`014303b0`)
- delete react package (`f477273d`)
- move repl2 to /next (`e2a082f7`)
- load imported samples directly in prebake (`8bde8724`)
- reduce drawTime to fix perf (for now) (`39e81aa7`)
- fix build error (`9b5842b2`)
- share + clean up imports (`63ae9518`)
- style fixes + remove lastShared state (`53484db7`)
- add trailing slash (`1b55a5e3`)
- autocomplete without react (`47aa4cf1`)
- tooltip without react + simplify autocomplete (`f524dde5`)
- remove react-dom dependency (`f44ceb46`)
- remove redundant test pages (`de4460a0`)
- first steps of using StrudelMirror in main repl (`d74d50c4`)
- working update + shuffle buttons + faster loading (`7dac1e27`)
- wire up settings and theming (`e19f7990`)
- checkbox for isPatternHighlightingEnabled (`5c41d678`)
- pull out repl utility functions + repl2 initCode / switching patterns works now (`8c0065f5`)
- still getting too late (`e12421ce`)
- working (`cb57d8f4`)
- working (`37767912`)
- tree-sitter experiment (`9e1ba442`)
- format (`756f37e2`)
- create release audio param method, make volume envelopes consistant (`d7fae262`)
- fixed test complaint (`34ba81a8`)
- trying to fix divergent firefox behavior (`f6d9ad51`)
- fixed release bug (`30e402b9`)
- filter should decay to set frequency (`e0a7fb82`)
- remove unused variable (`2dea3911`)
- fixed hold behavior (`deb973af`)
- still working on popping issue with firefox (`fb81f6f2`)
- fixed filter envelope popping... (`dec039ea`)
- account for phase complete (`5671b407`)
- fixed popping on font envelope (`77fee0b8`)
- initialize on first load (`000e1293`)
- prettier (`17dd48f2`)
- updated comment (`9979a08c`)
- removed accidental file commit (`fe60f340`)
- delete strudelmirror example (`592e54a5`)
- settings sync (`1cdb5964`)
- remove web component attribute support + settings are now controlled via nanostore (`9519d286`)
- ssr static code (`e17df791`)
- system standard resets audio context (`48fa04f1`)
- improve response (`96e0cce2`)
- use StrudelMirror directly in MicroRepl (`fc034830`)
- approaching proper draw logic in microrepl (`7fcd9d8a`)
- add autodraw flag (`c29d0320`)
- updated fontloader (`93a4efcf`)
- format (`b5dc65d5`)
- strudel web component (`d4afbc63`)
- move repl web component to new repl package + use it in /vanilla + /vanilla/mini (`f0c3d38e`)
- repl build config without externals (`46bfd74e`)
- bundle as iife instead of cjs (`cf445633`)
- bump repl to 0.9.1 (`9872a89a`)
- remove onReady callback + use strudelTheme as default (`be1e5a7d`)
- better theme style handling (`034df01e`)
- bump repl to 0.9.2 (`12932707`)
- band aid fix for process is not defined (`9fac2c3c`)
- bump repl to 0.9.3 (`9b58cf98`)
- repl now accepts code in innerHTML + bump (`372e93e8`)
- it works (`7da75544`)
- fileter envelopes (`9663c2ec`)
- more code in example (`8e35079f`)
- MicroRepl poc (`93e81863`)
- cleanup (`b5edcde6`)
- copy recipes page to test new MicroRepl (`20e8a305`)
- fix envelope behavior (`88f677e9`)
- - make codemirror package more capable - add themes from react package - add Autocomplete from react package - handle dynamic extensions - handle keyboard shortcuts - make StrudelMirror as capable as Repl (`87feac44`)
- move code hashing helpers to core (`99194814`)
- dedicated strudelmirror example (`9474ed7d`)
- add settings form to codemirror example (`72d1f129`)
- add /vanilla route to website + disable autocomplete for now (`60e559a9`)
- remove package-lock (`37f97e1c`)
- comment out fontFamily for now (`7b6f63db`)
- get rid of duplication (`49906e98`)
- move css variable injection to themes + prepare new HeadCommon in extra file (`8959c630`)
- add missing trailing slashes (`bc59f20a`)
- cleanup (`f38d1c27`)
- superdough 0.9.12 (`0a070a20`)
- move dirt sample selection to prebake (`b50f686a`)
- improve code init logic (`853a31b6`)
- cleanup (`f939e00a`)
- indexdb (`a7713213`)
- slim down template - move import + export logic to settings.mjs (`f19ac748`)
- db utils should be reusable (`ace71693`)
- seperating things out (`c4fe6642`)
- fixing merge conflicts (`4d361a2b`)
- fixing merge conflicts (`14551026`)
- fixing merge conflicts (`4051276d`)
- updating buton location (`59a4cd9c`)
- deselect user pattern when opening links (`98a1786c`)
- update text (`f45a3933`)
- pattern save was broken :-/ (`845571a7`)
- fixing async stuff (`5281c61f`)
- begin pattern import button (`a750490e`)
- remove log (`b03bf9ec`)
- clear sounds + cps on pattern change + similar to shuffle (`cec8553f`)
- more style + set reset flag (`10d86ff8`)
- add some padding (`8dd0e960`)
- style sounds and patterns tab more consistently (`31cd6268`)
- dont overwrite user pattern on shuffle (`63a01088`)
- cleaning up (`89fe0f11`)
- comments (`ec44103d`)
- make sure new recent patterns are on top (`0ca4c1de`)
- + show active pattern at the top + move active pattern functions to top (`c1b7476f`)
- reorder pattern action buttons (`5116bf74`)
- pattern json export + import (`8c89938e`)
- add tooltips (`37a13d1a`)
- pnpm 8.1.3 to 8.11.0 (`20f3ddd2`)
- merge changes (`7225b0b1`)
- add checks for forbidden actions (`056fae51`)
- style (`2f9244e9`)
- reorder tabs + remove duplicated examples (`e551dfed`)
- working (`52628920`)
- Fix comment (`e1a8bd33`)
- Fix another comment (`5332f99a`)
- Revert to simpler link parsing and run formatter (`8e9b607f`)
- Fix examples page and de workshop img (`93c5eeb9`)
- Fix piano and two doc images (`a32ed027`)
- Fix to not add trailing slash to URL with filename (`60cee7fd`)
- Don't trim serialized query part of URL (`78b2b5cc`)
- palindrome abc -> abccba (#831) (`1bca05e3`)
- github action checkout v2->v4 (#837) (`2ce8576a`)
- pin pnpm to version 8.3.1 (#834) (`b8866cf5`)
- github action pnpm version from 7 to 8.3.1 (#835) (`fe8debe6`)
- JetBrains IDE files (#840) (`ebf93c06`)
- fix weird rounding change... (`1190f6e0`)
- improve slashing + base href behavior + prefix sidebar links & mdx links with base + add trailing slashes (`ac41426a`)
- Rework rehype plugin and clean up changes (`018a45e2`)
- Prevent 404 on Algolia crawls (`7c948dd8`)
- style input (`34c44a29`)
- removed unessecary change (`8146725f`)
- add 1 to channel selection (`9aec18b3`)
- comment (`f51109cb`)
- update tests (`9be10890`)
- upload samples (`61919a1b`)
- Add quick fix for non root bases (`3e5664a8`)
- Remove unnecessary change (`2c43fb25`)
- Update first-sounds.mdx (`b16f235e`)

## Ноябрь 2023

### 🐛 Исправления

- entry.name mutation bug (`4fd3e6e9`)
- merge error (`c6f89431`)
- examples layout (`34c1f3c7`)
- slight layout improvement (`e5f94673`)
- save pattern (`4cae6c05`)
- multiple repls (`09752241`)
- sampler broke without vibrato (`2a09f9ef`)
- namespace mini-repl styles (`7ad0ec16`)
- scope pos (`52e2b90e`)
- share copy to clipboard + alert (`8d3ba2a8`)

### ♻️ Рефакторинг

- move vibrato up + cleanup oscillator (`0052d349`)

### 📦 Прочее

- Add and style algolia search (`dacd1e61`)
- Codeformat for search (`020c3264`)
- document feedStrudel (`407510e2`)
- snapshot (`25b2eb57`)
- list synonyms in Autocomplete (`458c97b9`)
- populate autocomplete with all synonyms + properly display synonyms based on label (`18e9ceb5`)
- format (`fb7a9e20`)
- else clause (`456be920`)
- comment (`9045ce05`)
- default hydra config without audio detection (`508a6c92`)
- add feedStrudel option + smarter reinit logic (`f7a283f8`)
- channel splitting and channels parameter (`e4197337`)
- preview outputs should be stereo (`3fd5fdf1`)
- cleaning up (`1f62d85d`)
- cleaning up (`88e933ee`)
- cleaning up (`64c2e7c1`)
- fixed panic function (`886b16d6`)
- panic function update (`e7376c4f`)
- need to figure out channels input + seperating left right channels from source (`f0b458cd`)
- fix hydra performance issues (`de00212b`)
- add hydra audio capture option (`66fd20ac`)
- enable hydra options update (`ebc6723f`)
- add Hydra audio example to documentation (`a58ac967`)
- prettier code formatting (`0a280e93`)
- improve hydra audio detection initialization (`e0e896d4`)
- Move jsdoc files to subfolder (`d71877f9`)
- Update the undocumented functions file (`2868a5ff`)
- More code beauty please (`ecc8e0a1`)
- testing (`7aa21782`)
- hsl + hsla functions (`049aa352`)
- pianoroll smear as float (`f7599998`)
- build (`b27f8e45`)
- add q1 .. q9 shortcuts (`cb9ef3ee`)
- add p1 .. p9 shortcuts as well (`265f01eb`)
- Initial work on crackle (`476bda81`)
- Fixing density and adding documentation (`fb28083c`)
- Fix documentation (`2a18df61`)
- Run prettier (`6e92c491`)
- bump superdough (`a84cfb56`)
- Mark optional slider params (`a3bf6975`)
- add options param to initHydra (`c0f56be2`)
- rename pattern (`b8ca83dd`)
- Update README.md (`83561a0c`)
- remove pseudo note variables (`ee7cd8cd`)
- p and q methods + all function (`a7b799f1`)
- + add hush + add the ability to evaluate without clearing (`f43f6270`)
- fix tests (`a4487179`)
- fix codeformat (`8d5ef45e`)
- add option to disable active line highlighting in Code Settings (`ef140741`)
- fix typo (`2d1fcf4a`)
- move fix to Repl.css (`7f1919eb`)
- remove unwanted cm6 outline for strudelTheme (`ff0af15a`)
- Merge remote-tracking branch 'origin/main' into phaser (`b7dd9b7d`)
- Parse the synonyms tag in source documentation (`e4b00117`)
- Use synonyms in Tooltip (`bbc29256`)
- Show synonyms in API reference (`6ba2b767`)
- Simplify JSDoc (`2aff31fe`)
- Fix code format of JSDoc synonym plugin (`bfff38a2`)
- Ignore jsdoc-synonyms by eslint (`24e18818`)
- Document wordfall (`1b0427e9`)
- Document slider (`2d6654b2`)
- Document euclidLegatoRot (`e52e257d`)
- Add snapshot for euclidLegatoRot (`8a7ff9e0`)
- support multiple named serial connections, change default baudrate to 115200 (#551) (`d25851ac`)
- phaser shortcuts (`df636cf4`)
- phaser synonyms (`ac359a0e`)
- add params to superdough readme (`3ecb7015`)
- add to doc page (`2682fd38`)
- dedupe / move supertdirt phaser controls (`858a0263`)
- format (`85835087`)
- simplify (`7c8a8b8b`)
- snapshots (`791a3636`)
- Update first-sounds.mdx (`b88f58cc`)
- Update first-effects.mdx (`eed2c304`)
- Update pattern-effects.mdx (`df277d6b`)
- Update recap.mdx (`23dd9d4c`)
- table (`4a16cd89`)
- cleaning up (`0edd54de`)
- cleaning up (`bee36a7a`)
- cleaning up (`f0a582dc`)
- turn off of depth is 0 (`51c63fa9`)
- update doc (`2e4d28b9`)
- create free running lfos for different orbits (`65d07dbf`)
- add more parameters (`4b4290e0`)
- fixed doc (`06eb8b5b`)
- samples loading shortcuts: - use main branch by default in github shortcut - add bubo shortcut (`2ef35a85`)
- don't use anchor links for reference (`07da9a9d`)
- change to bus effect experiment (`9cbf3079`)
- add xfade + docs (`8c5837e7`)
- snapshot (`d7752961`)
- Implement optional hover tooltip with function documentation (`35333365`)
- Add pianoroll function documentation (`624e540c`)
- Add label function documentation (`0d06840a`)
- Add color function documentation (`f045fb44`)
- Fix pianoroll documentation (`8fa7bf79`)
- Add function params in reference tab (`e9265eff`)
- Update pianoroll documentation (`8fe4dca4`)
- tweaking to make it sound good (`a9efda3b`)
- document scope functions (`26d87b69`)
- snapshot (`c4f26c3c`)
- initial commit (`b3f8df17`)
- rename bindings key (`638f5c73`)
- remove obsolete config (`326571b7`)
- update to astro 3 (`419cf6c1`)
- no double ! (`77686547`)
- use 0.1.4 @vite-pwa/astro (`36ef03a4`)
- use new flag (`41a9098c`)
- snapshots (`9e649a18`)
- add doc chapters (`34bd0e11`)
- integrated vscode bindings (`d4c12dce`)
- Fix chunk, add fastChunk and repeatCycles (#712) (`9ceab0f2`)
- (hopefully) fixed broken lock file (`b141e1ee`)
- Update CONTRIBUTING.md (`1e5edfca`)
- fixed style issues (`0c2838cc`)

## Октябрь 2023

### ✨ Новые возможности

- add step as slider param (`28966739`)

### 🐛 Исправления

- flip scope in y direction (`a57c2b9f`)
- additive synthesis (`c085694a`)
- scale offset (`d84c917f`)
- loud example (`80c98b6a`)
- trailing slash confusion (`a23765a1`)
- trailing slash confusion (`624e6852`)
- try different trailing slash behavior (`8f4344f4`)
- roomsize not required (`f34937d4`)
- reverb sampleRate (`6544e813`)
- pitched sample as ir (`57b166d1`)
- hashes in urls (`444d583a`)
- reverb regenerate loophole (`4718bfac`)
- conflicts (`b25b9d81`)
- conflicts (`2d750a14`)
- reverbGen.mjs (`1d4eb59e`)
- formatting (`2f0798de`)
- slider crash on some platforms (`020e8590`)
- imports (`4b64168f`)
- import (`d7bc309e`)
- comment (`935d8e8a`)
- import (`96f06d80`)
- reverbs[orbit] is undefined (`275796c2`)

### 📦 Прочее

- functions (`4a57d3ad`)
- move to signal file with other choose functions (`fd6c7131`)
- hide patterns tab for now + add examples to welcome tab (`b245d442`)
- fix logger import (`a2077f7b`)
- half working user pattern management (`53160c63`)
- Document striate function (`1e1d5ef4`)
- Document adsr function (`2633f4fc`)
- replace strudel.tidalcycles.org with strudel.cc (`af915be1`)
- add px in zen mode to fix logo overlap (`9fc87fe2`)
- basic hydra integration (`7f3cdede`)
- add play function (`9009eccc`)
- also return pat (`b14aa901`)
- add docs + fix hydra for mini repl (`0f7b3e9f`)
- add H function to plug patterns into Hydra (`22c9829f`)
- document H function (`8754b023`)
- ignore hydra.mjs in linter (`3ce50d80`)
- use new vite-pwa-astro version from file (`54301475`)
- completely revert config mess (`46cdbd51`)
- Fix krill build command in README (`072ae728`)
- Support international alphabets in mininotation (`cb8edd9b`)
- Add a human-readable error message for invalid char in step (`87f37e3d`)
- Revert error message length limitation (`9b9f031f`)
- add recipes (`d9db5263`)
- add understand cycles (`7b8a35a3`)
- move stuff around (`f51c01dc`)
- format (`2cc60c6e`)
- Add test snapshots for shabda shortcut (`1b072448`)
- fixed (`6b5de5fe`)
- document function (`d3edc62f`)
- fixed example test failure (`912a661c`)
- Add shabda shortcut (#406) (`28982018`)
- Update samples/shabda documentation (`1721209f`)
- Prettier doc file (`fed4c51a`)
- vite-vanilla-repl readme fix (`d7028252`)
- bump superdough to 0.9.10 (`5c76f2b9`)
- add compressor + postgain (`130ad445`)
- compressor docs (`0a3692e4`)
- format (`08ce8213`)
- snapshot (`56c14414`)
- consume n with scale (`b297888e`)
- snapshot (`296cd83a`)
- consistent naming + simplify (`3c4f835d`)
- remove unused reverb method (`75099abb`)
- Integrate with the new impulse generation functionality (`d6c8838f`)
- update internal reverb param names (`508af7eb`)
- simplify createReverb (`ce842f75`)
- more logical naming + update docs (`a3649148`)
- snapshot (`90430645`)
- codeformat (`1e352fdf`)
- add noise heading + hihat example (`a0884e2a`)
- refactor synth - separate waveform / noise oscillators - pull noise out of getOscillator - put fm into getOscillator - simplify overall value plumbing (`484bb6b1`)
- proper dry wet + pull out noise to extra file (`2bc6d084`)
- cache noise (`04712922`)
- rename zzfx noise to znoise (`376cf095`)
- snapshot (`9c9323e0`)
- Prepare to merge with PR #718 (`13cb3290`)
- make sliders work! (`062d9269`)
- fix some odd number / string problems (`33c40e5e`)
- use sample as an impulse response for the reverb (`fd185082`)
- Document reverb controls (`abff2797`)
- bugfixes for parameter passing (`e600b91a`)
- improve mouse tracking (`c6ecd31e`)
- use sample as an impulse response for the reverb (`755e2431`)
- cleanup (`ac9c629c`)
- use native events (`b550ff03`)
- add back some margin (`f84d5ba3`)
- use raw css instead of tailwind (`d4bf358e`)
- add extra sliderWithID function + add warning to slider function (`564697e1`)
- remove checkbox (`c051a124`)
- remove comment (`21b99b38`)
- add missing dependency (`edbd437d`)
- support mininotation '..' range operator, fixes #715 (#716) (`fd316c81`)
- Connecting all parameters to convolution generator (`2d07eeb5`)
- Connecting new reverb documentation (`1909caf7`)
- codeformat (`6ca99e33`)
- eslint ignore reverbGen + snapshot (`dfdd9e02`)

## Сентябрь 2023

### 🐛 Исправления

- add conditional imports to eval scope + fire postMessage on start (`cf72e3bb`)
- control naming (`5c5df748`)
- headings (`cd6d1fb2`)
- tune n -> note (`578bede4`)
- don't add frequency (`40de717c`)
- docs after fenv change (`7dbcc904`)
- snapshots (`735f892a`)
- filters without envelopes (`989dc0aa`)
- tune (`7ec5ab0c`)
- linting errors (`3a69fd50`)
- eval scope (`4cb93ddc`)
- duration for loops (`f09b89ed`)
- looped samples pitch (`ba27f2ba`)
- filter envelopes + simplify (`e2711ba9`)
- filter clicks (`9e4c548c`)

### 📚 Документация

- fit + splice (`b98e24fa`)

### 📦 Прочее

- Add pink, white and brown oscillators (`c2481e46`)
- Add noise parameter for base oscillators (`389c7be2`)
- Fix noise parameter and FM parameters compatibility (`bb7b8c2f`)
- Cap noise amount to 1 (`e3333e71`)
- Add documentation about noise sources (`b2acff40`)
- match slider as function (`276cf858`)
- use loc as slider id (`2731e70f`)
- add slider function to scope (`b36cee93`)
- Replacing old reverb by better convolution (`0ad0046a`)
- checkbox + number slider (`aa094bf9`)
- match number.slider (`c93e4a95`)
- Merge remote-tracking branch 'origin/main' into midi-in (`e4eff8c8`)
- dedupe (`6c3a3b9e`)
- prevent error (`0dcc55ee`)
- support midi clock via "clock" control (not on desktop yet) (`68ab43b3`)
- use midicmd instead of clock (`dea1c317`)
- sync start / stop automatically too (`f11462bf`)
- send start with accurate timing (`4eb0a7b7`)
- add dough function for raw dsp (`6e26f397`)
- encapsulate .dough (`52c01abb`)
- less garbage (`7078e202`)
- cleanup (`eec3752b`)
- bump superdough to 0.9.9 (`62743edf`)
- Adding vibrato to Superdough sampler (`08aab9d4`)
- Merge remote-tracking branch 'upstream/main' into sampler (`d0258756`)
- update loop examples (`2deafe21`)
- add note about wt_ (`4490681f`)
- snapshots (`3879b01d`)
- Merge remote-tracking branch 'upstream/main' into vibrato (`887fb6a8`)
- simplify vibrato logic (`c354ee32`)
- set vib default to .5 (`c2560e0c`)
- simplify examples (`f052adb9`)
- format (`a97384ce`)
- snapshot (`c6a74c04`)
- move stuff back (`32fee631`)
- add vib examples for : notation (`0e7c9a40`)
- snapshot (`4fb36419`)
- make desktopbridge private for now (`b7ef2d97`)
- add filter envelopes here and there + comment out outroMusic (lame) (`57265b9e`)
- Publish (`de9a5236`)
- snapshot (`9c73ef77`)
- Update tauri.yml workflow (`258eb886`)
- Update tauri.yml workflow file (`05c09eff`)
- upstream changes (`dc254bd5`)
- anchored fenv, more logical with negative values - defaulting to .5 for now... (`df211c99`)
- simplify (`7f86eac8`)
- add fanchor (`e959c275`)
- fenv (`b96e8093`)
- keep only lp envelopes as examples (`af6a5b85`)
- snapshot (`a7eb0bb0`)
- add some filter envelopes here and there (`53642731`)
- update snapshots (`e0c424eb`)
- drop some acid in there (`e3b7d0f8`)
- update snapshot (`a82eeb44`)
- superdough 0.9.7 (`4fecad16`)
- slow down examples + simplify (`44588199`)
- update snapshot (`b8932390`)
- add all filter envelope examples to doc (`9d5f9190`)
- support negative fenv (`828e4759`)
- update snapshots (`7e55258b`)
- format (`4688c0bf`)
- cleaner default filter envelope - fixes some unexpected envelope glitches (`9142fb75`)
- simplify fenv examples (`88c02920`)
- simplify fenv (`9049a8b9`)
- snapshot (`ee314e9b`)
- midi cc input poc (`0f72729f`)
- remove log (`30d96dcb`)
- generalize getDevice + begin midiIn implementation (`0a887418`)
- document wavetable synthesis (`7a741897`)
- add documentation but it crashes (`843d9d52`)
- do not panic when no description (`4a65113b`)
- fixing minor bugs and adding description (`78adaaed`)
- add snapshots (`534e3f7f`)
- adding test run (`78ccec5d`)
- replacing fenv by lpenv, hpenv, bpenv (`bea9a3c2`)
- documenting filters more + tests (`1383af62`)
- Updating documentation (`7a95bafa`)
- adding loopBegin and loopEnd (`3582ae91`)
- beginning documentation work (breaks) (`bac8594c`)
- turn loop on for samples starting with wt_ (`6de04f77`)
- working (`e75ac7b3`)
- its working (`26b0e2ae`)
- changed warning to error (`b4297580`)
- add missing controls (`75a97464`)
- use fenv to scale above base cutoff? (`606dcc8c`)
- fix last crack? (`48753b25`)
- acid party (`18640d59`)
- Documentation for filter envelope (`c5fb247d`)
- documenting vib and vibmod (`34318039`)
- documenting vibrato and removing broken slide/pitchJump mechanism (`74442b0d`)
- stable version (`16c8dcf7`)
- remove test console log statement (`e597b647`)
- saner vibrato default (`fc9525e7`)
- parameter renaming (`76051987`)
- Envelope filter and filter order (`ec8616a5`)
- Adding vibrato to synth oscillator (`fdc201a7`)
- Implement pitch slide (`60f5032b`)
- add new controls + rename slide_speed > slidespeed (`3ba195c2`)
- timetag mishaps (`77349526`)
- correct bit calcuations (`009760c3`)
- New technique using ADSR envelope (`5be78985`)
- replace odd envelope (`4319df0c`)
- cleaning up (`4001cc95`)
- cleaning up (`a9d8e7ca`)
- dont panic if message fails to send (`d233d50d`)
- remove unused import (`15c15a9c`)
- fix MIDI CC messages (`97f84ee8`)
- setting up (`60e600c3`)
- its working (`62e3774f`)
- different time strats (`349d11e7`)
- add sleep timer + improve message iterating (`192adccb`)

## Август 2023

### 🐛 Исправления

- use custom version of ZZFX - ZZFX audio context broke tests - only needed buildSamples function (`b991cfc4`)
- format (`b1b08445`)
- condition flip (`7cf7ebc5`)
- getExpEnvelope full adsr (`6bcf8b91`)
- history (`88b5b9b2`)
- exp envelope allow 0 without blowup (`876f97e9`)
- move desktopbridge dependency to website (`328af5f5`)
- history (`749b11a5`)
- import (`4a122523`)
- comments (`24ec47e4`)
- bad import (`66c895ee`)
- tests (`c9d98bf4`)
- double registering (`90cdea36`)
- don't import emoji pkg (`88651149`)
- improve performance of setting patterning (`6f6def34`)
- import (`4dadc18a`)
- lint (`f7d17875`)
- format (`84328cfb`)
- always run previous trigger + make dominantTrigger more dominant (`7a999de6`)
- build entry file (`2778e4d6`)
- still had strudel core imports (`82cb4c8c`)
- welcome message for latestCode (`0cf5c9c7`)

### 📦 Прочее

- pull out debugging fn (`213b1a2d`)
- only use fm envelope when needed (`ee0aaca2`)
- keep sine as mod default (`6ca89cb9`)
- keep sine as fm default (`30577730`)
- remove comments (`26d7a32f`)
- urgh npm (`1bade7be`)
- remove weird flags (`a786c642`)
- rename some params + use correct duration (`0f54bb19`)
- rename more stuff + pull out fork (`1241911e`)
- add lfo alias for repeatTime (`6ec53773`)
- add doc (`388e7b3c`)
- add zzfx sound + zzfx control to override params (`4c744da3`)
- Merge remote-tracking branch 'upstream/main' into betterfmsynth (`37c450dd`)
- fm envelope docs (`b48fd78c`)
- snapshots (`a7246c65`)
- more clear example (`5b3cd166`)
- update snapshot (`e29cf6fc`)
- Merge remote-tracking branch 'upstream/main' into zzfx (`d6c4bb0e`)
- improve synth docs (`8dd08c36`)
- bump superdough to 0.9.6 (`defcacb5`)
- configuring package, wiring everything together (`c29c4c0a`)
- remove console statement (`ad230a8d`)
- Boilerplate for ZZFX Synth support (`c3f27d1a`)
- fix lock file (`282f36c4`)
- more zzfx wiring (`afb8ab44`)
- move zzfx stuff to separate file (`1463f27a`)
- add panel bg (`a179313d`)
- remove protracker theme + font (not good) (`6bd3274a`)
- delete protracker theme (`5efea045`)
- delete protracker font (`9de344e4`)
- add font credit (`942ffc03`)
- adding fm wave choice (`ebeaac3c`)
- removing rogue console.log (`94bcfb11`)
- envelopes on fmsynth (`8fc76885`)
- adding linear and exponential envelope for fm synthesis (`c3cd5d08`)
- removing comments (`c625096f`)
- bridge functionality for midi (`284f5615`)
- add protracker3 font (`1faa8109`)
- add protracker theme + fix: make sure all text has a bg (`cb643f8f`)
- add teletext theme + font (`0e3d84e5`)
- add mode7 font + can now inject custom theme styles (`28937684`)
- pianoroll improvements: - add label / activeLabel controls - add new pianoroll options:   - fill   - fillActive   - strokeActive   - stroke   - hideInactive   - colorizeInactive   - fontFamily - add wordfall method - fix: some haps were drawn with wrong length - pianoroll labels now use set fontFamily - hide fold gutter (`e3010907`)
- dedupe .pianoroll (`038e6c31`)
- add emoji support (`f6cf7405`)
- move scope to webaudio package (`43efe692`)
- comment new controls (`a7728e3d`)
- basic scope feature (`7370f41f`)
- improve canvas quality on retina (`68ea086e`)
- fscope + scaling options (`96579471`)
- rename stuff (`988bd8cc`)
- use Float32 for higher precision (`2e31c869`)
- remove annoying logs (`eb0301d7`)
- add nanFallback for better errors (`281ad5fa`)
- basic pattern add function (`c75c3f1a`)
- control osc partial count with n (`5cbe3009`)
- add patterns tab with examples (`1855dae3`)
- basic fm (`0c124bcc`)
- add snapshot (`badda165`)
- add doc (`f43dd8db`)
- simplify (`980c1e48`)
- bump superdough (`02979926`)
- panel z-index < autocomplete (`01cccc64`)
- encapsulate panel tabs (`5be10042`)
- rename footer -> panel (`4e7e715f`)
- time clock improvements (`b5b866a8`)
- cleaning up changes (`42662748`)
- dont round duration (`abb0b3b4`)
- round duration down (`3eb63f17`)
- togglable panel position (`f6789aaa`)
- fix panel scroll (`dabc0331`)
- fix settings style for right panel (`195e6b61`)
- fix reference style for right panel (`35687b1e`)
- add hint for zen mode (`3afe5401`)
- fix panel scroll + move to bottom when minimized (`37a35022`)
- ignore tauri dist for format-check (`61dc6f98`)
- let to const (`5fabc5c7`)
- add logger (`8bdbc9ec`)
- superdough readme doc (`5c1d7baf`)
- superdough 0.9.4 (`cd0f1b6c`)
- fix osc bundle timestamp glitches caused by drifting clock (`10bce855`)
- add superdough package (`30fe2dd5`)
- better code example (`8d9be21c`)
- add link to codesandbox (`7f44d9cd`)
- bump to 0.9.2 (`898bfaee`)
- use superdough in webaudio package (`738e714f`)
- superdough.mjs > webaudio.mjs (to keep history?) (`75281afe`)
- add disableWorklets option to initAudio (`c6095fd9`)
- bump superdough to 0.9.3 (`0c6a0720`)
- Fix for #1. (`1170ff58`)

## Июль 2023

### ✨ Новые возможности

- support strings in voicing function (`0fd9ac6d`)
- add voiceOffset (`d1fe50e2`)
- add voiceOffset (2) (`35f5a5d1`)
- support n for voicing scales + simplify voicing logic (`d5c03098`)

### 🐛 Исправления

- major chords (`45b91cde`)
- add empty string as major alias (`5f57951e`)
- ^7 voicings (`d231ded7`)
- flip subtraction (`4cfaa394`)
- console error (`8589d72a`)
- chord typo + export only voicings (`bcf80048`)
- note2oct (`f2c16a06`)
- remove log (`a639d9f4`)
- control interference (`78dcab27`)
- tests (`e53098b1`)
- duck mode with n + detect chord obj in rootNotes (`bbb29ebd`)
- support existing voicing dicts in new logic + rename voiceBelow to renderVoicing (`b0cbeda7`)
- pianoroll isActive still used whole.end (`36fa9d81`)
- tests (`2d08b31f`)
- do not recreate haps mapping on update (`3fc5bb31`)
- format (`7f12ce9b`)
- remove test flag (`64693ffd`)
- adaptive highlighting - transpiler now uses m function with globalOffset - patternifyAST now accepts global offset - patternifyAST now calls .withLoc with global leaf location - .withLoc replaces .withLocation + .withMiniLocation - simple locs (offsets) are now used everywhere - some tests fail, seems some haps have reordered... - wip: Repl still uses hardcoded updateMiniLocations - todo: find way to call updateMiniLocations dynamically (`0b5d9051`)
- init phase (`08abec8f`)

### 📚 Документация

- add slice effect (`06fbcbe2`)

### ♻️ Рефакторинг

- voicings -> voicing in tunes (`70fe8f14`)
- - dedupe flash / highlighting logic - codemirror logic now lives only in codemirror package - remove old highlighting logic - use codemirror package in react package - cleanup CodeMirror6.jsx - pull setMiniLocations into useHighlighting - migrate MiniRepl, nano-repl + Repl to new highlighting (`34176ab5`)
- remove old location methods docs: update repl chapter to reflect new transpiler behavior (`66f8ca72`)

### 📦 Прочее

- [Bug Fix] Account for numeral notation when converting to midi (`ac3d8b83`)
- [Bug Fix] Midi: Don't treat note 0 as false (`5c2101de`)
- sort voicings by top note (`d5647c67`)
- sort by topnote and then by bottom note (easier to track diffs) (`6377378a`)
- a few more voicings (`b1a47884`)
- add octaves control for arpeggios (`52b8d9d3`)
- add common chord symbol aliases (`9b3fa835`)
- finish pitch page (`cec7b906`)
- small fixes (`92d57460`)
- format (`45c32933`)
- Merge remote-tracking branch 'origin/main' into ireal-voicings (`f419559b`)
- begin pitch page (`2d8176f2`)
- improve pitch page (`7373138e`)
- better exponent display (`aa3cb35f`)
- add ireal voicings (`08f94d21`)
- add links (`1e62554d`)
- update vitest (`ce820c23`)
- TODO in rotateChroma (`0fa5832b`)
- prettier formatting (`b8929a0d`)
- weird gain difference (`a3fdfbbc`)
- note2pc TODOs (`4298e582`)
- revert changing existing addVoicings + add registerVoicings for the new logic (`e5251422`)
- snapshots + fix default anchor for lefthand (`ffeda19c`)
- snapshot (`3d8724c9`)
- docs + rename under -> duck, quak quak (`e05f74ce`)
- change call signature of renderVoicing + pull logic in from voicings function (`81b14270`)
- adjustable default octave + pull out static objects (`58fc7864`)
- voicing modes: below, under, above (`4cf3655c`)
- can now set mode and anchor on voicing dictionary (`d8f80c05`)
- - make dictionary a control - standalone voicing function - simplify voicing control names (`0b3a8a5f`)
- new voicing function (`110e179e`)
- slice (`27643f4a`)
- slice array notation (`fe6d99d4`)
- move tonleiter (`70d6f300`)
- comment (`9438c69b`)
- remove log (`f40b57ee`)
- remove mock positions (`78770888`)
- comment out opacity logic for now (`dacd9afa`)
- snapshot tests: sort haps by part (`00bd60a8`)
- Merge remote-tracking branch 'upstream/main' into highlight-ids (`9e1b9f7f`)
- remove tests from soon removed package (`6e4c8732`)
- delete eval package (`1718dbe9`)
- delete tone (`ce09443e`)
- delete webdirt (`7466d8a1`)
- package-lock (`40117fbe`)
- Pass mininotation locations into highlight state (`aded178a`)
- Remove highlights when code stops (`ba9562f0`)
- Split up highlighting logic and add progress flash (`f5b092ac`)
- add emitMiniLocations flag to transpiler (`5f271ed1`)
- - transpiler now always returns an object - emit transpiler metadata from evaluate / afterEval - currently logging miniLocations from Repl.jsx (`8e717d2e`)
- update package-lock (`f7bd373c`)
- adaptive highlighting (`63c23736`)
- leaf location retrieval - add onEnter callback for patternifyAST - add leaf node location helpers - add tests (`c7e882e0`)
- add 2 more tests (`d686b65d`)

## Июнь 2023

### ✨ Новые возможности

- register fn for sample prefix handling (`fb3ca9f9`)
- can now load disk samples via samples + encapsulate file logic from FilesTab (`b50075f1`)

### 🐛 Исправления

- update canvas size on window resize (`1b85aa71`)
- out of range error due to rerender (`bd8ad1ed`)
- playFile import (`c7d8a9cb`)
- build (`b39d948d`)
- midi clock drift (`b55721f1`)
- loopAt with cps 1 for fallback (`d126c28b`)
- doc links (`f1e850a6`)
- better fix (`a71c5957`)
- flatten scale lists (`8d927841`)
- typo + remove notes (`d4c59cc7`)
- division by zero (`9b312283`)
- allow f for flat notes like tidal (`7850fb72`)
- main repl option-dot on mac (`3b4e44a9`)
- repl link to docs was old (`51cc6920`)
- link goes to wrong language (`4d8308a7`)
- format (`ffffdd2d`)
- build (`1fec2f7a`)

### 📚 Документация

- update clip entry (`16aeb7e1`)

### ♻️ Рефакторинг

- legato -> clip (`260b8318`)

### 📦 Прочее

- Publish (`90a58858`)
- optimize rendering (`13545d14`)
- do not flash when empty (`dc2ff83f`)
- - encapsulate sample map handling - add resolveUrl param (`cc6997ba`)
- expose tauri fs api (`610ea414`)
- can now play samples from file system - ui still wip (`058b9718`)
- rename fay to fit (`0fee279e`)
- - fix: loopAt for non default cps value - add fay to repitch samples to fit hap duration - add setcpm function (`c1649b9d`)
- add tauri github workflow (`5567959a`)
- tauri workflow fixes (`52878252`)
- fix tauri icons (`9273e1f8`)
- dummy commit (`360e9b59`)
- another dummy commit (`b5160d38`)
- add spiral viz (`eaab36b0`)
- add build optimizations (`b83664df`)
- add README (`9a4f3af5`)
- init (`6f41716a`)
- init (`349f7ae9`)
- rename tonal api -> tonal functions (`2a449ad3`)
- allow patterning theme + font settings (`3872322d`)
- quick fix: typo (`eef6c7d5`)
- Merge remote-tracking branch 'origin/main' into clip-support-floats (`92da70a7`)
- Merge remote-tracking branch 'upstream/main' into line_wrap (`aa51c2b6`)
- add checkbox in settings to enable line wrapping (`f37a220a`)
- add ratio function (`09b14a57`)
- - clip now works like legato in tidal - supports floats - hap.duration now respects clip value - hap.endClipped is now end*clip - visualizations show clipped length - clip(0) will now be silence (`8c9e06c3`)
- only accept undefined for clip (`b4cbec17`)
- update snapshot (`5e900216`)
- Merge remote-tracking branch 'upstream/main' into branch-daria (`623661f8`)
- move solmization logic from midi2note to sol2note (`190fe37a`)
- + disable cursor blink + make cursor thicker (`370d82e6`)
- add label support to pianoroll (`3ccbd35f`)
- optimize sounds chapter after teach test (`c287ff55`)
- hide gutter in mini repl (`ae868c63`)
- enable autocompletion (`3069b14e`)
- avoid tabs overlapping (`a9ad3a56`)
- add vanilla js completion (`6f04a1b8`)
- Add option in settings to enable autocompletion (`2d2d0cab`)
- lint (`8e690947`)
- more friendly heading (`89a4a855`)
- more detail (`7dc2a009`)
- Merge remote-tracking branch 'origin/main' into workshop-new (`576b8822`)
- add autocompletion extension from useMemo (`5e09c3c3`)
- bring de improvements to en workshop (`e10104d4`)
- Merge commit '4d8308a763a632cc4dd93ed2a2ab129eb957af72' (`fe36bf54`)
- remove dirt samples + load a small selection for workshop instead (`eccce6fa`)
- add missing sounds by hand (`f8c3e3d5`)
- fix typos (`185318a7`)
- add quotes syntax for titles (`51e81746`)
- add option to display line numbers (`0c2147d9`)
- translate rest of workshop to german (`06950a6b`)
- Merge remote-tracking branch 'origin/main' into workshop-new (`02d793e9`)
- replace tutorial with workshop + move parts of tutorial to more + move more before Pattern Functions (`105193a4`)
- - translate getting-started - fix de links - link english docs / german workshop (`119a66ac`)
- translate link (`37c45414`)
- learn/tonal: fix typo in "scaleTran[s]pose" (`799afd51`)
- enable line wrapping (`b6d1f969`)
- allow several values and one-liners (`2e221520`)
- add tests (`bd7f4dd7`)
- improve doc (`96ac0543`)
- translate notes chapter (`a88759ba`)
- Merge remote-tracking branch 'origin/main' into workshop-new (`f0faf5b4`)
- started german translation of workshop (`61a9b010`)
- look for song titles in code metadata (`3db1c7a5`)
- add tunes metadata (`f9ad5f0d`)
- load titles from metadata in examples page (`52641db5`)
- add section about metadata (`372e33c0`)
- metadata parsing (`98a89aea`)

## Май 2023

### 🐛 Исправления

- thinner outline for highlights with .color (`d74cc234`)
- nano-repl (`d93e396c`)
- dupe style.css to Repl.css for some reason they were missing after update + darken line backgrounds + change preview port to keep workbox out of dev server (`d3240a75`)
- codemirror reeval (`b5b0156e`)

### ♻️ Рефакторинг

- remove old draw logic + pianoroll now uses .onPaint (`95719654`)

### 📦 Прочее

- + pattern effects chapter + recap page + only load mini repls when visible (`c4a38d90`)
- also reflect gain in transparency (`8679dc63`)
- effects chapter mostly finished (`536327f4`)
- add function recap to sounds chapter (`39d5955e`)
- add adsr section to effects chapter (`8b7bb7b6`)
- add compound adsr + ds controls (`0edd7e14`)
- clamp delayfeedback (`8c93e578`)
- clamp function (`9971867e`)
- consume font settings (`d2dffe31`)
- start fleshing out effects chapter (`1e9979ae`)
- - add claviature flag to minirepl - bring back option+dot on macos - consume more editor settings in minirepl (`fc061812`)
- begin first notes page (`4e575c44`)
- continue notes chapter (`ed792fc0`)
- finish notes chapter (`1ba5d2e1`)
- started workshop pages (`82225f0b`)
- hide mini repl headers + improve workshop (`0d6fcf78`)
- Solmization added (`811346e8`)
- Format code (`ccf775e9`)
- Update solmization.test.js (`cd7bc09f`)
- Update solmization.test.js (`b63f4eb5`)
- bump soundfonts (`0fda83d8`)
- rename init to initStrudel + add to window (`6089849d`)
- remove soundfonts from web package for now (`e2bfaa62`)
- bump webaudio package (`1fdb1f0d`)
- bump web pkg dependencies (`db79c05d`)
- add headless-serverless-buildless example (`1de784b8`)
- update web readme + add more html examples (`087f5743`)
- format (`a1ec8545`)
- add @strudel/web umbrella package + example (`3b631cb6`)
- improve api for web package (`12228c56`)
- add vite-vanilla-repl-cm6 (`1494cc38`)
- vanilla-repl-cm6: add keybindings (`f069f53f`)
- vanilla-repl-cm6: add flash effect (`4b921c47`)
- draw scheduler in vanilla js (`1f4c2f8c`)
- vanilla-repl-cm6: style + codesplitting (`d96f3f9d`)
- vanilla-repl-cm6: better theme (`d35bf959`)
- prettier (`a6f57bce`)
- add codemirror package + use it in vite-vanilla-repl-cm6 (`f5075906`)
- lockfile (`e6b66f25`)
- add build script for codemirror package (`352e647d`)
- bump codemirror to 0.8.1 (`9039600f`)
- move Framer + Drawer to core ... from vite-vanilla-repl-cm6 (`9807c4b7`)
- vanilla-repl-cm6: draw first frame (`5b67fccb`)
- + simplify vanilla setup drastically + move repl + drawer boilerplate inside StrudelMirror (`9ec7109d`)
- bump codemirror package (`bb71c519`)
- lockfile (`299275e3`)
- rename core package + bump to 0.8.0 (`b52f65c4`)
- revert rename core package (`692fc4e1`)
- bump codemirror again to get new core version (`308f1391`)
- bump more packages to 0.8.0 (`b27a58df`)
- add basic triads and guidetone voicings (`d9710326`)

## Апрель 2023

### 🐛 Исправления

- controls import broke on ssr (`fd81867e`)
- make soundfonts import dynamic - it broke the whole docs page (`b7fcccb8`)
- colorable highlighting (`b88e0ecb`)
- load soundfonts in prebake (`c138cf0b`)
- do not prebake in tests + pull piano function to separate file for tests (`6cd3cc96`)

### 📦 Прочее

- - remove gh-pages - update peggy - rebuild parser (`13088e40`)
- remove c8 (`d1c8ebd1`)
- update codemirror + tailwindcss (`8b62b6e1`)
- update lerna (`0785bdd3`)
- update a bunch of minor versions (`602bfbdd`)
- update remaining packages (`a959e2e3`)
- remove preact (`c0b0ea44`)
- update astro (`efdce6d4`)
- update supabase-js (`0b2a4fda`)
- bump some minor versions (`930fc10a`)
- lockfile (`78293e64`)
- update vite to v4 (`5c6fa1c8`)
- install with pnpm 8.3.1 (`3a85f28b`)
- update vitest to ^0.28.0 (`dc6c29c6`)
- use vitests@^0.28.0 in packages (`a0ed25d3`)
- update vite-plugin-pwa (`99298ce9`)
- update nanostores (`12a97ec4`)
- update @vitest/ui (`2bdcfe13`)
- update @vite-pwa/astro (`b21a52f5`)
- Merge remote-tracking branch 'origin/main' into patchday (`c0701d8a`)
- upgrade jsdoc (`0fcec0a0`)

## Март 2023

### ✨ Новые возможности

- add loader bar to animate loading state (`17654b88`)

### 🐛 Исправления

- allow whitespace at the end of a mini pattern - fixes #546 (`a9811ee9`)
- reset time on stop (`6de2feff`)
- remove loading state on eval error (`30d1f423`)
- **footer**: fix link to tidalcycles (`43176e1f`)
- envelope in chrome (`33d35e47`)
- cut glitches due to envelope release trouble (`8a22c562`)
- endless preview (`a33ce67d`)
- nano-repl highlighting (`efddbb61`)

### 📦 Прочее

- add 2 FiraCode fonts (`e0c5566f`)
- dummy commit (github cert error :-/) (`3df2929d`)
- - feat: add freq support to gm soundfonts - refactor: toMidi -> noteToMidi - refactor: fromMidi -> midiToFreq (`ba35a81e`)
- update lerna (`b9ca9d3b`)
- Publish (`3abe6db3`)
- bump core + mini to 0.7.1 (`ff1c37d3`)
- Publish (`18d72228`)
- update package publishing guide (`251be606`)
- fix period key for dvorak + remove duplicated code (`73169563`)
- improve initial loading + wait before eval (`55c533c9`)
- do not reset cps before eval #517 (`8617b916`)
- Merge remote-tracking branch 'origin/main' into setcps-reset-only-on-shuffle (`6a201be1`)
- alias pm for polymeter (`1458ee64`)
- Maintain random seed state in parser, not globally (`5490b010`)
- Sync up `?` with `degrade()` (`4b51f1a3`)
- Uncomment test for random choice operator (` (`Ian Clester`)
- Format code with prettier (`c98b0e96`)
- Add pairwise independence test, tweak `randOffset` (`4e2a5e16`)
- Update samples.mdx (`848cbf9c`)
- Update intro.mdx (`ec594d52`)
- alias pm for polymeter (`fffd3c07`)
- group soundfonts into readable names (`a6880616`)
- format (`b5d3aa81`)
- use only _ for easier copying via double click (`15681262`)
- regroup sounds - extra tab for drum-machines - combine synths and soundfonts (`e63a5d10`)
- pass duration to source callback (`aad72189`)
- fix only tune that uses soundfonts (`9f9fe529`)
- snapshot (`58ed51b4`)
- add envelope to soundfonts (`f788d347`)
- Fix typo on packages.mdx (`74ad521f`)
- style sounds tab (`7b529a30`)
- soundfont list curation part 2 (`b59b2646`)
- add 2 illegible fonts (`6a73b43e`)
- start curating the soundfont list (`aa324ae2`)
- sounds onset and offset can be triggered independently + sounds tab now supports mousedown / mouseup to listen (`65e48c05`)
- proper audio node cleanup + cleanup onTriggerSample (`be6b0112`)
- add cleanup to sound tab (`41d82eaa`)
- document setSound api (`ff1e6714`)
- rename setSound to registerSound (`6f5d096e`)
- register soundfonts as sounds too (`ac148b2f`)
- level down soundfonts + do not load in prebake (breaks static rendering) + instead load on import (`ff412f63`)
- sounds list can now filter out defaults (`cee08ea6`)
- can now pass meta data to setSound + added types for synth and sample + list sample count for samples in sounds tab (as before) (`35ef26c0`)
- use nanostore for soundmap + rename tab samples to sounds + listed sounds are now reactive (`b08a0b81`)
- first draft of soundMap to register keys for s + refactor sampler to use it + refactor synth to use it + add 'source' control + wip: samples tab + wip: webadirt ? + wip: soundfonts (`6059c699`)
- Support list syntax in mininotation (#512) (`bf72908d`)
- instead of _ (`a50bd637`)
- add test (`9a4d8036`)
- add arrange function (`487ccb90`)
- add snapshot (`de503c7d`)
- update react to 18 (`81ab5c78`)
- Add control aliases (#497) (`db4d0f26`)
- Reinstate slice and splice (#500) (`2d5064bd`)
- can now use _ as a replacement for space in scales (`14508e91`)
- comment out cps ui (`f8724bf4`)
- reset cps before eval (`436ba067`)
- add readme to nano repl (`06cffbc3`)

## Февраль 2023

### 🐛 Исправления

- minirepl styles for package (`18c7e026`)
- reify after sequence to preserve array (`ed7763df`)
- lint error (`7ac0cdc0`)
- hash links (`4c5d6f1d`)
- osc should not return a promise (`a742a71f`)
- anchor links fixes https://github.com/tidalcycles/strudel/issues/432 + fix style regression in mini repl (`64ad75c7`)
- right sidebar anchors earlier (`64b8fd95`)
- share url on subpath fixes https://github.com/tidalcycles/strudel/issues/389 (`dcdb3839`)
- eval dependency + add deprecation note (`48fd9436`)
- minirepl styles (`6f5f8cc7`)

### 📦 Прочее

- implement cps + add baasic tempo control ui (`ead5942e`)
- Update @strudel.cycles/react to 0.6.1 (`8b51f0fb`)
- comment out autocomplete (`032acbd2`)
- add variables to react tailwind config (`7e135150`)
- Update @strudel.cycles/react to 0.6.2 (`66fba947`)
- fix disabled button (`e9d57749`)
- refactor react package for publishing (`668f97a4`)
- Update @strudel.cycles/react to 0.6.3 (`a5641aef`)
- fix disabled button take 2 (`5de40b6b`)
- Update @strudel.cycles/react to 0.6.4 (`dda93b3c`)
- Update README.md (`4463d4a4`)
- no need to warn if evalScope is called more often (`f5c0cc2e`)
- pass cps to hap trigger (`86d26522`)
- add setcps to global scope (`1f7e2932`)
- packages doc (`073fa096`)
- add dependencygraph (`75dfb0af`)
- better offline docs fixes https://github.com/tidalcycles/strudel/issues/424 (`1a75b79a`)
- add screenshots (`28534ea5`)
- fix format (`97c8ab0f`)
- Update mini-notation.mdx (#365) (`37d6723e`)
- change 1px (`3b3435c9`)
- add algolia config (`a7f6d6ec`)
- fix pseudo relative links (`2750a3c1`)
- add midichan + docs (`886f8449`)
- Merge remote-tracking branch 'origin/main' into fix-array-args (`49234183`)
- add helper hap.ensureObjectValue (`6283273d`)
- support ccn and ccv (`5de66436`)
- Update README.md (`5506d18b`)
- slice and splice (#466) (`7c367eb1`)
- Update input-output.mdx (`7ff87746`)
- more contrast for blackscreen theme (`8f6c1531`)
- customizable font family (`ff99dbcd`)
- refactor settings to nanostores (`b67b0498`)
- store footer tab + latest code in nanostore (`70f7e73b`)
- move isZen to nanostore + correctly parse non strings in store (`0006d57a`)
- add vim toggle to settings + added persistent global state store + refactored themes to use the new store (`014555fe`)
- add emacs mode + fontSize slider (`ceb3aa06`)
- fix app height for ios (`f3f18ffc`)
- add some custom themes (`1e3f09f6`)
- use css var for highlighting color (`632e8e96`)
- rename algoboy theme + add font (`ac3bd7d7`)
- define font + fix app height (`14af1df6`)
- more themes + fonts (`81b4cb2f`)
- get rid of all hard coded colors (`7994ba8b`)
- add settings reset button (`a21b3d78`)
- fix bluescreen line bg (`89cd0c76`)
- use terminal font for console + samples (`77165740`)
- fix store import (`20848aac`)
- weave and weaveWith (#465) (`fdb76867`)
- add vim extension (hard coded) (`18d3a7e2`)
- freqs instead of pitches (`c6fbebd9`)
- remove unused Footer + fix AvatarList (still unused) (`540bd938`)
- encapsulate footer components (`4a3540cf`)
- Warning about jsxBracketSameLine deprecation (`c6ce763f`)
- Continuation to Csound (`a08ad075`)
- Continuation to Samples (`9ab315ac`)
- Continuation to Synths (`42caa905`)
- Continuation to Functional JavaScript API (`0fd783e8`)
- Continuation to Pattern Constructors (`760bdea5`)
- Continuation to Time Modifiers (`90a90a87`)
- Continuation to Control Parameters (`8549d6ea`)
- Continuation to Signals (`f50a6ce1`)
- Continuation to Conditional Modifiers (`61b0d08b`)
- Continuation to Accumulation Modifiers (`176877f2`)
- Continuation to Tonal Modifiers (`451828e5`)
- Continuation to MIDI and OSC (`8ffaeeb1`)
- Continuation to Offline (`33dbf75e`)
- Continuation to Patterns (`cfd04ab7`)
- Continuation to Pattern Alignment (`9dbf52be`)
- Continuation to Strudel vs Tidal (`4704c03d`)
- Continuation to REPL (`9daea5bb`)
- Continuation to Docs (`99ec261f`)
- Continuation to Testing (`40e629cb`)
- Another attempt at composable functions - WIP (#390) (`cbae3558`)
- Update CONTRIBUTING.md (`f024b718`)
- Update code.mdx (`13a5d8de`)
- Update mini-notation.mdx (`5f82a868`)
- Update synths.mdx (`5c05a5a6`)
- update lockfile (`b001d1d0`)
- fix comment (`be3f68ed`)
- remove slash before anchor link (`12eb361d`)
- autocomplete dark / light (`9694ecf5`)
- no autocomplete until upstream merge (`0fb478a7`)
- update themes (`03262449`)
- switchable css variables for theming (`0b994a1b`)
- persisted theme + add all settings (`46f70e6a`)
- only invert prose for dark themes (`6db2c0bf`)
- themify rest of layout (except minirepls) (`ddb9af4d`)
- format (`6170eef4`)
- themed minirepl + add strudelTheme + add lineBackground with opacity + add some missing light flags + add fallback css variables for non js context (`14cb9542`)
- dynamic highlight color + refactor hooks (`3579b6f8`)
- Merge remote-tracking branch 'origin/main' into docs (`fe80478b`)
- use jsdoc in autocomplete, still not context aware + add noAutocomplete flags + add superdirtOnly flags (`97cbdc6b`)
- note (`8da81104`)
- remove logs (`0f18f5eb`)
- reactify autocomplete info (`f497d7b2`)
- fix param description (`f02a41b6`)
- update snapshot (`33e2631e`)
- add keyboard shortcuts (`3209afa4`)
- remove unused descriptions + fix some titles + comment out unfinished packages page + start offline page (`ea1b2252`)
- disable sw in dev (`cdd40df9`)
- basic theme switcher (`30b14905`)
- add freesound + shabda to offline cache rule (`36713448`)
- fix cache rule + disable service worker in dev (`b6e42876`)
- add cdn.freesound to cache list (`2a2c7437`)
- basic theme picker (`3e727893`)
- add caching strategy for missing file types + cache all samples loaded from github (`6a5d849b`)
- dont lint json (`f3f87b0a`)
- change rocket to strudel (`10580a0b`)
- basic pwa setup (`fd09dff3`)
- update og meta data (`44ef3723`)
- format (`3d27a5f8`)
- categorize effects + add envelope doc + use short, more logical filter params as default (`1b99ac92`)
- remove envelope section from synths page (`554e0d7a`)
- format (`74326f22`)
- fix examples + snapshots (`0ec65c85`)
- Update effects.mdx (`ef4a68a2`)
- add adsr image link (`ce64c98f`)
- improve samples doc (`0fdae704`)
- add snapshot (`379b8392`)
- remove double directive (`0a77770e`)
- better headings (`3e99f345`)
- google gtfo (`e15fecf6`)
- iclc reviews (`bb50df07`)
- Update README.md (`ac606bcf`)
- add shabda doc (`a7d484b1`)
- pin @csound/browser to 6.18.3 + bump (`9d8c812a`)
- update csound + fix sound output see https://github.com/csound/csound/issues/1690#issuecomment-1414301145 (`f7b3b582`)
- remove sc prefix to fix build (`08350d00`)
- bump eval (`7f67adb6`)
- update examples (`9a5bda86`)
- add nano-repl as workspace (`b58f5232`)
- overhaul the remaining readmes (`54a61dcd`)
- add pattern methods hurry, press and pressBy (#397) (`46f3b662`)
- add missing snapshots (`91fd3fb5`)
- update contribution guide to pnpm + update root tasks (`d60956e8`)
- organize tasks (`aa5dd792`)
- trash obsolete package-lock files (`2d52168c`)
- can now await initAudio + initAudioOnFirstClick (`014ae210`)
- bump webaudio (`fb6195b0`)
- deploy workflow install command (`2e390acc`)

## Январь 2023

### 🐛 Исправления

- gh actions use pnpm (`a373629f`)
- left sidebar scroll (`9539c6d3`)
- mini repl could eval before modules are ready (`a2d7d6e8`)
- another dependency array (`2732d64b`)
- warnings (`3744ea62`)
- double slash (`d4512f36`)
- snapshot (`79904560`)
- right sidebar width (`a120a989`)
- snapshot (`95fcce1b`)
- typo (`0974540e`)
- mini repls were broken (`4b79a75f`)
- smear 0 special case (`0abd062e`)

### 📚 Документация

- use note instead of n to mitigate confusion (`ffdfb07f`)
- improve time modifiers page + polymeter + euclidRot + rename Factories to Pattern Constructors (`10ee11c8`)
- control params (`b695e090`)

### 📦 Прочее

- update lockfile (`2823456e`)
- add pretest (`84bf4a26`)
- rename website package (`f448d8f3`)
- add prepublishOnly everywhere (`c032f6dd`)
- update core examples (`0aa2f72a`)
- write more about packages / monorepo setup (`4121fc92`)
- use pnpm workspaces (`35f20407`)
- fix website build (`b319754d`)
- use workspace protocol everywhere (`ea2d8b27`)
- add import linting + fix checks (`902f722d`)
- migrate lerna to pnpm https://lerna.js.org/docs/recipes/using-pnpm-with-lerna (`cfaecf9f`)
- add core build (`458dc6e0`)
- remove unused deps (`65709130`)
- add mini build (`be44c28f`)
- bump mini (`555fe0b6`)
- csound build (`cde23bbf`)
- eval build (`4aa32415`)
- midi build (`c25aad0c`)
- osc build (`75076547`)
- remove dist folder (`348d576f`)
- react build (`b5914a3f`)
- serial build (`cb7156b0`)
- soundfonts build (`fddbb2e2`)
- tonal build (`6118bd24`)
- bump (`f3da9d68`)
- tone build (`ffaa8f4a`)
- transpiler build (`cd1da3cb`)
- webaudio build (`19692e01`)
- webdirt build (`52e01321`)
- xen build (`a5d4a437`)
- Notes are not essential :) (#393) (`217e348f`)
- Merge remote-tracking branch 'origin/main' into docs (`fea1e71a`)
- add csound docs (`e946e1e0`)
- add run + test + docs (`468ccc62`)
- rename a to angle (`0a0a761e`)
- add draw examples (`ba6714f3`)
- add ribbon + test + docs (`9731eb8c`)
- update my-patterns instructions (`9923bcd1`)
- import patterns created by new method in: https://github.com/urswilke/read_beats/commit/ecd8c03b5e29544819831459f4b4ac48d954640f (`cd2b098b`)
- npm test (`ff9ebd64`)
- move drum patterns in a new file (`05b63533`)
- catch all query errors by default (`80098941`)
- do less work when not drawing (`5aa983b4`)
- fix sentence (`9afddf08`)
- fix dependency array (`88684d6d`)
- localize draw logic (`1ac784dc`)
- reset canvas when pattern changes + rename noteroll -> punchcard (`ea0e0b43`)
- fix option handling (`e4f538b6`)
- add punchcard flag for implicit vis + reintroduce editPattern + add punchcards to mini-notation.mdx (`b2c6d876`)
- fix minor bugs (`49bcb5e1`)
- more doc (`4059e9fa`)
- more doc (`4345fda9`)
- update snap (`bf13e420`)
- hide internals for now (`e3b0f47c`)
- format (`ca9c813b`)
- fix snapshot (`efa4bfef`)
- remove experimental class properties (`62af12f3`)
- add script that finds all undocumented exports (`48d0ffe8`)
- fix polymeter doc (`28aee1c5`)
- remove unused package (`19982a2f`)
- automatically print synonyms in doc (`78d14cdf`)
- add note about single quote strings (`171808cf`)
- reformat headings + add another note about single quotes (`7da94dd1`)
- burn down that undocumented list (`68cc1099`)
- add more synonyms (`ee9b89b6`)
- tests (`6a2a60df`)
- remove mocked .out (`e9c1db02`)
- add internals doc (`d5f37e2c`)
- split accumulation and conditions (`db75dca1`)
- arp + arpWith (`500c0b65`)
- arp use innerJoin instead of squeezeJoin (`6e315a50`)
- arpWith example (`86cde971`)
- further sidebar reorganizing (`bb00036a`)
- some corrections + n should not be recommended for notes as it does not work for samples (`9ca5f9ad`)
- Support for multiple mininotation operators WIP (#350) (`f57fe182`)
- support logs in mini repl + pass hap to logger + add editPattern hook to repl + useStrudel + do not throw when webaudio gets plain values (`69ecb7b5`)
- snaps (`57ba3535`)
- add https to url (`8f09e7f0`)
- 09 (`3d48135b`)
- preview command + build peg without logs (`a6758155`)
- Typo (`32e64bbb`)
- 08 (`c84714dc`)
- Merge remote-tracking branch 'origin/main' into docs (`93f520b9`)
- update snapshots (`22a64c49`)
- format (`09217216`)
- Update README.md (`4a2a5405`)
- Fix .out(), renaming webaudio's out() to webaudio() (#361) (`45842b1f`)
- 06 (`e56abaef`)
- add hideDescription flag to JsDoc (`5733e009`)
- pull apart functional docs (`62fdba06`)
- remove quotes around inline code snippets (`70695daa`)
- 07 (`efa1cdfc`)
- 07 (`a7aa09a1`)
- Fix Bjorklund (#343) (`68c90080`)
- began tidal comparison page (`d4518f7a`)
- improve effects doc (`ebb1bbc9`)
- update snapshots (`605a1365`)
- add filter synonyms (`8781fa2e`)
- write more tidal comparison (`dbb84ef1`)
- casing (`39923565`)
- 05 (`41f5f3fe`)
- fixes #346 (`4da2e003`)
- 04 (`cf12ecfa`)
- tweak (`64fd53a8`)
- deploy on push (`405c12ba`)
- make prebake relative to BASE_URL (`027c669a`)
- 03 (`9ec5787b`)
- add base path + use relative links (`11d3251c`)
- do not include url in base pth (`8e5d02fd`)
- fix examples route (`ba6c7b09`)
- more links to fix (`205debde`)
- rename .. (`54861977`)
- Patternify euclid, fast, slow and polymeter step parameters in mininotation (#341) (`db7ef01d`)
- 02 (`069d63e3`)
- Update README.md (`9170269e`)
- 01 (`0f3da5be`)
- animation options + do not sync by default (`1286468d`)
- move /my-patterns to /swatch (#338) (`7a964360`)
- custom domain (`b7624813`)
- 01b (`e9d965ce`)
- 01c (`98a83ddc`)
- rescale + move + zoom (`28912fff`)

## Декабрь 2022

### 🐛 Исправления

- can now multiply floats in mini notation - fixes #314 (`fb148657`)
- workaround Object.assign globalThis - see comment (`6061f102`)
- #317 (`243a350e`)
- #309 (`4e5a0d9e`)

### 📚 Документация

- testing + docs (`84838687`)
- mobile nav (`232a04a1`)
- envelope functions (`63fffd74`)

### 📦 Прочее

- animate mvp (`0caedeb4`)
- controllable smear + sync flag (`949e15d8`)
- Tidy parser, implement polymeters (#336) (`8bb46070`)
- improve pianoroll value mapping (`dd736130`)
- add color to some tunes (`af49c411`)
- simplify embedded mode (`cf21c408`)
- simplify meltingsubmarine tune (`a5765239`)
- roll back hh rhythm (`26b8f93c`)
- Merge remote-tracking branch 'origin/eval-fix-node' into docs (`1dd0ff13`)
- add examples page with prerendered swatches (`9f058bd8`)
- add my-patterns (`cb8b65f6`)
- fix build + simplify logic + add deploy note (`16b4058a`)
- fix paths + update readme (`6420a72c`)
- microrhythm experiment (`aaf3ae5f`)
- codeformat (`51a4c4d1`)
- wut (`320d987e`)
- css + sn -> sd (`26be10f9`)
- add scheduler.now to get phase starting from 0 (`0792d0d5`)
- begin reimplementing draw logic for parallel use (`2d1b62a9`)
- fix format (`b8f0a1dd`)
- better color support (`36f83773`)
- fix anchor links (`c1d2bf9b`)
- add technical manual to docs (`d7ea37c9`)
- react package build... (`37dc723b`)
- remove old graphic (`56cd7794`)
- only start animation loop when canvas is visible + add some placeholder files (`7b43ed70`)
- codeformat (`1b41994e`)
- remove color magic for now (`b37f3c81`)
- Merge remote-tracking branch 'origin/main' into docs (`6d522e17`)
- add withCanvas (`2ff71126`)
- tutorial updates (`858f4f74`)
- proper setup minirepl (`b46330fc`)
- translate parts of tidal "How-tos" (`3feed900`)
- fix import (`38e066ee`)
- build task for ci (`d59eec92`)
- add missing npm ci (`847602f9`)
- make sure jsdoc is built before build (`d89502f9`)
- go to repl link (`8c09e7ba`)
- update snapshots (`8fe3fda2`)
- supper notes without octave (`d1d9b37e`)
- Merge commit 'd1d9b37ec764dd7377754d88e72996ef887beb82' into docs (`5ca7ce1f`)
- rename folder (`006ca12b`)
- integrate repl into astro website + update build and setup tasks + workflow + move repl test folder to root + move docs and repl to website/src (`818cd904`)
- fix import + update build task (`9aaaa755`)
- make repl and doc styles more similar (`f18a2a11`)
- responsiveness (`6a9e9ebc`)
- autolink headings (`869a44dd`)
- copy potentially useful files from old tutorial (`ed79ec79`)
- delete now obsolete repl + tutorial folders (`08f7925e`)
- fix pretest task (`c9b73c68`)
- add tutorial redirect + place learn link last in repl header + fix all old links to /tutorial (`dcb14563`)
- basic style for toc + further cleanup (`7efaff4f`)
- format (`d0013f55`)
- simplify writing mdx files (`b8f991ed`)
- split tutorial into multiple pages + improve layout a bit (`b1202fcd`)
- remove theme.css + cooler bg (`7a19659c`)
- more styling (`f64ef32e`)
- style left sidebar (`0981f95f`)
- slight layout fixes (`b5f9ab7c`)
- ICLC2023 paper (#306) (`bf373d89`)
- throw out nunjucks (`0a9604d1`)
- fix anchor icon color (`613fc6e3`)
- fixed namespaced anchor links (`2c44c252`)
- basic astro docs (`bc7a84e4`)
- add scale test (`264cab3c`)
- support freq in pianoroll (`4de477d4`)
- add lint + prettier check before test (`67ad82d5`)
- code format (`bf1ba6ef`)
- run prettier + lint separately (improves output) (`5d3c522d`)
- code format - PR was issued before prettier check, now ci fails (`b0bc20cb`)
- Publish (`2118a204`)
- bump csound (`6a5010a2`)
- bump already published packages (`794cfe94`)
- Publish (`2bbdd21b`)
- bump root to 0.5.0 (`0c98a842`)
- fix core deps after release trouble (`8ebbef4c`)
- uncomment essential line :) (`94651731`)
- fix whitespace trimming (`b4f4d6ce`)
- add freq support to sampler (`df73ce8a`)
- fix error message (`09b06da4`)
- fix non note samples (`c6d57f6a`)
- Updated csoundm to use . (`c94443fa`)
- fix typo (`bcac3c5b`)
- make mini "?" behave like .degradeBy + add comment (`306004ec`)
- dont use random seedState for now (`caa207e8`)
- fix some tests (`719dcbcd`)
- "fix" voice leading (`a0ae8f4a`)
- comment out pipe operator test (`a2d83c48`)
- update snapshots (`8dd6440b`)
- add prettier task (`b98d2201`)
- add test (`def6bbf6`)
- lastOf was undefined (`210910eb`)
- update vitest (`df40204a`)
- scale now supports running on object values with n (`6679cd60`)
- remove whitespace from highlighted region (`1b06795f`)
- .defragmentHaps() (#299) (`56424d29`)
- Fancy hap show, include part in snapshots (#291) (`98e97808`)
- Reorganise pattern.mjs with a 'toplevel first' regime (#286) (`4340f024`)
- use register for degradeBy / undegradeBy (`09b15a07`)
- refactor most signal functions (`4f7c013c`)
- refactor tonal functions to 'register' (`bc43fc95`)
- refactor uses of voicings + fix usages of scaleTranspose (`b81a66f0`)
- refactor xen and tone packages (`27a6ac0c`)
- refactor csound (`2f4da3ed`)
- refactor speak (`1890e65b`)
- remove bootstrap + Pattern.patternify (`b9781915`)
- snapshots (`ccc90108`)
- can now add bare numbers to numeral object props (`5fbd1453`)
- delete bare value before numeral union (`a45d6359`)
- Switch 'operators' from .whatHow to .what.how, and make sure there are toplevel curried functions for all of them. Rename util.mod to util._mod, to make room for toplevel 'operator' of that name. (#285) (`f827201e`)
- implement collect + arp function (`4dd08268`)
- add arp example tune (`ff987f14`)
- update snapshot (`98a104f7`)
- now passing controls as string keymap (`e8fb7ee6`)
- Support sending CRC16 bytes with serial messages (#276) (`caea7bb3`)
- added loadOrc + removed livecode.orc by default (`486cd003`)
- update tunes (`c67b02b7`)
- add licenses / credits to all tunes + remove some (`72a8e4bc`)
- update snapshots (`33b6579b`)
- Merge remote-tracking branch 'origin/main' into csound (`edf2287e`)
- fix loadOrc in tests (`f491d0c0`)
- do not recompile orc (`7a799957`)
- get rid of csound warning on reevaluation: - only compile on init, use evalCode after - pull presets into csd file - use custom logging (`9052ff71`)
- lazy init + add livecode.org (`d78da55f`)
- cleanup (`f599088c`)
- clean up tunes + also load presets.orc (`5727c0dc`)
- allow defining csound on the window (`8e531f40`)
- Merge remote-tracking branch 'origin/main' into csound (`3fa3ebee`)
- delete strudel unrealted example (`e4ee4211`)

## Ноябрь 2022

### ✨ Новые возможности

- can now set a string parser for reify (`ea3e9de7`)
- can now load samples via url (`cf7c7347`)
- support github: links (`d7b82eda`)

### 🐛 Исправления

- #194 (`83049934`)
- could play multiple mini repl at once (`f1ae8a17`)
- each example (`f279c617`)
- rootNotes example (`83263968`)
- worklets now work in the tutorial again (`2293f1ba`)
- snapshot (`87463305`)
- highlighting error (`2341452d`)
- highlighting sync (`372bdf8b`)
- web midi (`eca92cf5`)
- hash urls (`dde7dff8`)
- tests (`c93fd900`)
- samples function did not work in tests (`86627751`)
- mini repl test tune (`6129f9af`)
- osc notes (`e3bc63e4`)
- test (`4f304659`)

### 📦 Прочее

- baasic csound output (`43aa2247`)
- baasic csound output (`eab86840`)
- fix typo (`729e0afd`)
- improve csound bindings: - better timing - can now pass instrument to .csound(...) - can now write instruments with csound(...) (`c40078ff`)
- mock csound (`0a098e30`)
- tiny time improvement (`517ada6d`)
- update tonal, fixes #269 (`6c0b86d8`)
- add csound minimal example (`ac2aa7f2`)
- continue tutorial (`e6210204`)
- update tonal: not working... (`afe17a50`)
- add eslint + fix lint errors (`e2898ee5`)
- lint before test (`611c4299`)
- optional eval callbacks (`4e3e533e`)
- add without-audio example (`f9bf2122`)
- simplify without-audio example (`805bd7e9`)
- Tidying up core (#256) (`e1a53250`)
- fix tune snapshots (`de19f3e5`)
- fix examples snapshot (`54c9c434`)
- fix sample examples (`94a594c7`)
- add s for sharp (fixes notes example) (`9717c696`)
- midi now works in the tutorial (`483c3f61`)
- Publish (`4ec1145d`)
- weird audion bug (`4c838aea`)
- package-lock (`d3f6a5c2`)
- show intro only when coming to root url (`c93faa95`)
- prevent flicker (`c7229048`)
- move draw stuff from tone to core + fix getPhase (`e769ed2f`)
- use new transpiler in test runtime (`122c4242`)
- fix first highlights (`d957b19f`)
- separate header + footer components (`21503c0c`)
- fix activeFooter state (`aa76419e`)
- proper draw cleanup (`8f31fbe2`)
- better draw cleanup + began reference tab (`c92cb1c0`)
- secret zen mode (`1b8ec1f0`)
- remove hideHeader function (`45b5b596`)
- floating error message (`7ab263b5`)
- remove refactoring notes (`839ba31d`)
- webaudio main file (`1507724e`)
- Publish (`4f570607`)
- package-lock (`08f207a3`)
- remove webaudio build for now (`418f09e3`)
- Publish (`e73044c5`)
- fix prebuild (`2e8a1b67`)
- fix share #261 (`4313fe67`)
- logger works now outside of react + dynamic sample loading logs + remove old sampler code (`0b9d59bf`)
- remove onLog callbacks (`27fb2d2d`)
- can now have multiple triggers + Pattern.log now makes sense (`ff5d14fb`)
- tabbed footer (`a27f399a`)
- basic samples tab (`5fc8f106`)
- refactor onTrigger (`b668a2c0`)
- basic intro tab (`04f2b905`)
- unfriend intro readers (`ce6495a4`)
- big style update + new log system (`45c7b29a`)
- scheduler error handling + style (`23e059a0`)
- Merge remote-tracking branch 'origin/main' into binaries (`f0b099d9`)
- revert server changes (`64275cfd`)
- started refac repl to new scheduler + transpiler (`14c2da4f`)
- fix mini repl + improve repl api (`c7c90b06`)
- add logger (`25ffc9cf`)
- fix core log (`ff3bc0e8`)
- fix logger (`42fedec0`)
- pass all params to onTrigger (`00550dda`)
- init audio on first click to remove warning (`b688b892`)
- add sourcemaps (`66c444ef`)
- cleaner error when midi is unsupported (`823ab735`)
- only connect via osc when .osc is called (`1ba4d3d5`)
- remove log (`22711dd7`)
- lazy midi init + remove useWebMidi hook (`b5c51e23`)
- scheduler error handling (`1a1363ef`)
- update todos (`3e6c3fda`)
- use new transpiler in vite-vanilla-repl (`4ed3ca41`)
- test with old transpiler for now (`16659c82`)
- core evaluate now works without transpiler (`2c7cc5d3`)
- support line,column,offset format (`f37eb92d`)
- use new transpiler in nano-repl (`a761aa9a`)
- vanilla-repl: use mini for all strings + skip transpiler (`dff0c0c3`)
- use tune that works without transpiler (`168ac608`)
- add transpiler readme (`30b18fc9`)
- Publish (`89acc487`)
- fix chord-voicings dependency (`99f416bf`)
- Publish (`3ec2fc60`)
- add webaudio cjs + esm build task (`431e1b87`)
- Publish (`f92d9994`)
- fix webaudio main file + add buildless repl (`51b14d55`)
- add amensister (`38b3337d`)
- Merge remote-tracking branch 'origin/main' into some-tunes (`da8e00eb`)
- Merge remote-tracking branch 'origin/main' into general-purpose-scheduler (`0485632e`)
- move evaluate logic without transpiler to core + breaking change: evalScope is now imported from core + breaking change: deprecated extend is now removed + add repl.mjs (`371af755`)
- add vite vanilla js repl example (`09045d53`)
- add vanilla repl build (`5f985f35`)
- rebuild with correct base (`5fb36acb`)
- add vanilla repl link to githack (`33cb506c`)
- remove old clockworker + scheduler (`7d47f5ab`)
- revert lerna upgrade (`2602ff6e`)
- Publish (`60ce34af`)
- Publish (`4e51f197`)
- npm ignore examples folder (`dd89e116`)
- bump core 0.3.0 (`5c01874a`)
- npmignore react examples (`57e373f5`)
- Publish (`b1ebf636`)
- core dependencies (`2be5bca4`)
- Publish (`fada7fc9`)
- transpiler package to replace eval package (`9b2899c8`)
- update plugin-react + osc-js (`1ac99e26`)
- update lerna (`2a6ffbf5`)
- add scheduled example (`96b7b689`)
- update shift packages (`84d34aa1`)
- dbdump script (`e5f38dbb`)
- replace old snippet (`89434fc3`)
- code format (`4fc7ef69`)
- remove cross import + code format (`67ee493e`)
- export controls from core root (`698f560d`)
- improve readmes (`96c49689`)
- revert auto formatted snippet (`230e8e9d`)
- update browserslist (`bdc321df`)
- use only node 18 to save some energy (`854692f9`)
- update webmidi + remove tone piano (`a677b942`)
- update codemirror (`a40a0f73`)
- update tailwind + react (`54b847a8`)
- package-lock (`5bbec7b1`)
- update vite in react package (`b3721618`)
- update vite in repl (`a191b1e8`)
- update more things (`78d7cdf6`)
- update tailwind (`519062c8`)
- #241 (`1a6743db`)
- test all in source examples (`e9544f8f`)
- render doc.json before testing (`5c2a8a68`)
- remove log (`3033acad`)
- add shared tunes test (`ed44f4ed`)
- all shared tests now pass (`37c45424`)
- exclude shared test for now (`5534db95`)
- refactor prebake (`d8843e60`)
- Merge remote-tracking branch 'origin/tidal-drum-machines' into some-tunes (`48fc68bf`)
- use new bank function (`97db8953`)
- flat rave (`2168dbc6`)
- support object arithmetic (`e128b02d`)
- get rid of asNumber on arithmetic functions (`fbf7c629`)
- simplify asNumber (`a0bd5bdd`)

## Октябрь 2022

### 🐛 Исправления

- multichannel worklets (`335d54b5`)
- reverb mono (`dadb8119`)

### 📦 Прочее

- 808 + 909 (`324cffe6`)
- drum demo (`566c358e`)
- add all drum machines + soundbank helper (`bdbb4cb4`)
- make bank a normal param (`0c5bdd02`)
- move sample data to json files (`caf58492`)
- add vcsl sample library (`7ef3d33d`)
- note default to 36 (`4d60a582`)
- add sample demo tune (`8033cfc4`)
- Merge remote-tracking branch 'origin/main' into some-tunes (`50f88798`)
- little fluty tune (`0fd85199`)
- started removing primitive haps from tutorial (`5d7e46b2`)
- add note calls everywhere (`08c4c641`)
- more fixes (`e38513f0`)
- Merge remote-tracking branch 'origin/main' into general-purpose-scheduler (`7e7e299b`)
- missing note calls in some tunes (`fbb3d144`)
- add two tunes (`d45c3803`)
- Fix zero length queries WIP (#234) (`24328ee8`)
- support primitive notes + tame default gain (`6af68135`)
- use webaudio output by default (`5c230c2d`)
- remove all occurrences of .out() (`ecc4e180`)
- remove webdirt + tone from tutorial + do not load webdirt in repl (`4264f013`)
- different default adsr for synths and samples (`699c8d5b`)
- remove old defaultSynth (`60616a52`)
- add baseDir to prebake + obj params (`db1fc399`)
- clearer error (`ecfeac4d`)
- fix sample name (`dac906a7`)
- in source value modifiers (`603c55fd`)
- refactor remaining usages of tone (`053e6d83`)
- update snapshot (`2c14514c`)
- Patternify range (#231) (`5f381cf1`)
- Core util tests (#226) (`113adcc5`)
- fix fastgap for events that go across cycle boundaries (#225) (`a5bf8f67`)

## Сентябрь 2022

### 🐛 Исправления

- remove TLA (`762a17d9`)
- tests (`a14c7233`)
- formatting (`fdc4bb45`)
- support numbers in piano / sampler (`42922a0d`)

### 📦 Прочее

- really simple reverb (`933ef02d`)
- more tunes (`0e7a0b57`)
- snaps (`c5342d3e`)
- roomsize (`5018669d`)
- Merge remote-tracking branch 'origin/main' into just-another-docs-branch (`985de366`)
- Merge remote-tracking branch 'origin/main' into general-purpose-scheduler (`e715e2ba`)
- Merge remote-tracking branch 'origin/main' into reafactor-tunes (`a8b02798`)
- improve chop + loopAt examples (`b64cf197`)
- euclid + euclidLegato (`69ce7c78`)
- legato gain velocity (`04973d15`)
- add reverb link (`4b083d6f`)
- begin end (`c4b48263`)
- delay as global send via orbit (`f76d03a0`)
- clean up getDelay args (`18bd10df`)
- support negative speeds (`9ebada63`)
- focus tweak for squeezeJoin - another go at fixing #216 (#221) (`513e0d74`)
- adjust use of focus in squeezeJoin - fixes #216 (`5b56fbed`)
- sample adsr support (`014c258c`)
- adjust default envelope (`91934006`)
- update snapshots (`d21734cc`)
- Merge remote-tracking branch 'origin/HEAD' into feedbackdelay (`5c99b72e`)
- Merge remote-tracking branch 'origin/HEAD' into just-another-docs-branch (`41513985`)
- Merge remote-tracking branch 'origin/main' into reafactor-tunes (`ead763b9`)
- encapsulate out + encapsulate destination + error handling to simplify merge scheduler branch (`6dbb2cb9`)
- Merge commit '6dbb2cb98a41edc6ad6b3458a6f7067044cb66cd' into general-purpose-scheduler (`0c152923`)
- migrate to encapsulated out (`d092068b`)
- getDestination collides with tone -> do not export for now (not needed) -> can be refactored when tone is removed -> also prevent loading worklets in node (`ca827960`)
- tutorial is now dark (`0d179f20`)
- chop + loopAt (`df2c7ec1`)
- attempt at fixing #216 (`dfdd918e`)
- fix sample slicing + unit + nudge (`819abbfa`)
- cut + loop (`8d5682de`)
- cut precision (`7f6940f3`)
- try build workflow (`6d3d9659`)
- add rollup-plugin-visualizer in root pkg (`6c363d9c`)
- add npm i in repl folder (`88d12223`)
- npm i inside tutorial (`8ab91242`)
- deploy after build (`1da005e8`)
- add update button (`0134a50f`)
- next try (`a09ec6b0`)
- rename deploy workflow (`1e827352`)
- fix workflow (`a679e47c`)
- wait for build before deploy (`0c3777f7`)
- do everything in one step (`ec823ade`)
- fix deployment permissions (`476b51ee`)
- echo + echoWith (`cc394d9f`)
- iter (`abe28367`)
- iterBack (`e4f4fe81`)
- chunk + chunkBack (`5d699496`)
- add missing params to echo (`227ad332`)
- add feedback delay effect (`1eb6ff8c`)
- improve adsr (`3a65bc06`)
- refactor caverave + throw out some tunes (`dbb05762`)
- fix tests (`d3645f6c`)
- revert exponential adsr (`8977c5ce`)
- load worklets on startup (`acf339ad`)
- remove headings for generated doc (`71ca46b2`)
- web audio effects (`cafcead6`)
- proper disconnect onend (`1ccc391c`)
- rand irand perlin (`dd69e4ca`)
- early, late, each, every, rev + doc structure (`b38b16f7`)
- dedupe most of core api doc (`19b8f073`)
- dedupe accumulation modifiers (`dfd33bab`)
- mark tone + webdirt deprecated + add superdirt param list (`e4913dfb`)
- make polyrhythm alias of stack #211 (`c786d77b`)
- remove empty heading (`505cc01a`)
- add coarse, crush and shape (`b9173ebe`)
- add license info (`5d28c5c8`)
- out should not be async (`a11d23ba`)
- disconnect chain after use (`3fdfd124`)
- fix build: dont add sourcemaps (`dd4464cb`)
- add all default samples to mini repl (`782e8522`)
- comment out timeout: it will cut of samples that are longer than hap.. (`3c8acf9e`)
- begin webaudio tutorial (`e58aff41`)
- improve sampler section (`de4726ec`)
- ignore stats (`0ffbb5cb`)
- add vowel (`4406dd88`)
- make vowel work with node (`149b2ab9`)
- refactor kalimba (`0a1577ff`)
- add drums to mini repl (`88e39d22`)
- degradeBy (`8e61673b`)
- add missing external deps to react pkg (`a022e5bb`)
- random functions (`ce91d36b`)
- chooseCycles + mini shorthands (`f0e412a4`)
- add dependencies graphic (`d1cedaa3`)
- Create README.md (`1441374f`)
- add build analyzer (`96f49f11`)
- regression of #196 - samples loaded as arrays would not be repitched (`a5f05b47`)
- package-lock (`5b721f64`)
- pull in midi logic (`59e16642`)

## Август 2022

### 🐛 Исправления

- runtime error reify undefined (`48bf1761`)
- jsdoc comments (`f5efa18e`)

### 📦 Прочее

- another round of scheduling (`4f8a2a09`)
- fix scheduler again.. add comments (`ec60fe42`)
- scheduler is now finally tight (`9059c1b8`)
- cleanup (`5be1a700`)
- no top level await + no evaluate on mount (`11a9f3ce`)
- fix nano-repl prod build (`4dfc3451`)
- fix pause + remove logs (`cd1203d1`)
- expose pattern form useStrudel (`29029714`)
- getTime callback for highlighting time (`7b0c80c8`)
- use new getTime callback in repl + mini repl (`ec4cfe6f`)
- build react pkg (`acafcc98`)
- nano repl now supports highlighting (`24f8f48a`)
- fix latency issues for larger intervals (`d0b07c48`)
- add nano-repl build (`067c3433`)
- add a little dot (`9a2a26e9`)
- scheduler error correction failed attempt (`27593196`)
- scheduler error correction via getTime (`2172d1a7`)
- merge evaluator and scheduler hooks + use getTime (`9497c99e`)
- wait for ctx.resume (`81039433`)
- build react package (`e37e0f18`)
- nano-repl add modules + proper eval (`e3e3a840`)
- better default tune + fix dirty state (`04502b05`)
- add master gain + panic (`75e57c75`)
- pause action + started flag (`9c7d0951`)
- add tailwind (`5f4d8f68`)
- add useKeydown helper (`a7562c38`)
- add codemirror + keyboard shortcuts (`0c08919b`)
- webaudio error handling (`5ea2124e`)
- comment out stuff that uses tone.js (`e72c4786`)
- fix getFrequency for .note (`1cd31cfc`)
- move scheduler to core + move audioctx to userland (`7af6188a`)
- working default tune (`59185591`)
- scheduler error callback (`e77129fa`)
- export webaudio out as separate function (`ee84b4e0`)
- add hooks for scheduler and evaluator (`bc345b56`)
- export new hooks (`d667e790`)
- package-lock (`46aa3d27`)
- add react nano-repl example (`1941b1ff`)
- fix #178 (`4e09cc14`)
- wait for prebake to finish before evaluating (`0e185b73`)
- support old way of setting frequencies (`48526824`)
- add soundfont2 dependency (`a08325c9`)
- Publish (`e9e25ef8`)
- bump soundfont package for rerelease (`0522996b`)
- add npte for new packages (`bdf2047e`)
- bump to 0.0.4 (`227dda92`)
- Merge remote-tracking branch 'origin/HEAD' into optimize-scheduler (`57f84748`)
- remove cps-sketch build (`98f4d4de`)
- Merge remote-tracking branch 'origin/main' into soundfont-file-support (`365a90e7`)
- memo all things codemirror (`f34d0a29`)
- change "stride"/"offset" of successive degradeBy/chooseIn (`58dc46b4`)
- import from modules (`2e4a3516`)
- test build (`86a68d78`)
- remove lastEnd (`ea1ffca4`)
- use phase to allow smooth tempo changes (`c2354bff`)
- build example (`4b294c4a`)
- allow changing cps from a hap (`eaf6617a`)
- rebuild example for cps control (`af585588`)
- relative script src (`aff81906`)
- work on scheduler: - simplify clockworker (no audioContext) - tick based scheduler with cps - support cps in .out (`9478915c`)
- change theme (`e71fec0e`)
- vitest config via file (`c1255bfa`)
- use vitest snapshot feature for tunes (`f955d77d`)
- Merge remote-tracking branch 'origin/HEAD' into vitest (`29009e14`)
- migrate mini test (`8b088ab6`)
- test with evalScope instead of extend (`543d5f06`)
- extend -> evalScope in runtime (`dc3a873a`)
- move tunes test to vitest (`22c60fb8`)
- bump vitest + remove mocha (`17d0b7d2`)
- make tests a little bit faster (`235e82a3`)
- dont use beforeAll (`2f1d9631`)
- try with isolate + add task for ui (`27b3a408`)
- disable isolate (`9aaeee20`)
- add contributor avatars (`bf3e90ba`)
- prebake flag to skip fetch (`8244ba77`)
- handle objects in getPlayableNoteValue (`467b3db8`)
- add sfumato soundfont player (`8bd56d29`)
- cache loaded soundfonts (`3986c407`)
- add vitest (`ffc0b364`)
- refactor core util.test (`a4158c16`)
- refactor value.test (`90e5e035`)
- refactor drawLine + fraction test (`3a790b03`)
- refactor pattern tests (`a15d8553`)
- vitest eval + mini (`765f2df1`)
- test tonal tone xen (`03158bd6`)
- replace old tests + ignore tune test for now + add happy-dom (`488ebf8f`)
- remove mocha from packages (`563dfbea`)
- readd mocha dep to root package (`b8843a0e`)
- run all tests at once (`7a4a8a45`)
- Merge remote-tracking branch 'origin/main' into soft-errors (`684bab99`)
- always throw error for invalid notes (`8d1c98f0`)
- add a non-probabilistic test for the ? operator (`1227d989`)
- add a "tune" and snapshot using the (`Bradford Powell`)
- fixes #130 + #156 (`decd2d66`)
- replace react-codemirror (`93251582`)
- fix style import (`1942c364`)
- tutorial styles (`0cd82c7e`)
- set background to 222 + remove log (`67c21c18`)
- remove old overrides (`7f0e98ea`)
- add onSelectionChange callback (`55b2d7f6`)
- Amend shapeshifter to allow use of dynamic import (`130fb9c2`)
- Merge remote-tracking branch 'origin/HEAD' into talk-fixes (`655a9ff1`)
- darken repl background + dont highlight active line (`800f2f46`)
- Change offset for "stateful random" streams (`f533e4a5`)

## Июль 2022

### 🐛 Исправления

- draw playhead only once (`1ddf9687`)

### 📦 Прочее

- update krill-parser for the changes to .pegjs file (`8988408d`)
- add chooseInWith/chooseCycles (`d79716c3`)
- chooseInWith, choose2, randcat, and some reformatting (`7fbd4527`)
- style fixes + hide-console option - thicker highlighting outline - only shade actual characters, not whole lines - strudel hide-console will now hide console (`913416a9`)
- style errors, fixed backdrop + hide console (`4f460eeb`)
- .out error handling (`712a2ae0`)
- darken mini repl bg (`884811fd`)
- implement "?" operator in minilanguage (for degradeBy) (`964ee689`)
- implement random choice in mini notation (`f858d89f`)
- pianoroll optimizations - fix flickering due to global alpha mutation - change default inactive color - add smear option - add playheadColor option (`f3871800`)
- log helpers (`a42ebd7a`)
- minirepl optimizations: - can now evaluate on init - support disable-highlighting - support keyboard shortcuts - support flash (`ce4d2c17`)
- getFreq + midi2note helpers (`fd61001b`)
- remove edit and pipe function (duplicate of layer and apply) (`82728be4`)
- add Euclidian, remove append (`bebfa23c`)
- added json assertion (`b11003c4`)
- use fetch instead of import (`669fce36`)
- added flag for node 16 LTS compatibility (`6728abf5`)
- added EMuSP12 samples to prebake (`611d216b`)

## Июнь 2022

### 🐛 Исправления

- WebDirt import (`8e68b4e0`)
- #122 was still not fixed for async patterns (`5cd80b58`)
- #108 (`307afa5c`)
- #122 ctrl enter would add newline (`81c20e67`)

### 📦 Прочее

- rename choke -> clip (`55f91143`)
- rerun snapshots (`2a18b271`)
- webaudio optimizations - samples with obejct format for pitch declaration - support note to repitch samples - support choke to fit samples to hap duration - support "legacy" context.velocity in .out - support ":" inside s or note to set n - fix sample fadeout for soundfonts and choke - move gain before filters (`c1ce7246`)
- add build:parser script once more.. (`39c2cf6c`)
- support colon sign as normal char (`a89fa766`)
- support "s" as "#" alternative inside note names (`dd3f4e47`)
- draw note property if set (`fe9a6e8f`)
- add prebake script with piano (`5791f12a`)
- tame oscillator gain (`e98829c9`)
- fix single sampels (`42343254`)
- migrate underground plumber (`4e282dac`)
- fix soundfonts + samples without base (`720b1eeb`)
- migrate tone piano to own piano (`7048ea47`)
- fix splitSN (`e29eaf37`)
- log core version + add warning to isPattern (`196d9db3`)
- add tune (`f8ada70f`)
- style version log + check if already loaded (`a437c698`)
- load piano samples locally (`b55067ad`)
- fix tests (`cf564a7d`)
- can now generate short link for sharing (`4039b962`)
- refactor share for build without top level await (`eca76dd7`)
- small fixes (`81e90cd4`)
- build (`70cc0b93`)
- flash effect on ctrl enter (`b9829bb6`)
- paper updates (`a2402847`)
- add sound chapter, trim down future chapter (`59b46db8`)
- improved ciations (`b14d0926`)
- improved ciations (`4d3bdfc5`)
- build (`00997408`)
- more citations (`80bd3be0`)
- Publish (`f0a934e0`)
- add tune (`5b0e31ac`)
- remove old code (`69810303`)
- add created param to controls (`e1673001`)
- no need to involve controls[name] (`f6c04f9e`)
- remove async from serial, default baudrate to 38400 (`aee9bf7c`)
- Publish (`e2eab1ee`)
- object support for pianoroll (`64f717f8`)
- remove hackery (`f2bd9fae`)
- remove console logging (`a0618f7f`)
- bump (`328e4db1`)
- began soundfont implementation (`82d61039`)
- Publish (`3ac592b1`)
- fix looping fonts (`d5938a01`)
- load fonts from forked version (`38b1717f`)
- remove unused sf control (`319400ab`)
- no sound in ios webkit (`5334abeb`)
- began soundfonts package (`b353db67`)
- fix snapshots for objects (`5fd88f92`)
- regenerate snapshots - update webaudio ones (`c1e934d1`)
- dont use tone audio context (`faa4019b`)
- use closures to simplify api (`44574af3`)
- move sampler to webaudio + add basic sample playback to .out (`d532f800`)
- support speed, begin and end (`71b6b83a`)
- add top-level chop (`c191accc`)
- fix cracks + add tune (`925fb30e`)
- add snapshot (`c3a3a680`)
- add comments to tune (`9eb32668`)
- simplify url (`18f1f7d8`)
- update tune (`774c02af`)
- cache gist by default for faster eval (`ecd93c9a`)
- allow _base prop for samples loader (`ad62ccd8`)
- hackery (`23be3e5b`)
- rewrite webaudio + migrate tunes + empty setters (`4c058760`)
- add new tune (`a4a30e8b`)
- add controls to tune test (`2bb095d4`)
- Update README.md (`3d68b0c5`)
- Update README.md (`11e33f5c`)
- add tutorial to setup (`8dee0708`)
- package locks (`c3f03f50`)
- Update README.md (`729b4e18`)
- Update README.md (`badc4bcc`)
- funky serialisation for serial (`34ba762b`)
- automatically run onTrigger in scheduler (`87de8c08`)
- can now set latency on the scheduler (`25d6ff1c`)
- remove cycle + delta from onTrigger (`cac5d96c`)
- add onTrigger helper (`3e1e92a6`)
- prepare webaudio rewrite (`f69d776c`)
- declare from/to with let, as they can be changed (`a479a574`)
- re-add serial (`affce4a4`)
- node 16 compat (`fb8dd537`)
- ignore samples (`4b9a6ffe`)
- more flexible pianoroll (`c7fdf524`)
- autorange + fold + vertical (`394e77c1`)
- replace from to with cycles / playhead (`ba5cf5ba`)
- flipTime + flipValues (`ac39a609`)
- hideNegative flag (`68814dd9`)
- disable autorange by default (`b3d2d302`)
- remove old comments (`6d6784d4`)
- Update CONTRIBUTING.md (`cbb59eaf`)
- Update CONTRIBUTING.md (`36a401c6`)
- Merge remote-tracking branch 'origin/HEAD' into webdirt (`e8614425`)
- delete doc.json (build artifact) (`6306cc9c`)

## Май 2022

### 📦 Прочее

- add samples helper + dont play samples after load (`5c6c5c96`)
- minor style tweaks (`92dc50e3`)
- add webdirt with some default samples (`f9f1b551`)
- pass all value props to webdirt + list available (`e1fb0acf`)
- Merge commit 'd132a9bec58f40ccdea411ec971a0343aa1b0743' into webdirt (`822659f7`)
- remove splitLanes from branch (`a996a704`)
- support webdirt in minirepl + add section in tutorial (`17fa8d90`)
- hopefully fix sample folder for deployed version (`093049a1`)
- link webdirt - superdirt controls (`ea6d728b`)
- rename readme (`052159ce`)
- Update README.md (`9147b4e3`)
- basic sample loader from github (`91dfef52`)
- save sample list to local storage (`7274a455`)
- fix cache + add cache reset (`e27b8aeb`)
- document non random signal functions (`93e565f8`)
- add dummy function with todo (`15b32aba`)
- add build script for binaries (`8c9e1782`)
- add missing osc import (`69703072`)
- small fixes (`b5b0fbbd`)
- can now generate mdx from nunjucks (`4acdabe4`)
- use tutorial.mdx with nunjucks (`cad24714`)
- delete old mdx (`e7fe1ab5`)
- use nunjucks filters (`cc1a61d6`)
- add superdirt params (`7250824e`)
- reorder stuff (`8a812dd8`)
- document more controls (`68a3842c`)
- document more controls (`58104b5a`)
- even more control doc + more logical grouping (`4c4d0b42`)
- more doc (`4be798d1`)
- dont render attack yet (`b2743108`)
- add n + todos (`8646410e`)
- even more doc (`b0c6236c`)
- doc (`1b8a12dd`)
- hide api doc by default (`2680eed6`)
- tonal jsdoc (`f2b5dcf4`)
- jsdoc voicings (`a9f5bd67`)
- fix repl imports (`a8672ec4`)
- test control doc (`987cc637`)
- add missing styles + fix demo (`7eeae429`)
- Publish (`96825f9f`)
- remove now redundant style (`cb13eb7f`)
- bump core deps (`d2f47262`)
- Publish (`2d2c671e`)
- delete docs as now deploying to gh-pages branch (`4c49e307`)
- Merge remote-tracking branch 'origin/main' into in-source-doc (`b50daed0`)
- add autogenerated api doc to bottom of tutorial (`f2a70396`)
- autogenerate doc on build tutorial (`6b26f999`)
- hideOutsideView flag (`ae81f6d5`)
- tutorial toc (`c643f105`)
- Publish (`87162a9f`)
- revert unpublished versions (`73d8afd6`)
- fix cp error (`43a0c7e6`)
- messed up everything (`b7a23868`)
- Publish (`a29c5f1b`)
- small fixes (`43c2f461`)
- fix tests (`788a4c91`)
- hide todo (`ab1dd278`)
- add basic readme (`c41ac9a4`)
- delete react logo (`bbf05650`)
- remove old todo (`f31a5a4b`)
- add todos (`666cbf65`)
- fix deploy out (`f34301b4`)
- clear out before build (`44ba1f85`)
- add barrel exports (`9fa01aa6`)
- tone barrel export (`aa3db09f`)
- add evalScope + deprecate extend (`2a618b42`)
- basic react package build config (`b59f36f4`)
- unify barrel exports (`fb2f63ee`)
- react packaging (`b8a05590`)
- package meta (`605cb98b`)
- add description (`b7fac65a`)
- add missing deps (`0bcc01ed`)
- migrate repl + move imports out of minirepl (`ed35f967`)
- new build script + fix draw (`806c8925`)
- fix favicon (`6c6ea3ab`)
- migrate public files + add deploy script (`fdb60a3f`)
- use Function instead of eval - fixes scope issues (e.g. seq duplication) - should be much safer and much faster (`273cb81d`)
- loopAt (`ee5bc0f3`)
- Fix osc timing, and send cps, cycle and delta (`f2e1ee1a`)
- fix asNumber (`55d5cc93`)
- Merge remote-tracking branch 'origin/main' into repl-package (`999251e1`)
- basic react package (`0d63ec91`)
- use vite for repl (`645ea30b`)
- migrate missing files + fix tests (`92c17d63`)
- migrate tutorial (`42d3510c`)
- pull out tutorial (`446ddc2c`)
- fix name collision (`1cbf4c8c`)
- Implement brak() (`4fd843f8`)
- brak() fix (`a8d67664`)
- Implement inside/outside (`aca3bea5`)
- drawLine (`f5e5ef86`)
- euclid (`608283ba`)
- fix run-license call (`6dedeae6`)
- build (`73b0a3a0`)
- document some mathematical pattern methods (`d5a1832f`)
- add license to those extracted during javascript minification (temporary hack) (`b0dfb44e`)
- standardise on hap rather than event, especially in function names (`f8f744dd`)
- more docs (`5419b089`)
- add createParam + createParams (`e7def57c`)
- embedded styles (`8034eb14`)
- local testing comments (`8b4af5ce`)
- configure jsdoc to parse all package files (`56962196`)
- build (`cdc068bb`)
- Merge remote-tracking branch 'origin/main' into in-source-doc (`2739c894`)
- support old 'silent' parameter, renamed as 'dropfails' (`4779dd13`)
- Support string concatenation for add() and variants (`6273b342`)
- Merge remote-tracking branch 'origin/main' into embed (`d752d0eb`)
- reflect licence change to AGPL (`c23f9b86`)
- Add link to packages/serial (`90b74a72`)
- Prettify (`53d82e51`)
- add basic embed package (`b01865f0`)
- fix example (`6e8bd1ff`)
- bump (`91261b8b`)
- Update README.md (`25bfdd80`)
- Merge remote-tracking branch 'origin/HEAD' into in-source-doc (`339e91b0`)
- try out jsdoc-json (`e0c77a5f`)
- patch holes in highlighting query span + make sure disable-highlighting works again (`bdb5dabb`)
- handle edge case (`29fceff9`)
- build (`f469e489`)
- test jsdoc (`2348d056`)
- More composer tests (`41b7939d`)
- Use this.duration for things (`b2d58ce0`)
- Rename TrigZero -> Trigzero, + more composer tests (`065f4191`)
- try adding some pattern docs (`f81190bf`)
- add showWhole (`3eebccd5`)
- basic snapshot test for tunes (`6cac8ba1`)
- add repl test to root package (`5f26773c`)
- fix shoot (`c939a8a0`)
- more readable tune test + longer snaps (`a119b83b`)
- add cycles for all tunes (`38bb0ca0`)
- add bossa snapshot (`42269cf7`)
- Add ption for asNumber to return original event on fail. Make numerical composers use asNumber. (`469db7d5`)

## Апрель 2022

### 🐛 Исправления

- .wave (`0d1e7f5c`)
- .wave (`4b49c95e`)
- dont highlight out of range (`9e46b3cd`)
- cycleArg imprecise fraction operation (`72eeaf44`)
- core imports in scheduler (`dcb5af33`)
- disable highlighting (`f8cbd675`)

### 📦 Прочее

- reset -> trig, restart -> trigZero, then alias reset -> keepTrig, restart -> keepTrigZero (`026ae3c8`)
- highlight only events with onsets (`d4607c68`)
- Add binary composers (`e40bdad6`)
- binary composer fixes (`3fe70542`)
- use cm6 in tutorial + move highlighting to hook + optimize tutorial loading time (`fc9a9e33`)
- Trying a different naming scheme for composers (`86e8ed2a`)
- Tidy and add 'keepif' (`7af3c338`)
- Try without the special case for reset/restart (`f7dedd62`)
- remove old codemirror packages + styles (`cf922b03`)
- support custom dynamic highlight color (`4fcec829`)
- Merge remote-tracking branch 'origin/HEAD' into codemirror-6 (`a6dac6de`)
- filter out inactive events (`f6d12884`)
- add AGPL headers (`97a0fb3e`)
- redo headers (`ee514ee8`)
- Change to Affero GPL (`9ca49c58`)
- Work on Codemirror 6 highlighting (`ef42ca69`)
- Tweaks, more inspirations, and figure (`07b22a2c`)
- use codemirror 6 (`12967f28`)
- tiny bit of style (`10487683`)
- remove background color from cm6 theme (`5dea15ad`)
- fix z-index (`5aed046b`)
- fix some bugs (`069fff67`)
- match font size of old editor (`37218e87`)
- Merge remote-tracking branch 'origin/main' into codemirror-6 (`f97f8be5`)
- Merge remote-tracking branch 'origin/HEAD' into paper (`0c5903d8`)
- remove duplicate range (`d3a04071`)
- remove old patched osc-js (`8da4217d`)
- revert sound function (`7f8cdfa3`)
- add more shapeshifter flags (`0d325a3e`)
- support stack in drawLine (`dd03ad6c`)
- improve drawLine (`a0d6fc47`)
- test silence (`58a936cf`)
- fix restart overlap (`f96ca562`)
- fix drawLine polyrhythm (`283e5fde`)
- Experimental rejig of the pattern composers. Now `restart` is `setRestart`, use `constRestart` for the old behavior. Same for `reset`. (`5c3f095e`)
- Export unionWithObj (`a194b908`)
- Tidy up composers, supporting function patterns (`df5ffb47`)
- Prettify (`27ff3299`)
- Try 'keep' instead of 'const' (`60027e66`)
- Remove append, fixes #89 (`6a6464b0`)
- fix reset / restart default method (`23836e23`)
- Merge commit '47717e872b87f497990d7c1d6c3d54bfb96f2003' into paper (`a32d77b2`)
- future outlook + links (`dc66dab2`)
- fix citations, build (`d5957035`)
- Tweak copyright (`11f697fa`)
- tweak copyright (`602cac5a`)
- build (`fe13a7fe`)
- cat/append/fastcat/slowcat as pattern methods (`cc445e89`)
- support `cat`ting subpatterns, fixes #87 (`9df20fcb`)
- Support subpattern sequences in `stack` like `slowcat` in ref #87 (`a4cacc07`)
- Make sequence a `fastcat` alias and change `cat` to be a `slowcat` ref #87 (`4c43d3d8`)
- seq -> sequence shorthand ref #87 (`ff418c9f`)
- Fix evaluate test with new cat behaviour ref #87 (`615f71b0`)
- add todo (`ab987164`)
- add seq to factories for highlighting (`dd2552ed`)
- refactor cat -> seq (`f1c0d8c8`)
- migrate tutorial cat -> seq (`67bef933`)
- remove featurelist as it's now kept up to date in #31 (`e747c0b0`)
- log + drawLine (`9e2e5ce5`)
- refactor event.duration references (`766cccf3`)
- drawLine test (`2bbd306b`)
- Make set work for primative patterns, allow ops accept sequence (`b510ab03`)
- Tests for set, setFlip and setSqueeze (`d9e3ff32`)
- squeezeflip, tests for add including failing one.. (`97450316`)
- add tune + fix another (`52840f91`)
- Make comparisons rational in _compress (`2c224d50`)
- Extra test for ply (and prettify) (`87cfa305`)
- fix striate: union -> set (`8e441ecb`)
- build (`48d4541e`)
- use rational multiplication in echo (`c801660b`)
- fix the fix for echo (`1029f867`)
- Remove recursion from `mod` (`3742bf38`)
- Use mathematical `mod` in slowcat, fixes #85 (`903c065e`)
- test for negative time, ref #85 (`47717e87`)
- helper functions (`c38e4f72`)
- first take at reset (`0fae8b2b`)
- simplify (`ecfc6fd3`)
- rename to restart (`ab14d70c`)
- restart example (`08f8a7a1`)
- Add pattern composers, implements #82 (`b370c405`)
- Tidy test and console logs (`3ae6443c`)
- Remove old files (`884c13a2`)
- fix adsr + add example tune (`7dbdb005`)
- package-lock (`58bbbecb`)
- build (`36d1a68b`)
- First go at serial output (`b52c42c3`)
- Tidy up (`d7dc1588`)
- build (`56c3c046`)
- Merge remote-tracking branch 'origin/main' into webaudio-compat (`1778d19b`)
- add waa example (`e6c15dc3`)
- package-lock (`bcefce9d`)
- webaudio@0.0.6 (`83a46c68`)
- Merge commit '83a46c68dc6583ad36b256bc0d03e0913ecbc1f0' into webaudio-compat (`3d74b8a2`)
- remove logs (`c2feb6e2`)
- patternify filter (`1ead12a2`)
- respect midi numbers (`f8739e34`)
- experiment with passing time to withAudioNode (`6bba661e`)
- A couple of tests for continuous haps/patterns (`1c6e3dd9`)
- Fix steady() (`28ebf655`)
- Fix appLeft/appRight to discard nonmatching events (`30b74291`)
- build (`07a5ae83`)
- Make intersection_e actually raise an exception (`0b370bd4`)
- add `striate()` (`1d56d797`)
- add randomBells (`1d83ad52`)
- Merge remote-tracking branch 'origin/HEAD' into webaudio (`d0443a7f`)
- add example info (`2aed40d9`)
- add 'constant' (`544e32e0`)
- perlin noise (`f1ac9c44`)
- fix palindrome (`031b93f0`)
- tidy (`c88508ac`)
- add `degrade`/`degradeBy` (`dca8b46a`)
- undegrade, sometimes, sometimesBy, sometimesByPre, often, rarely, almostNever, almostAlways, never, always (`8abbaf2e`)
- undegrade, sometimes, sometimesBy, sometimesByPre, often, rarely, almostNever, almostAlways, never, always (`911d9d17`)
- build (`7adea0ac`)
- fix degrade/undegrade (`48945882`)
- build (`3d6f01c8`)
- Publish (`0e26d4e7`)
- core publish (`6c8fc6bb`)
- Publish (`0ce2dcac`)
- stub change (`27ebe7ba`)
- Publish (`86a3fd6e`)
- add core dependeny (`03cb80b7`)
- Publish (`5588b62b`)
- fix core dependency (`0ed1c75c`)
- use skypack in example (`2cf12940`)
- Publish (`d42cd6ef`)
- remove mini for now (`028af735`)
- add repl link (`803b5053`)
- Tweak applicative (`1274641f`)
- Add `someCycles` and `someCyclesBy` (`1c3793e2`)
- Make sure random numbers are positive (`c751d4eb`)
- build (`d394b493`)
- add preliminary getFrequency (`1b35dd0c`)
- test fromMidi + getFrequency (`a638f950`)
- remove tonejs experimental features (`9cc568fe`)
- remove experimental tone tutorial (`e0e3ab94`)
- make webaudio compatible with repl + rename osc to wave + migrate repl.html (`f210c442`)
- Fix `choose` and add weighted `wchoose` (`eccb20c3`)
- update task instructions (`233cc4bd`)
- initial demo submission (`89934886`)
- First effort at rand() (`3ae0762f`)
- Ignore continuous haps (those without a whole) (`b6740143`)
- add brand (boolean rand) and brandBy (`19f7dc16`)
- Add `choose`, fix `rand` to be unipolar. (`ea16d6b0`)
- fix notes example (`9d2cbbaa`)
- Merge remote-tracking branch 'origin/HEAD' into webaudio (`25103990`)
- remove old imports (`9c6652b1`)
- build (`c163039c`)
- rangex (`939bc643`)
- palindrome (`8fda84c1`)
- Merge remote-tracking branch 'origin/main' into webaudio (`192eb28a`)
- fix lockfile (`db20ff93`)
- npm i (`5629c0ee`)
- refactor scheduler constructor + add onEvent (`325eec2c`)
- add readme (`d718eb0b`)
- fix header (`ff32485f`)
- A bit more blah blah (`21e32856`)
- use new fixed version of osc-js package (`18d8049f`)
- package-lock (`45e8cd10`)
- add build script (`ed415653`)
- build (`14774ae6`)
- add speakerman (`8dde3456`)
- more citations (`f278c85f`)
- readme additions (`42e2531e`)
- fix server import (`6bdb0fee`)
- package-lock (`3cd10bf4`)
- add queryArc + update readme (`2060e868`)
- Prettify (`ad69fa19`)
- sort exports (`2797856f`)
- sort classes to top of export list (`fb826bed`)
- move some utilities to utils.mjs (`9eba8839`)
- Add `_zoom` and `linger` (`ec10f349`)
- Allow linger to accept non-fractions (`ec3371c4`)
- patternify zoom (`0df67fa9`)
- compress (`ca4bffe5`)
- build (`97044fee`)
- separate out strudel.mjs, make index.mjs aggregate module (`f75da0e2`)
- make separate signal module (`050296d0`)
- export patternify helpers (`16e2c97e`)
- add speak output (`1dd19c08`)
- Merge remote-tracking branch 'origin/main' into speak (`70fbbd65`)
- fix import (`38fe310a`)
- export patternify (`02ece118`)
- fix minirepl import (`61892bdf`)
- A bit of background (`023da097`)
- First run at squeezeBind, ref #32 (`357b0ee4`)
- Working squeezeBind (`c2cc6b6d`)
- Add ply() (`b10e0c3a`)
- rename inner <> outer bind and join (`8ea59067`)
- squeezeBind itself + tidying (`529d4483`)
- Prettify (`83c9200e`)
- Test and fix chop / squeezebind (`51db0eaf`)
- Patternify chop() (`e1995644`)
- patternify jux, add juxBy (`7c10d171`)
- rename range > listRange and remove from exports (`e75d8048`)
- add `range` (`0e5dc4f3`)
- Add range2 (`3c318917`)
- add root scripts (`c604f920`)
- use local osc.js (`324dcd7e`)
- more info (`57c1c4e0`)
- hard temporary fork of osc-js (`4111a12c`)
- osc instructions (`c9d7a2f3`)
- patch osc for client (`e069e669`)
- remove osc-js folder + remove umd stuff (`f9b043dc`)
- add setup script (`171f8450`)
- reference links (`7182894e`)
- fix citations (`0ad80c1a`)
- Update tutorial.mdx (`abfff8bc`)
- Update tutorial.mdx (`3f1df8fa`)
- add pipe + compose (`de3c4537`)
- use own compose (`9effc166`)
- remove ramda dep + update lockfiles (`81598aef`)
- fix "every" example (`7ca8c8d2`)
- just a little plumber tune (`4dbe3669`)
- some loose references (`914bc117`)
- Fix polymeter (`c596fe33`)
- Create CONTRIBUTING.md (`c6d7daba`)
- Merge remote-tracking branch 'origin/HEAD' into paper (`0f003abf`)
- Update featurelist.md (`0c61bb00`)
- dont create defaultSynth on start (`fd28cfc4`)
- some more sprinkles of text (`0e7b3450`)
- add .range (`fb1976a6`)
- dont create defaultSynth on start (`a242c065`)
- fix readme instructions (`f43ecf1a`)
- Publish (`80393bf7`)
- Merge remote-tracking branch 'origin/osc' into paper (`a54b6f03`)
- add sound synonm (`f698078f`)
- patch osc-js browser version (`05c900d2`)
- pattern comparison snippets (`95e4239f`)
- add shapeshifter snippets (`068b95ea`)
- add more shapeshifter flags (`b20467d6`)
- Make param args a sequence, remove some redundancy (`e28a4114`)
- move osc/superdirt to core/controls (`acca363a`)
- package-lock (`5a03386a`)
- controls (renamed from packages/osc/superdirt.mjs (`64ea9b7c`)
- add all the superdirt controls (`1fb1d15d`)
- newline (`7bbac0ed`)
- remove duplication (`c3b15fff`)
- fix absolute time (`86b1e992`)
- fix keybindings on safari (`0c726d83`)
- osc timing now hopefully works everywhere (`c8e9fe51`)
- begin webaudio package (`47a046ca`)
- workaround for osc-js timestamp bug, reduce latency (`ac01a621`)
- tidy (`bf7c8ceb`)
- move core repl + add package (`7fb9c3bf`)
- remove logs (`bfb757b3`)
- formatting (`723b239f`)
- formatting (`46b2e82b`)
- superdirt parameters (`89a7bd4e`)

## Март 2022

### 🐛 Исправления

- overwrite Tone.Pattern (`ca318375`)
- sometimes wouldnt start the first time (`72b963fb`)
- slow fractions (`682462ca`)
- replications (`73738acf`)
- empty options (`305b4e12`)

### 🧪 Тесты

- evaluate (`9120affd`)

### 📦 Прочее

- send actual osc parameters (`0b8919de`)
- prettify (`fb52227a`)
- Stop failing on no tests (`ba095d8e`)
- try to send osc as timestamped bundle (`0bfbd87b`)
- try sending osc in timestamped bundle with added latency (`d0d19778`)
- it seems the custom handler wasn't being used (`33026302`)
- add core examples (`ee0d835c`)
- Publish (`429fcaf0`)
- remove codesandbox specific line (`2a0d8c3f`)
- prettify strudel.mjs, plus add top level echo function (`f529d092`)
- Expain style and community a little bit. (`3c230986`)
- Add chunk, chunkBack and iterBack (`d2a715af`)
- basic communication with superdirt (`0be0347f`)
- enlarge button (`ed6cf2ec`)
- first (broken) attempt (`69884c61`)
- attempt to send an OSC message (`7fca7e6b`)
- test mini (`ff4beaeb`)
- rename org to strudel.cycles (`75097594`)
- rename main package json (`bac1a289`)
- v0.0.1 (`e05842cb`)
- independent mode (`65796002`)
- remove node 14 test (`c4d14594`)
- add install commands (`73996997`)
- repl readme (`3c123538`)
- add core example (`4142b113`)
- add eval example (`76822717`)
- add mini example (`1cf6b37e`)
- add tonal example (`f8f2fb60`)
- add duration getter (`9ef57152`)
- add missing dependency (`33c93264`)
- simplify onEvent call (`788f3440`)
- simplify import (`779d3035`)
- Publish (`11be5db6`)
- add publishing info (`4c277afe`)
- add tone example (`6d4a7f96`)
- add midi note (`4603d161`)
- remove old file (`2c636dfd`)
- fix cra lint errors (`a9c95bbb`)
- fix tutorial (`93db3cd9`)
- fix build path (`df996693`)
- add nojekyll + clear tutorial on rebuild (`5481919f`)
- use public dir (`55ff571d`)
- remove .nojekyll from root dir (`1812300f`)
- manifest (`8911326a`)
- build (`74e49aba`)
- add lerna bootstrap command (`81b6dede`)
- start packaging (`6f60a3a1`)
- add repl-react + fix deps (`6430f620`)
- fix more deps (`2338a1ee`)
- rename repl + fix eval for spread op (`e2eea912`)
- fix more deps (`edba815b`)
- import tone always from @strudel/tone (`65deb7df`)
- remove hooks package for now (`43d8f46e`)
- remove peer deps notes (`ef755908`)
- fix pattern checks for minified code (`9458faf5`)
- add xen package (`e1bb58fb`)
- replace relative imports with package imports (`0a8397b3`)
- basic readmes (`54d41410`)
- add extend to eval to control scope (`2f13e425`)
- metadata (`5dbe550c`)
- update setup instructions (`134d29d8`)
- add cross dependencies (`6f838c55`)
- tidy up, get citations by URL (`45a0f340`)
- began value api (`4b124e34`)
- added .color (`10bb347d`)
- add tune (`6d02f4c2`)
- build (`b3c853ee`)
- fix firefox keypress (`d417d5b3`)
- patternify echo + echoWith (`b1cde584`)
- build (`a19d7891`)
- fixes minirepl (`f83aba0d`)
- add stut + stutWith + iter (`68b5fc40`)
- build (`2441e701`)
- map pianoroll alpha to velocity (`8ebe4440`)
- add cpm (`c71be675`)
- build (`c0bd0966`)
- initial (`876e8be5`)
- integrate with WAC latex template (`f5228123`)
- integrate with WAC latex template (`e8f6825b`)
- draw above background (`2efbb686`)
- allow pianoroll bg to be transparent (`a02a48ac`)
- allow await + refactor locations (`98f91607`)
- build (`44878345`)
- can now reference midi output by index (`07aca218`)
- ignore mytunes (`9af6a779`)
- remove duplicate block (`5c0d9f6f`)
- add memoized version of fraction.js (`d13bfa92`)
- build (`77fafd40`)
- now running smooth without memoization (`61e81a4e`)
- build (`77fd516b`)
- revert un-memoization (`3e33427b`)
- build (`a3166dfb`)
- always stringify fraction param (`67c54c07`)
- fix euclid import (`aec5b3bb`)
- build (`3c56f24e`)
- cleanup constructor (`733994f9`)
- hidden 100s button (`679f30ab`)
- can now disable highlighting via comment (`6691afb9`)
- build (`8af4a92f`)
- add static playback button (`88bcfc5e`)
- build (`c263eac7`)
- remove pushLog + add more logging (`9e8f911f`)
- build (`aef7f7bb`)
- fixes mini repl (`b7a61cbd`)
- fix tests (always bootstrap on define) (`d973d56e`)
- build (`1e57526d`)
- proper reset (`bbc8ef00`)
- fix rootNotes with accidentals (`eaa250a9`)
- add euclid (`91c0a46b`)
- build (`15e49952`)
- add floor ceil and segment (`a3fa476c`)
- build (`c1a0f11c`)
- add euclidLegato (`6c6a7cf1`)
- patternified rootNotes (`e6944755`)
- build (`1182d0e4`)
- add ui helpers (`963cfe05`)
- fix piano velocity (`d8d460f1`)
- build (`4128213a`)
- build (`fe604ca2`)
- remove functions (`6fa78f83`)
- small style fixes (`947aecdd`)
- build (`d87049c7`)
- paren marking (`e5f46281`)
- remove functions (`252b7037`)
- filter out event parts from draw (`d5a928e6`)
- ignore sample names in draw (`b5bd9260`)
- add silent flag to asNumber (`5d8ccdff`)
- build (`225f6c77`)
- write more tutorial (`9644a54f`)
- build tutorial (`82b75e26`)
- comment out tunejs logs (`abb69828`)
- optimize draw logic (`2295ba75`)
- style repl (`984bd95c`)
- fix draw return + optional querying (`1558fd95`)
- fix draw (`78a2a47f`)
- build (`ffd6ab2c`)
- add lookaheadCycles to fix pianoroll (`92937fa7`)
- build (`04d14dea`)
- add draw helpers + pianoroll (`ffeaf7e0`)
- ignore oldtunes (`0b206a4d`)
- add draw cleanup (`1d1d29a1`)
- build (`cbd1bde7`)
- Update package.json (`29b3965e`)
- Update package.json (`ab37b80f`)
- add sampler + players + switchify onTrigger (`05804262`)
- add duration + legato (`7e0b6d7f`)
- add tunes with sampler + players (`d5c79f69`)
- build (`629d36c6`)
- add Pattern.velocity (`fbc629c6`)
- use velocity in tone + midi + defaultSynth (`58e8e311`)
- add velocity to example (`0a7b8317`)
- add kalimba to example (`68b481af`)
- build (`1d4129e8`)
- Merge remote-tracking branch 'origin/main' into notes-and-numbers (`af81626f`)
- add gist helper (`1dda16c0`)
- build (`76760fc8`)
- Merge remote-tracking branch 'origin/HEAD' into notes-and-numbers (`830f0a10`)
- build (`a2ab2b9d`)
- add Pattern.len (`52da8552`)
- add Pattern.round (`0ef62708`)
- remove scaleOffset nonsense (`29802b0f`)
- xen fixes (`43f861e2`)
- highlighting ideas (`a1fa306c`)
- add Pattern.tune with tunejs + example tune (`0792ef72`)
- fix formatting (`14ab6c59`)

## Февраль 2022

### 🐛 Исправления

- scaleTranspose for scales without C in it (`125d190f`)
- 💄 Enhance visualisation of the Tutorial on mobile (`4b778e44`)
- webmidi hints (`9f2bc146`)
- voicings wouldn't work with objects (`bc790d8c`)

### 🧪 Тесты

- 📦 Add missing dependency and a CI check, to prevent oversights ;p (`1217dea5`)

### 📦 Прочее

- comment out todos in tutorial (`63eb9124`)
- test util (`020c1d55`)
- test tonal methods + move mod to util (`df21c81b`)
- fix scales without c (`71952ca8`)
- Merge commit 'df0650e48d9aad4e72f501e6f480480c31a4aac0' into notes-and-numbers (`c4f31cb9`)
- Merge commit '63eb9124340ee1a650b5bf695493d025e4069a97' into notes-and-numbers (`9b3f77f4`)
- add tonal dev dep to root package (`1242b173`)
- add getPlayableNoteValue helper (`ffe258a9`)
- add xen! (`85f08868`)
- add .tuning + 12ji preset (`e78b3a88`)
- sync highlighting with tone draw (`1375932c`)
- fix mini repl draw + tutorial (`e719cea0`)
- use default context for now (`ef3ab616`)
- build (`9442b5c6`)
- build with setContext (highlighting broken..) (`70472f11`)
- lookAhead 1 (`4da1fa48`)
- Merge commit '1375932c05cea422500a9328fec3207ac4e1a64c' into stateful-events (`634d4f10`)
- patterns can now be async (`4f2f00ee`)
- piano (`31319b40`)
- "add" example (`f361b50f`)
- add pending flag (`5f424e71`)
- simplify (`f209841e`)
- added _asNumber + interprete numbers as midi (`8a7e4978`)
- fix tests (`420a9553`)
- fix uppercase notes (`ee2d3233`)
- install events (`df0650e4`)
- build (`827c9831`)
- Fix resolveState (`921b23e9`)
- Update test.yml (`6091f749`)
- Update README.md (`0d78f369`)
- Update README.md (`cefe9f76`)
- Update README.md (`1dd4c7ea`)
- Update README.md (`62fd2f59`)
- Update README.md (`c3ad7a03`)
- Update README.md (`54f052d6`)
- Update README.md (`cad81acc`)
- Add stateful flag to hap, add sketch of resolveState to hap. Also firstCycle now strips the context by default. (`34872b3a`)
- fix tests (`0969f474`)
- Add event/hap context (`912c36d5`)
- fix new things up to use state (`fc739d12`)
- _withContext() (`29bdfb2d`)
- comment out logging + add pipe for legacy... (`755e2dcb`)
- build (`9ccc78a9`)
- use state query in repl + move locations to context (`f1362b1c`)
- use context for scale (`a3971f79`)
- use context for tone (`08ab06c9`)
- simplify (`067c0bfe`)
- highlight voicings (`8b0e036e`)
- fix numbers (`dcebdd6d`)
- parse numbers (`4bea0710`)
- merge contexts (`50876727`)
- dont log (`867dbf33`)
- fix transpose number bug (`aeb51a15`)
- higher latencyHint (`83d0644f`)
- Fix show() to show sign, fix off() (`37a240ee`)
- repl build (`164d007f`)
- repl build (`1b5046da`)
- remove pipe + fix examples + add new tunes (`914d1211`)
- shorten (`c246bec7`)
- add featurelist (`56069a2d`)
- fix pure with number arg in shapeshifter (`8bf7a6ba`)
- fix off() (`b323855b`)
- Make pattern a function of state, not just time. (`a9a4cd2f`)
- add apply and layer, and missing div/mul methods (`14a4a449`)
- test div and mul (`fb149808`)
- patterns with double quotes and backticks + improved highlighting + many shapeshifter fixes + add highlighting to minirepl + add error handling to minirepl (`522005a7`)
- refactor tunes to double quotes (`f6be175a`)
- build (`413d4015`)
- replace groove with struct + delete groove (`9170eadb`)
- build (`b4b38f1d`)
- add location for replicated synthetic child (`26fe6c8f`)
- fix mini notation multiline highlight + started operator overloading (`3b248ae9`)
- build (`966de74d`)
- make offset pure (`fd5ac589`)
- build (`8ce99864`)
- add onEvent to useRepl (`72835c80`)
- add editorDidMount (`292f171f`)
- add locations to primitive pattern args (`ed50da52`)
- add withLocation (`cf5ae936`)
- highlight active events, js only (`8d872cce`)
- replace m with cat in tunes (`1de371e9`)
- build (`f8df4b48`)
- better number support in tonal functions (`74040e52`)
- specify error source (`358ba010`)
- started mini notation highlighting currently deactivated by addMiniLocations flag (`acd07c31`)
- Add continuous signals (sine, cosine, saw, etc) (`b4b303a2`)
- Tweaks and a couple of tests for signals (`fd8f70d9`)
- It was the test that was wrong all along (`f3a3376e`)
- struct() and invert()/inv() (`7b34f3f5`)
- Fix struct() and add mask() (`0039b600`)
- More struct tests (`ca9fef50`)
- add synth helpers (`c548b61f`)
- tone api doc (`4614b6b6`)
- build tutorial (`ce78e0d8`)
- scale now turns numbers into notes (`c5089d8c`)
- rename chordBass > rootNotes (`1380babc`)
- basi tonal api doc (`f0a96ba7`)
- build tutorial (`8339d18e`)
- midi doc (`b2646dbd`)
- build (`d3dce4c6`)
- fix could not play after pressing pause + some notes (`f95f1813`)
- build (`57d85ee7`)
- remove hot mode + cleanup (`873ca66d`)
- encapsulate repl logic into hook (`bad6cf0e`)
- boilerplate for tutorial + minirepl (`aa850918`)
- build tutorial after repl (`7b28a620`)
- tutorial layout (`3f051cdd`)
- support multiple repls on same page (`a05757f8`)
- docs docs docs (`5d649a51`)
- fixes (`da2c5c73`)
- dont autolink minirepl (`cd815d06`)
- disable parcel cache to fix build (`57bddf91`)
- build (`d26467d0`)
- simplify querying + hopefully fix mini repl switching bug (`d58a706b`)
- build (`c427b159`)
- simplify scheduler (`3c614e6e`)
- add bypass + hush (`9ac7212f`)
- add "!" and "_" to mini notation (`ef9527d6`)
- add Tone namespace + allow functions (`6babfa34`)
- support "!" in mini notation (`71023fc0`)
- add .tone + helpers (`70f85836`)
- eval only on ctrl+enter, not on every keystroke (`47d29bc6`)
- add tone songs (`b8d3fe6f`)
- build (`f97837d1`)
- add makeComposable for chainable composition - only used with transpose so far (`2f164649`)
- build (`c225c058`)
- Pattern .define + .bootstrap + seperate parse / evaluate + add Pattern.edit + move string hacks to evaluate + automate eval scoping with globalThis + add random tune button + mini: handle angle brackets + add chordBass + fix old tunes (`c11c217b`)
- build (`b64ccb45`)
- automated patternify, pseudo getters, string hacks (`240ebede`)
- build (`22baae10`)
- make strudel classes available to eval (`67c8ac7d`)
- build (`016d5c5e`)
- add tranpose method (`3d0d5260`)
- build (`b29857e8`)
- add scaleTranspose (`0c8d4a96`)
- build (`69d802a0`)
- add groove (`3fb4582d`)
- build (`f2acc121`)
- add chord voicings (`70dea049`)
- build (`64e24e5f`)
- fmapNested prototype (`c20d5e5b`)
- add giant steps (`2069b02b`)
- allow "^" in mini notation step (`ffffca8c`)
- build (`a0c09f75`)
- fix appWhole / appBoth (`8d81458b`)
- build (`b95daf78`)
- style fixes (`4c15cab0`)
- add swimming (`1e7cbf23`)
- build (`f15686ba`)
- add timecat to parser (`ad777ed1`)
- build (`fabfee59`)
- timecat mini notation (`8fa9fda9`)
- tune (`a977a9b4`)
- build (`ea7c079e`)
- save code as base64 in url (`cff3da3f`)
- build (`89863997`)
- Bugfix every, and create more top level functions (`abe6509b`)
- Failing test for `when` (`fff10f43`)
- fix wrong values in test (`a074c146`)
- ast shape shifting pseudo note variables (`9ed198db`)
- add m for mini (`e783de34`)
- build (`b0e50db4`)
- added midi support (`f6c4bdb8`)
- build (`c891dda8`)
- attempt at fastGap, with failing test (`ab2cfdee`)
- Create FUNDING.yml (`295e910c`)
- fix _fastGap, add compressSpan (`f5fb5857`)
- timeCat seems to work (`5b8c34e5`)
- deps (`9d809be7`)
- another test for fast (`c4a81805`)
- tidy - use const or let for declarations (`44336efb`)
- tidy (`89ee94e9`)
- pass all the args through patternify, to properly support sequences as args (`d6c22d51`)
- add some module level curried aliases (`fa59334a`)
- hot mode + tone synth experiments (`20cbaf91`)
- remove line (`ce579e48`)
- Fix slowcat for proper interleaving behaviour (`4cdcfd24`)
- Merge commit '4cdcfd24fa68ca37255ed391144c3ceeea5de976' (`651f6d14`)
- filter out events that dont have onset (`76153c57`)
- clean up (`8663f7f1`)
- build (`45ff4444`)
- fix duration (`d1befc29`)
- build (`cf7a1589`)
- rebuild with / docroot (`3e989f4b`)
- fix _slow test (`e12bcea6`)
- actually apply the function in `every` (`d3f4dcac`)
- fix tests for sequence() and polyrhythm() (`6f9c51db`)
- fix input error handling (`e9b2bb61`)
- build for root dir (`92103dbb`)
- fix typo (`4dadcbcb`)
- General fixes, breakages and WIP (`868a7b97`)
- Fix base url to / (`7812eb49`)
- rebuild (`cf9d6fb6`)
- CNAME (`47f051b7`)
- Patternify fast and fix tests (`e98e8ea1`)
- failed attempt at getting better test of fast() (`de7b67f0`)
- patternify slow, early and late (`988e8167`)
- add poc for krill parsing (`2062e423`)
- Merge remote-tracking branch 'upstream/main' (`4437f7c8`)
- tetris in mini notation (`06ebcfff`)
- build (`174c0135`)
- build with /strudel (`f56d40ac`)
- support stack with "," (`3919a216`)
- build (`bc4893b4`)
- add linebreaks (`5b7054b0`)
- build (`c8bf4331`)
- add many more krill operators (`27be9035`)
- build (`b91eeb72`)
- add some todos (`36db8441`)
- add codemirror (`ffe0556c`)
- slow bug as test (`557f3b77`)
- build (`a00f00a7`)
- now supporting tidal syntax directly (`bc79986d`)
- build (`8b17437e`)
- basic scheduling (`df865e32`)
- build (`6eae714d`)
- fix build (`07d534b4`)
- add nojekyll (`e024c091`)
- fix schedule dropout (`e9e76f72`)
- reify all the things (`96bac589`)
- add slowcat + stack example (`9d20c621`)
- build (`7fc10552`)
- cooler example (`1a56c636`)
- build (`01af71cd`)
- Merge remote-tracking branch 'upstream/main' (`74355e86`)
- Create CNAME (`0d266413`)
- update readme for local dev (`68c3450a`)
- add basic repl (`71a3bdfe`)
- js path (`fa7796ea`)
- code stuff (`83d06ec2`)

## Январь 2022

### 📦 Прочее

- fix and test rev (`2756155b`)
- jux (`eb6f685f`)
- sequencing (`b0979ba1`)
- prepare for npm (`c496002c`)
- 1.0.3 already (`d911e1f5`)
- mix strudel location (`e31fa95c`)
- fix strudel location (`caa28ce0`)
- more (`38cae8c9`)
- fix fastcat (`fc542861`)
- slowcat test (`c924057c`)
- off (`e53ec44d`)
- more (`b62571f9`)
- twiddles (`780a6482`)
- more blind porting (`d15ac429`)
- shenanigans (`f696cd55`)
- start porting pattern (`b5bafc8f`)
- init (`30b7eb7c`)
- more tests (`e0023a72`)
- get tests working (`615d4413`)
- Initial commit (`38b5a0d5`)
- ignore node_modules (`fb72bb22`)
- first (`40f2355f`)

---

*Сгенерировано автоматически: 2025-12-15*
