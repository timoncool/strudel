# Changelog Bulka

Все изменения проекта Bulka документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).

---

## Декабрь 2025

### ✨ Новые возможности

- добавить страницу Лента с галереей треков и системой лайков (`f78755d`)
- сохранение черновика чата в localStorage (`78dd067`)
- легкая плашка вместо модального окна для шаринга + объединение Пекарни и Ленты (`a8130ae`)
- 3 колонки в ленте на широких экранах (`641fcda`)
- добавить ссылку 'сообщество' в навигацию (`df6fb33`)
- добавить Hydra визуализацию к треку coastline в примерах (`ceca8b8`)
- добавить секцию поддержки проекта с графиком звёзд (`7139d93`)
- добавить 35 новых примеров треков в галерею (`7e3cca7`)
- добавить 35+ треков в галерею и примеры (`2033ed5`)
- добавить динамические Open Graph мета-теги для шеринга треков (`2586e02`)
- добавить полную документацию по Hydra на русском языке (`b6ff1ed`)
- update Supabase credentials for URL shortener (`4322c07`)
- обновление домена, сообщества и добавление Яндекс.Метрики (`6940255`)
- Integrate Sparkway Drum Kit sample pack (168 samples) (`7d965d0`)
- Add Sparkway Drum Kit and fix sample count display (`847acea`)
- Add expandable sample banks in sounds browser (`63f2687`)
- Add sample pack tools for AI agent (`d857ca0`)
- Add E-Pianos sample pack (CP80, Wurlitzer, Pianet) (`6cdfa57`)
- Add E-Pianos sample pack (CP80, Wurlitzer, Pianet) (`bd605ab`)
- Add agent behavior patterns and console debugging (`007349e`)
- add getExamples usage instructions for agent (`a1eb3a2`)
- add thinking protocol and workflow examples to AI agent (`4225b11`)
- add advanced API functions to knowledge base (`67467c5`)
- add 10 anti-patterns to agent prompt (`dc04ceb`)
- add music theory and composition knowledge to AI agent (`40dd36a`)
- add track structure guide to agent prompt (`fab1552`)
- add arpeggios usage guide and metadata tags (`40cd131`)
- Add version and commit hash to header and console (`031abdf`)
- Major UI/UX improvements and AI agent enhancements (`7465340`)
- Add audio recorder to header (`8354d7d`)
- Add AI Music Agent chat interface for vibe-coding music (`fd5879e`)
- Add AI Music Agent with full RAG on MD documentation (`57b7b26`)
- Update AI models to latest versions (GPT-4.1, o3, Claude 4.5) (`5aaa09e`)
- Implement full AI agent with tool calling (`c7a6890`)
- Add GPT-5/5.1 and Claude Opus 4.5 models (`45f4fba`)
- Artifact-style code editing + fix model defaults (`230493e`)
- Add markdown rendering in chat messages (`59a7543`)
- Improve AI music agent UX and system prompt (`14880d7`)
- Enhanced system prompt with detailed tools, Strudel reference and examples (~7k chars) (`74e6d58`)
- Enhanced system prompt with creativity, visualization, sliders and complex examples (`f7304b2`)
- Enhanced system prompt with creativity, visualization, sliders and complex examples (`23b6e6d`)
- Add volume control, undo/redo, selection support and highlight tool (`94b60cd`)
- Persist chat dialog in localStorage (`85e79f3`)
- UI improvements and rate limit handling (`3427c6c`)
- Add Claude extended thinking streaming support (`f78c24b`)
- Add GPT-5.1 Responses API with reasoning summaries streaming (`8a706ab`)
- UI tweaks and short URLs (`7f1d1e8`)
- Multi-provider API keys + Gemini support + reasoning models fix (`fdf8f96`)
- Dynamic model fetching from provider APIs (`95393fe`)

### 🐛 Исправления

- исправить позиционирование плашки шаринга (`aca75e6`)
- предотвратить дубликаты и пустые записи при шаринге (`95a0d43`)
- заменить первый пример на coastline с Hydra (`d6f8d0c`)
- исключить URL функции Hydra из парсинга mini-нотации (`57ca8ea`)
- разрешить перезапись уже расшаренных треков (`04c90e7`)
- брать код из редактора напрямую при шеринге (`05011fa`)
- использовать editorRef.current.code для шеринга (`225ff8c`)
- заменить иконки штруделя на булку для og:image превью (`68b3cc2`)
- заменить OG-картинку на версию с тёмным фоном (`9636d80`)
- заменить иконку REC на SVG с поддержкой цветов темы (`5526bcb`)
- Showcase newest first by default, add themes page (`fa4e750`)
- Remove oversized samples (>5MB) to fix build (`1234fb4`)
- Convert all samples to OGG format (168 files, 20MB total) (`049b179`)
- Show bank names in getAvailablePacks for agent (`3356bcf`)
- address rate limit errors for OpenAI and Anthropic APIs (`10bc1be`)
- Resolve rate limit errors based on analysis of top AI agents (`e25dc5f`)
- add critical warnings to enforce tool usage for samples (`31ca3f8`)
- correct .room().size() to .room().roomsize() syntax (`d398dbc`)
- syntax error and auto-versioning (`d99dedb`)
- escape backticks in code examples (`9c66e69`)
- Proper streaming and thinking for all providers (`b2646a6`)
- Real-time streaming for all AI providers (`abb4b6c`)
- Capture thinking signature for Anthropic conversation continuity (`5adf751`)
- GPT-5 series model support with reasoning_effort (`7ae7055`)
- Hide o-series models that don't support tools (`d7bd176`)
- GPT-5 models don't support temperature parameter (`b3e84d7`)
- Remove unsupported reasoning_effort parameter for GPT-5 models (`36e3450`)
- Version color, update AI docs, remove Mastodon link (`4448791`)
- Change default volume from 80% to 100% (`31b72c3`)
- Default volume 100%, fix falsy check for 0 volume (`e39e84b`)
- Show only commit hash in header instead of unused version number (`5f5ce29`)
- Improve audio recorder - WAV lossless, @title filename, UI position (`2ef6bc0`)
- Improve header spacing - more distance between elements (`1996584`)
- Switch to Vercel adapter for server-side API routes (`c37523d`)
- Copy docs_md to website for Vercel serverless access (`2fb3188`)
- Update Vercel outputDirectory to .vercel/output for Astro adapter (`ebe8f54`)
- Remove outputDirectory - let Astro Vercel adapter handle output automatically (`f7f43b3`)
- Copy .vercel/output to root after build for Vercel to find it (`910f28a`)
- Correct path for copying .vercel/output to root (`194ed89`)
- Update ChatTab colors to match other panel tabs (`9a7ddd8`)
- Add Claude Opus 4.5 as top model (`e3e964f`)
- Correct docs_md path for RAG search (`e6e28e0`)
- Update default Anthropic model to Claude Sonnet 4.5 (`834dd84`)
- Change superdough import to webaudio for Vercel client build (`3b95b0f`)
- Prevent AI agent from inventing sample pack names (`8222dc0`)
- Volume slider as floating overlay + mute state sync (`c1a7cdf`)
- Volume slider UX improvements and undo/redo fixes (`00bf123`)
- Update to December 2025 model names (`211f68d`)
- Use real working model names (`a504034`)
- Proper model filtering and thinking display (`297e768`)
- use simpleTune (Pixel Sunrise) as default (`1f5e08a`)

### 📚 Документация

- добавить примеры использования initImage и initVideo с URL (`2912f79`)
- добавить ссылку на внешние источники в навигацию (`406f9ed`)
- Add color themes documentation and improve gallery (`85e636c`)
- Add Sparkway Drum Kit to AI agent knowledge base (`0b8be5d`)
- Mark Sparkway Drum Kit for modern electronic genres (`83b1b57`)
- Expand sample pack descriptions with genre guidance for AI agent (`646a68d`)
- Add E-Pianos to agent knowledge base (`c830479`)
- Add e-pianos to agent genre recommendations (`a1b8756`)
- добавлен критический отчёт о несоответствиях в системе знаний агента (`0d95d5d`)
- comprehensive AI prompts analysis and recommendations (`eb0371b`)
- Add autonomous agents analysis from research (`b7a5df1`)
- Add Bulka AI Agent documentation page (`b1d722b`)

### ♻️ Рефакторинг

- улучшение README и создание отдельной страницы скриншотов (`c5d4d3d`)
- replace rigid structure with abstract music principles (`c88cdbe`)
- Simplify system prompt to ~1600 chars (`744f641`)

### 🔧 Обслуживание

- удаление мусорных MD файлов и обновление .gitignore (`3967642`)
- remove version numbering, use git commit only (`2d2f066`)
- Add .vercel to gitignore, remove build artifacts (`bf2097f`)

### 🔍 Отладка

- добавить логирование в shareCode (`08b2e28`)

### 📝 Блог

- Replace Strudel changelog with Bulka changelog (`425b579`)

### 📦 Прочее

- Описание изменений (`e2be366`)
- Описание изменений (`c995e5b`)
- Добавлен пак семплов Sparkway Drum Kit (`0898290`)
- Заменены жанровые примеры на каверы от eefano + обновлена Витрина (`40d054c`)
- Fix GitHub sample packs loading - pass empty baseUrl (`b815729`)
- Добавлены видео 2025 в Витрину + треки Nerual Dreming в Галерею (`d355a25`)
- Добавлены сложные примеры от сообщества в Галерею (`fe4bdee`)
- Удалены дубликаты треков Nerual Dreming из конца Галереи (`25ccfa8`)
- Добавлена полная документация в формате Markdown для обучения AI агента (`9f840f3`)
- Исправлена EN документация: добавлены все примеры кода (`6b375d2`)
- Set Pixel Sunrise as default tune (`222e6d2`)
- Fix default tune - exact Pixel Sunrise code (`54cba34`)
- Add samples() to load RolandTR808 drum machines (`90cb836`)
- Load all sample packs globally in prebake instead of per-tune (`818c248`)
- Update composer credit to Nerual Dreming (`886c609`)
- Add Vercel config to deploy full website instead of minimal example (`4dbc703`)
- Fix Vercel output directory - remove framework override (`2192d80`)
- Replace default tune with Pixel Sunrise by Nerual Dreming (`b90f290`)
- Rebrand project to Bulka - Russian fork of Strudel (`9890c75`)
- Fix Vercel build config - explicitly build website directory (`aa19cb9`)
- Clean up branding - remove excessive attribution, add noto:bread favicon (`881d536`)
- Локализация интерфейса на русский язык (`a219d78`)
- Обновлён README - фокус на проект, а не на форк (`babe5f6`)
- Исправлен switch в Panel.jsx - вкладки снова работают (`4cd6467`)
- Перевод встроенной справки API на русский язык (`b419466`)
- Перевод документации и туториалов на русский язык (`757a77e`)
- Перевод навигации и Technical Manual на русский язык (`1a8b387`)
- Добавлены кнопки копирования и вставки звуков (`75584a5`)
- Исправлена кнопка вставки звука в редактор (`d1e4161`)
- Обновлены тексты и добавлена информация об авторе (`ef3117d`)
- Добавлен фильтр по пакам для браузера семплов (`4e27152`)
- Добавлена документация по сообществу и продвинутым интеграциям (`582b9ee`)
- Add 5 new sample packs (4733 samples) (`5c6b869`)
- Add tree view component and UX spec for samples browser (`c1c18c0`)
- Integrate TreeView for samples browser (`8e52982`)
- Добавлена галерея треков от сообщества (`b467be3`)
- Fix samples browser and add local sample packs (`06a3bfc`)
- Исправлены примеры в галерее треков (`2a8e412`)
- Восстановлены оригинальные треки от сообщества (`e4bf28c`)
- Галерея с оригинальным кодом от сообщества (`eea28db`)
- Галерея с 15 треками от сообщества (`57395fb`)
- Load ALL Dirt-Samples via JSON + add local sample packs (`3a883b5`)
- Fix sample loading paths and add local sample packs (`f1e65e4`)
- Fix _base paths in local sample JSON files (`ad8e372`)

---

*Сгенерировано автоматически: 2025-12-14*
