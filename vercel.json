{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "astro",
  "installCommand": "pnpm install",
  "buildCommand": "cd website && pnpm run build && mkdir -p ../.vercel && cp -r .vercel/output ../.vercel/",
  "outputDirectory": ".vercel/output"
}
